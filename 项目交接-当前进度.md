# 项目交接 · 当前进度与注意事项（换对话/换电脑前必读）

> 本文件是**会话交接快照**，记录了"聚火拜访"项目进行到哪一步、有哪些未决事项、以及反复踩过的坑。
> 更新日期：2026-09-09 深夜（后台大改会话）。⚠️ 换对话先读下方「2026-09-09 深夜快照」与 §3.3 顶部部署项、§4 末尾新坑。详细设计见《开发计划.md》；账号与数据源见《miniprogram/项目账号与初始化资料.md》。

## 0. 2026-09-09 深夜会话快照（后台大改，全部改动已 staged 未 commit）

**部署待办（老板侧，按顺序）**
1. **重传 `adminapi` 云函数**（两项）：
   - ACTIONS 白名单补 `listRegistrations` / `reviewRegistration` / `setUserBoss`——此前三个 action 不在白名单，31 行校验直接返回 BAD_ACTION，**注册审核列表/审核通过拒绝/启用老板功能实际一直失效**
   - `listCustomerBatches` 新增返回 `summary`（客户级去重四数字汇总：total/in_task/free/visited；客户是多对多批次 batchIds 数组，Σ 各批会重复计数）
2. **重启壳程序**验证（壳已禁 F5/Ctrl+R，改 admin.html/nt-map.js/server.js 后**必须重启壳**才加载新代码）
3. 前端/admin/server.js 本地已全部生效；`nt-map.js` 引用版本号 **v=0924**

**本次大改清单**
- **任务管理大卡**：任务卡+历史卡统一为批次卡同款白卡（行节奏同批次卡）；首屏慢三连修——①`nav` 里 `taskListTitle` 悬空引用已删（此前对 null 赋值抛错导致 `loadTasks()` 永不执行、首屏只能等轮询，老板报障 10 多秒的代码级根因）②`startAuto` 进入工作台立即跑一轮全域拉取（不等刷新间隔）③server.js 启动即预热 token+云函数（云环境冷启动唤醒）；大卡高度跟随窗口自适应、页面不出滚动条（方案A：滚动条全局隐藏、滚轮可用）；新建任务按钮缩小置大卡右上角（上移 10px）；**任务地图标签**：visitStatus 修复（此前误用手机端字段名 visitedToday 导致已拜访也画红 pin）、图例变可点击开关（未拜访/拜访中/已拜访显隐，tmKindOn）、⭐仓库开关（默认关；进入/切天自动检测起点距仓库 50m；开=星在仓库坐标+直线路线以仓库为起点）、z 序=已拜访→仓库星→未拜访→拜访中、天页签居中、业务员下拉固定最右、刷新按钮已删、地图底部收缩 30px+图例上移 16px、`renderTmDay` 已 try/catch（出错显示到统计行）
- **客户管理**：批次卡两栏布局（左栏=名称+副标题+日期+＋新建任务+✏️改名铅笔｜1px 细竖线｜右栏=四数字+双进度条垂直水平居中）；顶部工具卡左侧四数字汇总统计区（云端 summary 重传后显示，无 summary 自动隐藏防假 0，新客工作台隐藏）；批次详情：行收矮、**固定每页 20 条翻页**（上一页/下一页、勾选跨页保留、搜索对全量过滤后重分页、末页补空行同高）、标题栏中央搜索框（店名/手机号，筛出 Y 家灰字，右移 50px）、进详情先隐藏「共 X 家」防上一批残留；DJ.mp3 音效：页面加载即预热缓存，挂批次卡/新建任务/导入新批次
- **位置监控**：全览 ⊡ 圆钮移入地图内导航球下方；trackMap flex 撑满
- **新建任务地图**：电梯圆标 ↓/↑ 修复（页面无滚动条后滚动容器改为 #page-newTask，双监听；地图全览圆钮已按老板要求删除）
- **server.js**：每次云函数调用打耗时日志 `[api] 动作名 Xms`（shell.log 可查）；启动预热
- **卡片 hover 辉光**：三卡（批次/任务/历史）hover 底部中央径向渐淡橙光（62% 宽 64px 高，0.6s 舒缓浮现，峰值 9% 极淡）

**新口径/数据**
- `getTask` 返回 `customers[].visitStatus`（visited/ongoing/pending）——后台一律用 visitStatus；`visitedToday/visitOngoing` 是**手机端 tasks 云函数**字段名，后台没有
- `listCustomerBatches` 返回 `summary{total,in_task,free,visited}`（客户级去重）
- 任务地图状态开关 `tmKindOn{visited,ongoing,pending}` 默认全开；仓库开关 `tmWhOn` 默认关

**新坑（详见 §4 末尾 29-33）**：TDZ 同名声明、marker 数组清理、ACTIONS 白名单、行尾还原脚本、壳拦 F5

## 1. 项目与当前阶段

- 名称：聚火拜访（小程序发布前需从"聚火配送"改名"聚火拜访"）
- 目录：`D:\WFR\visit-miniapp\`
- 形态：微信小程序（业务员，**正式版已发布过但新版本因二期地图页面被拒**，当前走体验版）+ Web 管理后台（老板 qingyan，**固定 8581 端口**）+ 微信云开发（免费环境 cloud1）
- 当前阶段：**第一期验收闭环 + 服务号长期通知（5 人全绑）+ 坐标报错审核链路 + 任务编号/拜访中单开/取消拜访——均已开发并真机验证通过**；**定位系列优化大改**（双档位刷新/提交定位 v3/高精度收敛加权多采点/审核改「待确定」）已开发完成，**待部署重传+真机测试**；剩余：上线前清单（改名/头像/改密码）、服务号两个新模板
- 数据：49 家回访客户、1219 条商城客户列表、3 个任务（第一批 18 家/第二批/第三批 15 家青恩艳）
- 二期（未开工）：地图点选/预规划路线、按实时位置重新规划路径、拍照录音；**坐标纠错审核已提前完成（§5 口径 19，注意 2026-09-06 已改「待确定」新口径）**
- **2026-09-06 全天改动明细见 §3.6**；部署清单见 §3.3 顶部。

## 2. 技术结构（三部分）

| 部分 | 路径 | 说明 |
|---|---|---|
| 小程序 | `miniprogram/miniprogram/` | 8 页：home/login/task/customer/visit/tasks-all/mine/map；app.js 已配 envId `cloud1-d0gwlmbwp31181eb5`，含全局审核观察员（reviewWatcher） |
| 云函数 | `miniprogram/cloudfunctions/` | init/ping/login/tasks/visits/subscribe/coordfix/bindadmin/seed/**adminapi** |
| Web 后台 | `admin/` | admin.html（单文件前端）+ server.js（**固定 8581**，微信云开发 HTTP API 直连 + /mpTokenRefresh 服务号 token 108 分钟同步）+ config.json（appid/appsecret/envId/**mpAppId/mpAppSecret**）+ xlsx 解析 + **tts_gen.py（云希男声双文案：任务审核/坐标报错）+ voice/（md5(名字+类型).mp3）+ notify.wav（任务审核响铃）+ bengbao.wav（坐标报错响铃）** |

- 启动后台：双击 `admin/启动管理后台.bat`（**固定 8581**；8080 被 EnterpriseDB Apache 占用，8081 被 Windows 排除端口段占用，都别用）
- adminapi 调用链：浏览器 → server.js /api → 微信 HTTP API invokecloudfunction → 云端 adminapi（账号密码 qingyan/123456，sha256 校验）
- **语音提醒依赖**：本机需 `pip install edge-tts`；server.js `/tts` 接口收 `{name, type}`（review=任务审核文案，coordfix=坐标报错文案），缓存按 md5(名字+类型) 隔离

## 3. 未决事项（换新对话后优先处理）

### 3.1 ✅ 天页签问题（已定位根因并修复，2026-09-03 老板已真机验证）
- 根因：`task.wxml` 天页签缺 `wx:for-item="d"` + `task.js` `switchDay` 字符串/数字类型比较。
- 已修并**老板真机验证通过**，无需再处理。

### 3.2 云函数云端测试的坑（别再浪费时间）
- `tasks/visits/login` 等依赖微信 OPENID 的函数，**云端测试必然报** `查询参数对象值不能均为undefined`（at where）——这是正常的，别用它诊断。
- 诊断任务数据用 `adminapi` 的 `getTask`（POST /api，body: action/taskId/username/password），已实测可用。

### 3.3 待部署/待验证队列（2026-09-08 晚间更新）

> **2026-09-09 深夜新增（最高优先，详见文首 §0 快照）**：
> ① 重传 `adminapi`（ACTIONS 白名单补 3 个 action + listCustomerBatches 返回 summary 去重汇总）；
> ② 重启壳程序（壳拦 F5，改 admin.html/nt-map.js/server.js 必须重启才生效；nt-map.js?v=0924）；
> ③ 后台验证：注册审核角标与审核通过/拒绝、人员管理「👑 启用老板」、客户管理工具卡四数字汇总、批次详情 20 条翻页与搜索、任务地图图例开关与仓库开关。

> **2026-09-09 新增：老板手机端四页已开发完成待部署（方案定稿见 §7.13）**：
> 重传 4 个云函数：`login`（openid 双占用修复：正式员工优先+绑定解占 trial）、`tasks`（boss 全量查询+虚拟 finish/replanDay+bossBoard/bossWar）、`visits`（boss 虚拟 start/submit/cancel/saveDraft+定位丢弃）、`coordfix`（boss 模拟提交）；上传小程序体验版（新增 bossWar 页+首页/map/mine/login 改造）；老板微信需已绑定 super_admin（bindadmin，开通码 JuhuoAdmin2026）→ 打开小程序点「👑 进入老板模式」。业务员端全部无感。
> **2026-09-09 修订：老板白名单**——老板页面仅对指定管理员账号启用（users.boss===true，后台人员管理管理员行「👑 启用老板」开关=adminapi setUserBoss；login 返回 canBoss 控制入口显示；云函数 boss 判定同步 boss===true）。老板指定账号后再开关。
> **2026-09-09 登录改版（老板拍板：注册→后台审核→通过后免登进入）**：小程序登录页四态=注册表单（姓名+手机号）/审核中/被拒可重提/游客入口（保留）；云端 registrations 集合（init 已补）+ adminapi listRegistrations/reviewRegistration（通过=手机号匹配已有业务员则绑 openid、不匹配则新建业务员）；后台人员管理第三 Tab「⏳待审核」红点+通过/拒绝；login bindUserId 仅限 trial（正式账号绑定一律走审核）。**丰炳全/宋子军/程绍君已从云端删除**（老板指示重绑；三人当前无进行中任务，删除安全；历史拜访记录 salesmanName 保留可见）——他们重新打开小程序会看到注册表单，填姓名+手机号提交后待老板审核。
> 程绍君误绑「实习」事件：login 已修复（见上）；后台人员管理已显示其「已绑定」→ 重传 login 后让其重开小程序实测（情形 A 双占用自动好 / 情形 B 需解绑重绑）。


> **2026-09-08 深夜~09-09 凌晨新增（状态信标 + M3 防漂移 + 文员热更新 + 位置监控大改，当前版本 v0.9.04）**：> ①**状态迁移信标（v0.9.02）**：`loc.beacon()` force 必写通道——6 个钩子（开始拜访/手动提交/取消/超时自动提交/超时自动取消/交任务）+ 云端 visitTimeoutTick 双态后同步 latest 文档 `visitOngoing=false`（不伪造坐标）；状态一变后台即刻感知。
> ②**M3 防漂移（v0.9.03）**：双层过滤——手机端 sampleTrack + 云端 reportTrack：精度>150m 丢弃；与前一保留点速度>30m/s（108km/h）判漂移剔除；片段过滤后<2 点整段丢弃。
> ③**v0.9.04**：adminapi listTasks 补 `salesmanId/customerIds/dayPlan`（任务客户实际在 dayPlan[].customerIds，此前位置监控"业务员→客户"映射永远为空 → 点业务员客户点全消失的根因）；total 家数同修。
> ④**文员热更新链路**：云端分发包存 `settings/admin_dist`（90KB 分片，云函数出入参 100KB 限制；uploadAdminDist 鉴权 / getAdminDistMeta+getAdminDistPart 免鉴权）+ server.js 两条路由（/admin-dist/check、/admin-dist/apply 分片拉取拼接写文件）+ 系统设置页「版本更新」区块（当前版本+检查更新按钮→有新版本变「⬇ 立即更新」→自动下载重载；状态文字情绪化配色：绿=最新/橙=发现新版/灰=暂无/蓝=更新中/红=失败）+ `tools/upload_dist.py`（**老板说"上传"才执行，绝不自动**；读本地 admin.html/nt-map.js+APP_VERSION 分片上传）。**标题栏刷新保持原功能、不查版本**。云端当前分发版本 **v0.9.04**。
> ⑤**位置监控页大改**：三卡片布局（地图→控件→业务员列表，div 平衡已验证）；控件卡=刷新时间(最左)/轨迹/客户点/仓库⭐/路况(默认关)/拾取/点大小(8~26 默认 16)/四色图例/⊡全览(最右)；业务员卡=chips+「全部」(最右)；**选中业务员=只显示他的任务客户点（其他客户点含灰点全消失），取消/全部=全显示**；图层 z 序=已回访>拜访中>未拜访>无任务；无任务灰点默认显示；地图 70vh+2px 白描边；**官方 API removeControl(DEFAULT_CONTROL_ID.ZOOM/SCALE) 移除 +/− 与比例尺、保留 3D 导航球**（GL 版不识别 zoomControl/showControl 参数——老板贴官方示例后查证修复）；**地图拾取器**：勾选即显示中心坐标胶囊（地图顶部居中，z-index 99999 压 canvas）、点胶囊=复制（剪贴板+execCommand 兜底、DJ.mp3 预加载严格一次性、弹一下动画、捕获阶段直调 copyPickCore——inline onclick 会被捕获 stopPropagation 拦掉，坑 28）；**店铺搜索框**（左上角：本地缓存匹配 8 条候选+地址两行+超出灰字提示、点候选平移+zoom16+金色高亮 5 秒）；**实时路况开关**（默认关，开启重建地图保留视野）；全局禁拖拽（拖动地图时鼠标变禁止/加号光标=HTML5 拖拽）。
> ⑥**设置页重排**：卡片 1000px；四组=📍定位与拜访（拜访定位校验/定位轨迹）、📢任务与通知（任务结束审核/服务号长期通知/过期任务自动归档）、⚙️系统（数据刷新间隔/任务编号区域码/版本更新）、⚠️危险区。
> ⑦**GitHub 私有仓库已建**：`git@github.com:hotakun/visit-miniapp.git`（Private）；.gitignore 隔离服务号 Secret/模板 ID/admin/config.json/客户数据表/缓存产物；**重要节点 commit+push**（首次 dc16ee3，最新 4d54648）。
> ⑧**审查加固**：refreshTrackPage 各绘制步骤独立 try（一步出错不拖累其他图层）；drawTrackMarkers 脏数据过滤。
> **部署提醒**：重传 `adminapi`（listTasks 字段+分发三接口+轨迹）+ `visits`（防漂移）+ 小程序体验版（beacon/防漂移）+ 后台刷新；壳必须重启才加载新 server.js（分发路由）。
> 待办遗留：定时触发器控制台手动创建（cron `0 */10 * * * * *`）；M3 覆盖分析未开工（待 M2 真机验收后老板拍板形态）。

> **2026-09-08 晚间新增（本段会话，明细见 §3.11）**：①**重传 `adminapi`**（导入超时修复/比对认领本地化/删批次并行——fetchAll 分页 1000、importCustomers 两遍处理+15 并发写库、conflicts 只返回 unresolved、deleteCustomerBatch/addCustomersToBatch 并行、**新接口 listMallLibrary/applyMallMatch**、runMallMatch 预规范化+字段裁剪）；②**后台 Ctrl+F5**（admin.html：导入断点续跑 index 修复、比对认领改为本地预览总览+应用认领、登录会话恢复 sessionStorage、任务行加高）；③**壳程序已换新**（老板本机 `admin/JuHuoVisitAdmin.exe` 与 `D:\JuHuoVisit\admin\` 均已复制 52,224 字节最终版）——重启验证：窗口居中、右键无菜单、F5 无效、最大化不盖任务栏；④**文员包已重打**（12.98MB 含新壳）发给文员覆盖安装，验证 2880×1800 屏最大化不盖任务栏；⑤上批遗留仍待办：`visits`/`tasks`/`coordfix`/`init` 重传 + 小程序体验版上传（现场证据/历史任务/批次管理）。
> 注意：比对认领前端已不再调用 `runMallMatch`（改本地比对），但云端 runMallMatch 保留作备份（importMallCustomers 兼容路径仍用）。
> 已完成：adminapi 编号版已生效；`visits` 已重传（老板确认，**待真机验证**）；小程序正式版已发布、体验版已传过多轮；后台已切 8581。**本地=云端基本同步，剩验证与个别确认。** §7.11 地图选店向导与 SVG 路线（方案 C）已开发完成并在本地生效。
> 2026-09-07 晚间新增：`adminapi` 需再重传一次（拜访目的三选的服务号通知文案映射：激活增单/走访维护/活动推广；resetTestData 若之前未传一并生效）。其余 09-06 部署项见下文。
> **2026-09-08 凌晨新增：现场证据（拍照+录音，二期提前做，§3.8）+ 任务历史板块（§3.9）**：`adminapi`+`visits`+`tasks` 三个云函数再重传一次（visits submit/history 收 photos[{fileID,thumbID}]≤4+audio{fileID,duration}；tasks detail 返回 recordingDurationLimit；tasks finish 提交/自动完成留痕 logs、业务员端 list 过滤归档；adminapi 新增 getTempFileURL/autoArchiveExpired、setSetting 启用 recordingDurationLimit(180/300/600)+expireArchiveDays(2/3/5)、listTasks 分区 mode active/history、任务动作统一写 logs 流水、审核留痕保留 finishReq、sendTask 不再重置 createdAt、editTask 通知日期修复）；小程序上传（visit/customer/task 三页 + utils/media.js 新文件）；后台 Ctrl+F5（任务页当前/历史 Tab、历史档案卡、详情页流程日志+审核档案、设置页录音上限与过期归档两个下拉、拜访详情弹窗照片缩略+audio 播放）。
> **2026-09-08 全天修订（§3.8 现场证据终版 + §4 坑 25/26）**：照片上限 3 张；拜访页区块顺序=拍照→录音→样品→记录→结果；照片缩略图铁律=单次 canvas 零分支零循环（320×240 中心裁切+quality70 固定）、显示格固定像素写死（拜访页 212×159rpx/坐标弹窗 88×66px/历史卡 84×63px）、mode scaleToFill；试听修复（闭包变量+obeyMuteSwitch+saveFile 转存）；历史卡录音播放控件（▶⏸+进度条+seek）；「拜访中·计时中」按钮文案；历史 ongoing 置顶排序；坐标修正拍照全链路启用（coordfix 收 {fileID,thumbID}≤3+后台审核弹窗预览）；init COLLECTIONS 补 customer_batches/batch_members；**文员后台程序包已重打**（含全部后台改动）。

- ⚠️ **待重传 `adminapi` 最新版**：smartSortDay 返回 polyline 真实道路轨迹 / purgeCancelled / listCoordFixes / reviewCoordFix / resetTestData / 服务号通知目的名映射（thing5 按 purpose 显示激活增单/走访维护/活动推广/新客开发）/ **客户批次管理全套接口（listCustomerBatches/getCustomerBatchInfo/renameCustomerBatch/deleteCustomerBatch/createManualBatch/archiveInitialBatch + importCustomers 两阶段分级匹配合并 + listCustomers 批次过滤批内状态 + createTask/sendTask/withdrawTask/deleteTask 批内状态联动，2026-09-07 已开发待部署）**——若未传，客户管理页批次列表报 action 不存在，导入走旧逻辑
- ⚠️ **待重传 `adminapi` + `visits`（2026-09-07 批次管理全套 + 两层状态模型 + 后续迭代接口）**：批次 CRUD（listCustomerBatches 含 visited 计数/getCustomerBatchInfo/renameCustomerBatch/deleteCustomerBatch/createManualBatch/archiveInitialBatch/removeCustomerFromBatch/addCustomersToBatch）、importCustomers 两阶段分级匹配合并、listCustomers 全局状态(in_task/free)+lastVisitAt、批次统计任务中/无任务——**未重传则批次页/详情页/删除/添加客户均不可用**
- ⚠️ **部署后执行一次初始归档**：客户管理页 → 批次列表空态 → 点「🏗 执行初始归档」按钮（把现有全部客户归入「2026年9月1日第一批导入」）
- ⚠️ **后台重启 + Ctrl+F5**：admin.html（登录记住我、按钮去粗体、拜访目的三选、清空当天、坐标文案待定/补标、客户批次两级页面、`nt-map.js?v=0921` 引用）
- ✅ 服务号 AppSecret 已换新并同步云端（2026-09-07 老板提供新 secret，本机 https 直调验证 token 获取成功；云端 settings.mpConfig 已更新含 enabled:true）
- ⏳ 上线前清单：小程序名称确认「聚火拜访」、头像、qingyan 密码
- ⏳ 服务号新模板：「新工单提醒」（发布任务）与「工单确认提醒」（审核结果通知）老板已选但未添加，添加后开发审核结果推送
- ⏳ **客户批次管理（方案定稿见 §7.12，待开工）**：开工后需重传 adminapi + 后台 Ctrl+F5，云端执行一次性初始归档（现有客户归「2026年9月1日第一批导入」）

**部署顺序建议**：①重传 adminapi ②后台 Ctrl+F5 ③真机按 §3.3 顶部新增部署项测试。

> **2026-09-08 深夜新增（定位升级方案定稿，待分期开工）**：持续定位三接口 `wx.startLocationUpdate` / `wx.onLocationChange` / `wx.startLocationUpdateBackground` 已获批。专项方案见 **《定位升级方案-持续定位.md》**（需求已对齐：后台实时位置/轨迹监督 + 拜访证据 + 端上体验 + 防作弊/覆盖分析全做；轨迹上传云端可回看）。
> 已拍板口径：**工作时间（默认 7:00~20:00）拜访中 5 秒/点、非拜访 30 秒/点；非工作时段独立四档（5S/30S 不降频 / 10S/60S 默认 / 20S/120S / 30S/180S），上传发包跟随档位、绝不空转；云端保留天数进系统设置（默认 30 天）；后台新增「位置监控」页面全业务员；后台定位仅拜访中开、拒绝不阻断；拜访时长上限三档（30 分钟/1 小时默认/2 小时，5 分钟前页内+服务号提醒；**点选结果即上报草稿，到点双态：已选结果→自动提交（跳距离、无照片录音）/未选→自动取消，均档案留痕**；动态闹钟+10 分钟定时触发器）并入 M1；M1 含最新位置上传（完成后台可见实时点；**工作时段正常写、非工作时段移动超 10 米才写**）与 mine 页位置说明「工作时段记录位置轨迹，仅管理员可见」**。实施按 **M1→M2→M3** 分期（M1 端上总线+地图实时距离+拜访时长上限+最新位置 → M2 云端轨迹存储+位置监控页+拜访证据 → M3 防漂移+覆盖分析+清理）；未开工，动工前先读专项文档（§2.1 时间分层、§5.5 时长上限、§10 挑毛病六问已定稿）。

### 3.4 本段会话改动清单（2026-09-04 会话，交接自上一会话）

#### A. 后台体验与通知体系（本轮重点）
| # | 需求 | 改动 | 验证状态 |
|---|---|---|---|
| 1 | 任务操作精简 | 撤掉「复制」「结束」全部按钮与功能（admin.html 三处 + adminapi copyTask/adminFinishTask 接口与函数全删） | 已清理待目视 |
| 2 | 任务详情顶部重排 | 信息卡+KPI 并入同一 4 列 grid；右侧新增**状态卡**（与 KPI 卡同尺寸、右端对齐第 3 列）；状态卡六态：草稿/已发放（附开始日期）/进行中/审核中（卡内同意拒绝+剩 N 家）/已过期/已完成（附"X月X日任务已结束"），左侧色条+同色系极浅底 | 待目视确认 |
| 3 | 任务列表增量渲染 | 15 秒轮询改为**指纹对比**：无变化纹丝不动；变化只更新相关行/统计栏；顺序变化才全量重绘；详情页 JSON 指纹不变不重绘 | 待体验确认 |
| 4 | 消息浮窗+提示音 | 右下角铃铛+未读角标+消息中心（本地持久化、清空）；新审核/新过期推送；审核中胶囊 13px 呼吸动画 2.2s；同意/拒绝悬停放大 18%、查看放大 10%+渐蓝 3s | ✅ 老板已体验 |
| 5 | 云希男声语音 | 审核到达：响铃（Winner WAV，admin/notify.wav）→ 1.6 秒后男声播「X提交了任务审核，请及时处理。」；edge-tts 免费接口（pip 装，不要 KEY）；**每人一份 mp3 缓存**（admin/voice/，md5 命名）；登录预热全部业务员；失败自动重试 3 次；0 字节坏文件自动重合成；并发去重锁 | ✅ 端到端验证（含故障重试） |
| 6 | A+B 全局提醒 | 任何页面 15 秒全局事件检测（响铃/语音/浮窗）；页面不可见时补 Windows 系统通知（点击直达任务详情） | ✅ 老板满意 |
| 7 | Web Push（C）已做后撤除 | 按老板决定：关闭标签页收不到可以接受 → **C 全清**（server.js 推送/订阅/VAPID/检测、admin.html 订阅/状态卡/测试按钮、sw.js、push_subs.json、vapid.json、web-push 依赖全删） | ✅ 已清 |

#### B. 手机端
| # | 需求 | 改动 | 验证状态 |
|---|---|---|---|
| 8 | 手机端被动收审批结果 | tasks 新增 `reviewStatus` 接口；app.js 全局审核观察员（有审核中任务才 15 秒轮询，无则停止）；home/task/tasks-all 三页监听，结果出来自动刷新+toast（通过=已完成✓/拒绝=继续执行） | 待真机验证 |
| 9 | wx.showModal 环境弹窗失败修复 | api.toast 改纯 showToast+截断；visit.js 提交成功 toast+900ms 自动返回任务页（原依赖弹窗 success 回调会卡死）；task.js 错误提示去 showModal | 待真机验证 |

#### C. 设置与口径
| # | 需求 | 改动 | 验证状态 |
|---|---|---|---|
| 10 | 任务结束自动审核通过 | key 改 `autoApproveFinish`：**勾选**=全部拜访完提交自动通过直接结束；**不勾选**=必须人工审核；提前交始终人工审核；布局=勾选框在文字前 | 待目视 |
| 11 | 拜访定位校验新规则 | 阈值 1~500 整数（勾选时校验）；**填 0 或不勾选=关闭**；默认 100 米；说明重写；云端钳制双保险 | 待目视 |
| 12 | 定位失败拦截 | 不再"待复核"放行：云函数返回 LOC_FAIL「定位失败，无法校验距离…」；前端文案同步；旧 pending_review 数据兼容保留 | 待真机 |

#### D. 杂项
- 侧边栏按 demo 全量 9 项：仪表盘/拜访记录/效果报表/销售导入/数据备份 5 项灰显不可点（开发中），已有点亮项不变。
- admin.html todayStr 改东八区（与云端一致）；通知列表 esc 防 XSS。
- 后台任务详情去掉了「自动刷新中·刚刚更新」胶囊（静默轮询）。

#### E. 服务号长期通知（2026-09-04 会话后段，§7.6 开发完成）
| # | 需求 | 改动 | 验证状态 |
|---|---|---|---|
| 13 | adminapi 服务号链路 | settings `mpConfig`（AppSecret 脱敏、留空保留旧值）+ `mpAccessToken` 缓存；`setMpOpenid`/`testMpSend` 新接口；`sendNotify` 服务号模板消息优先→订阅回退（createTask/sendTask/editTask/extendTask/reassignTask 全生效）；listSalesmen 返回 mpBound/mpOpenidMask | 语法通过，待重传联调 |
| 14 | 后台配置/绑定 UI | 设置页「服务号长期通知」区块（启用/AppID/AppSecret/模板ID/测试发送）；人员管理「服务号绑定」列＋绑定/更换/解绑弹层（openid 打码）；创建/发送成功提示区分服务号/订阅渠道 | 语法通过，待目视 |
| 15 | 手机端感知 | tasks `subStatus` 返回 mpBound；home 订阅横幅服务号绑定后隐藏；mine 提醒开关旁显示「服务号已开通」 | 语法通过，待真机 |

#### F. 拜访单开·取消·编号·UI 微调（2026-09-04 晚段，21:00~22:30）
| # | 需求 | 改动 | 验证状态 |
|---|---|---|---|
| 16 | 拜访中单开（老板口径） | visits start **单开检查前置**：同任务当天已有其他客户 ongoing → ONGOING_OTHERS「XX 家还未完成拜访」（**含已拜访客户的二次拜访也拦**，修了先放行 done 的漏洞）；无自动作废 | 云端旧版待重传 |
| 17 | 取消拜访不留痕（老板定稿） | visit 页「取消拜访」窄按钮（灰字细边框、距文字框 28px 防误触）；visits cancel=**直接删除记录**；adminapi cancelOngoing 改删除式 + 新增 purgeCancelled 全库清痕 | 已删青恩艳3条+范宇琨4条（果果蔬菜店） |
| 18 | 任务编号 | `JH05-混码`（如 JH05-42379761）：区域码 settings.taskRegionCode 可配；混码=(年尾数月日×1000+当日序号)×2654435761 取后8位，不可逆靠库查 | 云端已验证生成 |
| 19 | 卡片状态样式（老板定） | 拜访中=**蓝边框1.5px+蓝字+蓝点**（cust-cur/st-cur）；已拜访=**灰绿字 #5FB88A**（st-done）+整卡 55% 弱化保留；未拜访不变 | 待真机 |
| 20 | 苹果/鸿蒙修复 | textarea 固定高 140px（5 行）+cursor-spacing/show-confirm-bar（鸿蒙可输入）；submit 加「校验定位…」loading + 6 秒超时（苹果不再无反应） | 待真机 |
| 21 | 新增业务员青恩艳 | 手机号 15067951632、微信 qingenyan99；修复姓名乱码（命令行中文坑，UTF-8 文件方式重加）；服务号已绑定；语音 mp3 已合成 | ✅ |

#### G. 坐标报错审核全链路 + 语音通知（2026-09-04 深夜~05 凌晨）
| # | 需求 | 改动 | 验证状态 |
|---|---|---|---|
| 22 | 坐标报错审核 | 手机端电话行最右「📍 报错」→ 确认弹窗（新坐标+距离两行+备注两行 textarea+照片三宫格演示占位「拍照功能开发中」）→ coord_fix_requests(pending)；后台两处坐标列「审核」呼吸胶囊+弹窗同意/拒绝（同意才写回坐标，拒绝可再报）；tasks detail 返回 coordFixPending | 待真机 |
| 22+ | **报错定位升级（2026-09-06 老板定稿 v2，已开发待真机）** | 点「📍 报错」→ 立即取点**最多等 2 秒**弹窗（未出用缓存兜底）；弹窗 📍 图标可多次点击做**高精度收敛**（半径 **10m** / accuracy≤**30m**，其余规则同收敛终版）；成功 → 新坐标/距离刷新 + 质量行「✅ 已精确定位 · 精度 ±X 米」（X=通过窗口点平均精度，calm 已返回 accuracy）；失败 → 保持原显示 + **6→1 秒倒计时冷却**；初次单点质量行显示「精度 ±X 米」（缓存兜底无精度信息时为空）；坐标行下方展示质量（customer.js prepCoord/applyCoord/onCoordRefresh）；**弹窗坐标显示=纬度在前**（28.xxx, 120.xxx，老板 2026-09-06 定；后台各处仍经度在前） | 待真机 |
| 22++ | **加权质心+多采点（2026-09-06 老板拍板，已开发冒烟测试过）** | 质心=全部有效点按 **1/accuracy² 加权**（精度越高权重越大，±5m 点抵 16 个 ±20m 点）；**稳定后不提前结束**，采满目标有效点才成功（报错档 target=8/maxValid=12/maxAttempts=16 ≈10~30 秒，老板接受低效率换精度；提交页保持 target=5 快速档）；稳定性判定=最近 3 点滑动窗口 ≤ 半径；收敛期间报错弹窗质量行显示「🛰️ 正在精确定位 · 请保持不动」提醒；成功返回 accuracy=加权平均精度 | 待真机 |
| 22+++ | **审核口径改「待确定」（2026-09-06 老板定，已开发待真机）** | 同意**不再写回客户坐标**：客户 coord_status → `pending_confirm`，后台坐标列显示**暗红「待确定」胶囊**，点击弹窗只读查看业务员上报经纬度（原坐标不动）；coord_fix_requests 状态 confirmed 留痕；listCoordFixes 支持 customerId 查全状态；弹窗按 pending（同意并标记待确定/拒绝）/confirmed（只读）双模式渲染 | 待真机 |
| 23 | 坐标报错语音通知 | 轮询检测新 pending → **bengbao.wav**（蹦蹦报错.wav 复制的英文名）响铃 → 男声「X提交了坐标报错，请及时审核」→ 浮窗+角标+系统通知（只提示不直达）；/tts 双文案 type 参数、缓存 md5(名字+类型) | 待真机 |
| 24 | 服务号模板重配（5 词版） | 新模板 ID `kdgr7e7C-E8a3DZARR_zzmZMeFyFhAySs0XUk8VdDf4`；实测字段 character_string1(订单编号)/thing2(服务人员)/thing6(服务用户)/time5(服务时间=开始日期 00:00)/thing7(地点=「共X家客户，X天时长，X月X日开始」方案B)；5 个 sendNotify 调用点补 salesmanName/startDate/days；测试消息已发范宇琨 errcode 0 | ✅ 已发测试 |
| 25 | 拜访中禁止交任务 | 双层防御：task.js finishTask 前端拦截（有 visitOngoing → toast 不弹确认）；tasks 云函数 finish 加 ONGOING_VISIT（同任务当天 ongoing → 拒绝+客户名） | 待真机 |
| 26 | 天页签定位修复 | 未开始任务（todayDay<1）默认第 1 天（原错误跳最后一天）；进行中=当天；已过排期=最后一天 | 待真机 |
| 27 | 后台 UI 微调 | KPI 四卡配色（已拜访绿/待拜访蓝/任务客户橙/完成率紫，两工作台统一）；汇总行加「提交日」（finishedAt 东八区）+ 紧凑化（gap 8px/去 min-width/字号微缩）；客户表格 ST_MAP 曾改后撤回（保持原绿/蓝/灰） | ✅ Ctrl+F5 生效 |
| 28 | 定位阈值前端展示 | tasks detail 返回 locCheck（设置值）→ task 页 curCustomer → visit 弹窗三态（关闭/通过·阈值X米/超限·超过X米拦截） | 待真机 |
| 29 | 数据清理 | 青恩艳/范宇琨拜访中残留已清；新增 adminapi `purgeCustomerVisits`（按客户清空全部拜访记录恢复待拜访——果果蔬菜店待重传后调用）；purgeCancelled 全库清痕 | 接口已加待重传 |
| 30 | 果果蔬菜店 | 客户档案存在且 visitState=todo；名下历史拜访记录待 purgeCustomerVisits 清理（adminapi 重传后执行） | 待清理 |
| 31 | 动画需求撤销 | 圆环/圆弧漂浮动画（CSS+JS 两轮尝试均未见效）→ **全部撤销零残留**（home/task 恢复原样）；保留：h1 14px+上移10px、.sub 状态行下移10px（均 transform 不占布局） | ✅ 已撤销 |
| 32 | 环境/体检 | 8080 被 EnterpriseDB Apache 占用 → **固定 8081**（bat 重写 CRLF 纯英文）；login.wxml br 修复；adminapi ping 显式分发；全项目体检（JS/JSON/WXML/WXSS/事件/字段一致性）全过 | ✅ |

**本段遗留未闭环**：
- **收尾动作（老板侧，按顺序）**：①后台设置页把服务号「模板 ID」改成 `kdgr7e7C-E8a3DZARR_zzmZMeFyFhAySs0XUk8VdDf4` 并保存；②重传 `adminapi`（新 buildMpData 五词映射 + purgeCustomerVisits + listCoordFixes/reviewCoordFix）；③重传 `tasks`（locCheck/coordFixPending/ONGOING_VISIT）；④上传小程序（visit/task/customer/login/home 五页最新）；⑤后台 8581 + Ctrl+F5；
- **明早真机验证四件套**：①取消拜访（提示+自动返回+历史无痕）②单开拦截（大弹窗「XX店还未完成拜访」）③坐标报错（报错→后台 bengbao 响铃+男声+浮窗→审核胶囊→同意/拒绝）④拜访中不能提前交任务（前端提示+云端拦截）；
- 服务号：白名单=本机动态 IP（40164 第一排查项）；token 由 server.js 每 108 分钟自动同步（重启后登录一次即恢复）；5 人 mpOpenid 已全绑；
- 上线前清单（§7）：小程序正式版已发布——确认名称「聚火拜访」、头像、qingyan 密码；「新工单提醒/工单确认提醒」模板未采用（老板 09-05 决定沿用服务人员已接单提醒 5 词重配版）；
- 果果蔬菜店：待 adminapi 重传后调 `purgeCustomerVisits` 清空历史（恢复待拜访）；青恩艳尚未登录小程序绑定微信（首次打开选姓名）。

### 3.5 本段会话改动清单（2026-09-05 深夜 ~ 09-06 凌晨，§7.11 地图选店三步向导 + SVG 路线方案 C）

> 本段核心：把「新建任务」第 2 步升级为腾讯地图选店、第 3 步加智能排序与天数修正；绿色导航路线最终走**方案 C（SVG 自绘层，老板拍板）**，彻底绕开 GL Polyline 绘制。

#### A. §7.11 三步向导（已开发，待老板验收）
| # | 需求 | 改动 | 验证状态 |
|---|---|---|---|
| 33 | 三步向导主体 | 第 1 步任务名/业务员/天数/开始日期；第 2 步腾讯地图选店（五层圆点/店名 DOM/Shift 框选/天页签/「＋」加天胶囊/⭐ 智能规划）；第 3 步预览 + 📄 创建草稿 / 📨 创建并发送；createTask 支持 status draft/published + dayGroups | ✅ 本地跑通，待老板整体验收 |
| 34 | 五层圆点层级（老板定） | 顶→底：①当天已选（天彩色+白序号，点击取消）②待回访（浅灰点+深灰描边，点击选入）③其他天已选（天彩减淡，点击提示跨天）④任务中⑤已拜访（后两层深灰蓝不可点）；切天时①层整体换天、旧天店落③层 | ✅ |
| 35 | 店名双圆点开关 | 左=可选（默认亮）、右=不可选；可选店名文字底 50% 透明 rgba(255,255,255,.5)（JS 内联）；不可选店名无底色灰字弱化 | ✅ |
| 36 | 智能规划（单天） | 仓库起点 28.970802,120.154526（lat 在前）；云端贪心 3 候选 + driving 验真距离选最短；成功 toast 2 秒自动缩回；排序结果保留；该天构成变化 → invalidateDayPlan 失效（第三步显示「未规划」）；0 店天不显示底部行；规划过天数卡右上角绿色 ✓ 徽章 | ⏳ adminapi `smartSortDay` 返回 polyline 需**重传云端**（否则只有直线兜底） |
| 37 | 天数修正（老板定） | 第 2 步「＋」胶囊加天数（≤7）；天数与初始设置不同 → 必须修正，否则存草稿/发送都被拦（needsFixDays）；「🔧 修正任务天数」=去空天、紧凑重排、同步选择器/初始基准/截止日期/规划数据/路线，toast 结果 | ✅ |
| 38 | Shift 框选 | 透明遮罩捕获阶段拦截 + unprojectFromContainer 换算，圈中批量选入（15 家上限拦截） | ✅ |

#### B. 绿色导航路线：方案 C = SVG 自绘层（2026-09-06 老板拍板定稿）
- **背景**：老板截图看到「蓝色连续实线、无绿线无白色蚂蚁纹路」，与代码配置（绿 #16A34A + 白蚂蚁线）不符；服务器无缓存（no-store 实测 200）→ 判定 GL PolylineStyle 的 `dashArray` 与绘制链不可靠，老板拍板**方案 C：SVG 自绘层绕开 GL 彻底重做**。
- **实现**（`admin/nt-map.js`）：
  - SVG 覆盖层 append 进 `#ntMap`（`z-index:7`：盖住地图画布、垫在店名 10 / ⭐ 11 / 框选 12 之下）；`pointer-events:none` 不影响地图交互。
  - 双层 path：**绿实线 #16A34A 宽 4** + **白蚂蚁虚线 #FFFFFF 宽 3**，`stroke-dasharray="16 16"`（SVG 原生生效）；`stroke-opacity="0.5"`（**白色部分 50% 透明，老板 09-06 定**）。
  - 流动：JS 定时器每 150ms `stroke-dashoffset` 推进 2px（≈13px/s 舒缓行走感，老板确认的节奏）；不用 CSS animation（老板设备 CSS 动画曾失灵）。
  - 坐标：`projectToContainer` 经纬度 → 容器像素 → path d；`bounds_changed` 触发重投影（rAF 节流 scheduleRouteSvg）；`resize` 同步；容器隐藏时（clientWidth<2）不重画，回第 2 步 renderRoute 重算。
  - 层级保护：SVG mask 在**每个圆点位置挖孔（r=13）**，维持「圆点压在路线之上」的老板定稿层级。
  - ⭐ 起点、底部里程标签、店名 DOM 全部保留原样（DOM 方案）。
- **清退**：GL 的 `ntRouteLine`/`ntRouteDash`/`buildDashGeometries` 全部删除，零残留。
- **验证**：node --check 通过；DOM mock 冒烟测试全过（路径生成/挖孔/动画相位/无路线清空/隐藏容器保护）；老板真机确认「**这次真的是蚂蚁线了！**」→ 白色 50% 透明已交付（`?v=0907`）。
- **备份**：`_scratch/backup-nt-map-20260906-024850.js`（GL 版原样）。
- **防缓存机制（新）**：`admin.html` 引用 `nt-map.js?v=0907`；`server.js` 静态服务已修「剥 query string」（`req.url.split('?')[0]`，否则带 ?v= 的请求 404）；改 js 后 bump 版本号即可，老板 Ctrl+F5 双保险。

**本段遗留未闭环（2026-09-06 下午更新）**：
- ① ~~重传 `adminapi`~~ ✅ 已重传（smartSortDay polyline / purgeCustomerVisits / listCoordFixes / reviewCoordFix / purgeCancelled 全部生效）；
- ② ~~明早真机验证四件套~~ ✅ **2026-09-06 老板真机验证全部通过**（取消拜访/单开拦截/坐标报错语音/拜访中禁交任务）；
- ③ ~~果果蔬菜店~~ ✅ 老板已自行处理；
- ④ ~~地图选店整体验收~~ ✅ **2026-09-06 老板验收通过**（＋加天数/修正天数强制校验/绿色路线+50% 透明蚂蚁线全 OK）；
- ⑤ 上线前清单（改名/头像/改密码）；⚠️ 正式版提审被拒：审核方认为二期地图页面未完成——二期未做，正式版暂缓，先走体验版；
- ⑥ §7.9 实时距离+防漂移、§7.10 AI 日报评分（**未开工，勿提前动手**）。

### 3.6 本段会话改动清单（2026-09-06 下午）

#### A. 后台基础设施
| # | 需求 | 改动 | 状态 |
|---|---|---|---|
| 1 | 后台端口 | 8081 被 Windows/Hyper-V 排除端口段吞掉（EACCES）→ **固定 8581**（bat 已改；坑 20/24 已更新） | ✅ |
| 2 | 设置页服务号区块精简 | 「📨 测试发送」按钮、AppID/AppSecret/模板ID/测试OpenID 输入框、说明文字全删；只留勾选+保存+绿色 token 状态；saveMpConfig 只切开关；**adminapi mpConfig 留空一律保留旧值**（防清空凭据） | 待重传 adminapi |
| 3 | 设置页 UI | 任务编号区域码说明文字删除；4 个保存按钮改 `btn-sm` 小按钮（30px、纯色无阴影无渐变） | 待 Ctrl+F5 |
| 4 | 查公网IP小工具 | `查我的公网IP.bat` + `get-my-ip.html`（电影解密版：四段数字逐段翻滚锁定+光扫；Edge --app 模式 460×500 窗口，JS resizeTo 强制尺寸）；`get-my-ip-v1.html` 圆环版备份 | ✅ 已交付 |

#### B. §7.9 A 实时距离刷新（已开发，待真机验证）
- **双档位体系（2026-09-06 老板定稿 v3）**：页面分**正常页面**（默认 30 秒，档位 30/45/60，设置项 locRefreshInterval）与**重要定位页面**（默认 15 秒，档位 8/12/15/20，设置项 locKeyRefreshInterval）；后台「拜访定位校验」区块两个下拉（距离刷新间隔 + 重要页面刷新）与阈值共用保存按钮；tasks detail 返回 locRefresh + locKeyRefresh
- 当前重要页面仅有：**拜访记录页**（visit.js onShow 按 locKeyRefresh 档位刷，写全局缓存；退出/切后台停止）；老板后续再指定其他重要页面
- task.js（正常页面）：进页面/回前台立即刷一次；按 locRefresh 档位 setInterval；切后台/离页停止；指纹对比只 setData 变化的 distText；定位失败完全静默；成功后写全局定位缓存
- 触发规则（2026-09-06 老板定）：点天页签**不**触发刷新；点「开始拜访·自动计时」立即定位一次（customer.goVisit 静默写缓存）

#### C. 提交拜访定位交互重构（2026-09-06 老板定稿 v3，已开发，**待真机测试**）
> 背景：提交时重新定位经常超时/失败（任务页定时刷新与提交定位并发挤垮系统定位服务），且"距客户XX公里+定位校验已关闭"等解释文字体验差。
- **点「提交拜访」**：立即刷新一次定位，**最多等 2 秒**出结果再弹窗（2 秒未出也弹窗，用缓存值；晚到的新鲜结果自动更新弹窗距离与提交坐标）；定位失败静默用缓存
- **弹窗**：📍 图标 + 距客户 X 米/公里**纯距离**（无任何解释文字；**客户无坐标也显示距离行**，值为「—」；缓存空「—」）
- **📍 图标可多次点击**：每次点击做收敛精确定位；**成功**刷新距离+更新提交坐标、图标恢复原样可再点；**失败**保持原显示、图标变**6→1 秒倒计时冷却**（显示到 1 恢复可点，避免连续刷定位）；收敛期间图标 JS 字符轮换转圈（◐◓◑◒，**勿用 CSS animation——老板设备曾失效**）
- **收敛规则终版（2026-09-06 老板定稿）**：取点间隔 **1.5 秒**（串行不并发）；**不丢点**全部参与；有效点 accuracy≤50（0 未知不计）；满 3 个有效点即判定**最近 3 个滑动窗口**两两 ≤ 收敛半径 20m → 提前收敛（最快约 3~4.5 秒）；不满足继续**做足 5 个有效点**，仍不收敛 → 失败；最多 8 轮兜底；结果=通过窗口 3 点质心（冒烟测试三路径全过）
- **防并发（单飞）**：utils/loc.js getOne 全局同一时刻只允许一个定位请求在飞，后到者复用进行中结果（提交刷新/15 秒定时/收敛取点全走此通道）
- 确认提交=弹窗当前坐标（缓存或刷新/收敛后的最新值）；云端 visits submit 校验逻辑不变

#### D. 开始拜访按钮拜访中变蓝（2026-09-06 老板定）
- 客户详情页「▶ 开始拜访 · 自动计时」按钮：客户**拜访中**时底色变蓝（var(--blue) #2F80ED，与任务页拜访中卡片同色）；取消拜访/提交成功后恢复主色
- customer.js onShow 静默调 tasks detail 云端核对 visitOngoing（不依赖本地快照，返回页面颜色实时正确）

#### E. 拜访计时基准持久化（2026-09-06 老板定）
- 问题：切后台/页面重载回来计时重新开始（t0 内存值丢失）
- 改法：开始拜访成功时把开始时间写入 storage `visitStart_{taskId}_{customerId}`；进入页面读 storage 沿用旧基准（同一客户拜访中再进继续计时）；显示与提交时长都基于该基准；**只有取消拜访/提交成功才清除**

#### F. 拜访相关 UI 蓝色系（2026-09-06 老板定）
- 拜访页店名：单行不换行，超出省略号（.vname nowrap+ellipsis，仅拜访页）
- 拜访页「✓ 提交拜访」按钮：蓝色（.btn-blue = var(--blue)，仅此按钮）
- 计时胶囊：移到页面右上角与店名同行（.vhead flex）；**深蓝色底**（#1D4ED8 + 白字）
- 首页「我的任务」任务卡：任务内有拜访中客户 → **蓝色边框 1.5px**（tasks list 新增返回 `hasOngoing`：当日 ongoing 拜访记录按 taskId 聚合）
- 任务详情页天页签：拜访中客户所在的天 → **右上角蓝色 ✓ 圆角标**（task.js refreshShow 算 ongoingDays；.day-check 蓝色圆底白勾）
- 时长口径：显示与提交均基于持久化开始时间，后台记录时长正确
- 客户详情拜访历史：拜访中卡片蓝框辉光 + 蓝色「拜访中」胶囊（样式放 latest 之后保证蓝框优先）
- **二次拜访漏洞修复（2026-09-06 老板测出）**：visits start 删除"已完成则不覆盖"旧分支——已拜访客户再次进入同样创建拜访中记录，蓝色提示体系/历史卡片/计时全部生效；淡色已拜访卡片与蓝框并存（老板确认：淡色照旧+蓝框）

#### G. 后台分发打包（2026-09-06 老板定稿，**已完成并本机模拟安装测试通过**）
- **最终形态（老板拍板）**：zip + 安装.bat 双包；安装目录全英文（`D:\JuHuoVisit\env` + `D:\JuHuoVisit\admin`），bat 纯 ASCII + CRLF 杜绝编码问题
- **包 1「聚火拜访-环境安装包.zip」**（36MB）：node.zip 绿色 Node + setup-env.bat（检测 Python→pip 装 edge-tts→解压 Node 到 D:\JuHuoVisit\env\node→报告）
- **包 2「聚火拜访-后台程序包.zip」**（6.9MB）：admin/（后台全部文件+node_modules+voice）+ install.bat（释放到 D:\JuHuoVisit\admin + 桌面快捷方式「JuHuoVisit Admin」）+ uninstall.bat + icon.ico（聚火 LOGO 转 ico）
- **端口自愈已进 server.js**（2026-09-06 老板定稿）：启动端口被占自动 +1 顺延（最多 30 次）+ 成功后自动打开**Edge --app 无边框窗口**（--start-maximized 打开即最大化；无 Edge/Chrome 退回默认浏览器；NO_BROWSER=1 供测试跳过）；只认实际监听端口防双横幅；启动成功写 `current-port.txt`（壳程序读它加载页面）；支持 `--port=N --no-browser` 命令行参数（壳程序 spawn 用，绕开 .NET 环境变量传递坑）
- **无边框壳程序（2026-09-06 老板拍板，已开发并本机测试通过）**：`JuHuoVisitAdmin.exe`（40KB，csc 免 SDK 编译 C# 5 + WebView2 WinForms 官方包 1.0.4191.47；依赖 Microsoft.Web.WebView2.Core/WinForms.dll + WebView2Loader.dll 随包分发；Win10/11 自带 Runtime + .NET Framework 零安装）。功能：无边框窗体 + 顶部拖动条（双击最大化）+ 自绘最小化/关闭按钮 + **内置 spawn server.js**（--port=8581 --no-browser，优先 ..\env\node\node.exe）+ 读 current-port.txt 加载页面 + 关窗即停服务 + 单实例 Mutex + 每实例独立 %TEMP%\JuHuoVisitWV\<pid> 数据目录（防旧进程锁目录）+ 退出优雅 Dispose。⚠️ 调试坑：①taskkill /IM msedgewebview2 会杀所有 WebView2 应用，只能按 PID 杀 ②.NET Framework ProcessStartInfo.EnvironmentVariables 传参有坑 → 用命令行参数 ③壳卡死排查用 shell.log（LOAD/CREATE ENV/ENSURE/NODE 各步打点）
- **壳标题条样式（2026-09-06 晚~07 凌晨，仿小易管家规格 + 登录态显隐）**：44px 横向渐变条（左 #FDE3D7 乳橙 → 右 #F0502E 红橙）+ 左侧聚火 LOGO + 橙色粗体「聚火拜访」+ 深红圆角胶囊「管理后台」（自绘圆角底）+ 右侧 8 枚白色 MDL2 图标（通知/设置/锁定/全屏/最小化/消息/帮助/关闭；全屏·最小化·关闭为真功能，其余视觉占位悬停有提示）；**登录页无标题条**（右上角仅半透明悬浮 ✕ 可关闭窗口），登录成功 admin.html postMessage('logged-in') → 壳显示标题条隐藏悬浮钮（admin.html 加了 try 保护的非壳环境跳过）；壳在 admin/ 目录与启动管理后台.bat 并存，双启动方式可选
- **edge-tts 改离线安装（老板反馈在线装太慢）**：wheel 全套（Python 3.14 win_amd64 匹配，14 个包）内置环境包 `edgetts_wheels/`；setup-env.bat 走 `pip install --no-index --find-links` 秒装，失败才回退在线；已 pip --dry-run 验证完整性
- 打包工程：`packager/make_packages.py`（改内容后重跑即可出新包）；inputs/ 有 icon.ico + node-v24.16.0-win-x64.zip + edgetts_wheels/；shell/ 有 Program.cs + MiniTest.cs + lib/（WebView2 dll）
- ⚠️ 分发前提示：config.json 凭据随包（自家文员）；文员电脑公网 IP 需加服务号白名单（查公网IP小工具）；升级=只重新发包 2
- **分发状态（2026-09-06 晚）**：老板已把文员 IP 加进服务号白名单 ✅；两个 zip 在 `packager/output/` 待发；文员操作=解压→setup-env.bat→install.bat→双击桌面「JuHuoVisit Admin」
- **完全无边框窗口（2026-09-06 老板：以后再说）**：现状=Edge --app（无标签栏/地址栏，保留系统标题栏）；若要做真正无边框需 WebView2 壳程序 exe（自绘拖动区+最小化/关闭按钮，约半天工作量；Win10/11 自带 WebView2 零安装）；kiosk 全屏方案不适用（锁死无法正常关窗）

#### H. 2026-09-06 全天改动 · 文件与部署总清单（换对话后先看这张表）
| 文件 | 改动摘要 | 部署 |
|---|---|---|
| `admin/启动管理后台.bat` | 端口 8581（8081 被 Windows 排除端口段吞） | 已生效 |
| `admin/server.js` | 无改动（仅当天上午尝试方案 A 后已还原） | — |
| `admin/admin.html` | 服务号区块精简+mpConfig 只切开关；区域码说明删除；保存按钮 btn-sm；双档位下拉（正常 30/45/60 + 重要 8/12/15/20）；坐标列「待确定」暗红胶囊；openCoordFix 双模式（pending 审核/confirmed 只读）+「同意并标记待确定」；.pill.dred 样式 | **后台 Ctrl+F5** |
| `miniprogram/cloudfunctions/adminapi/index.js` | 新增 locRefreshInterval(30/45/60)+locKeyRefreshInterval(8/12/15/20)；mpConfig 三字段留空保留旧值；reviewCoordFix 同意**不再写回坐标**→coord_status='pending_confirm'；listCoordFixes 支持 customerId 查全状态 | **重传 adminapi** |
| `miniprogram/cloudfunctions/tasks/index.js` | detail 返回 locRefresh+locKeyRefresh；list 返回 hasOngoing（首页卡蓝框） | **重传 tasks** |
| `miniprogram/cloudfunctions/visits/index.js` | start 删除"已完成则不覆盖"（二次拜访也建拜访中记录） | **重传 visits** |
| `miniprogram/miniprogram/utils/loc.js` | **新建**：getCached/setCache/getOne（单飞防并发）/calm（加权质心+多采点：间隔1.5s、不丢点、最近3点窗口≤半径、target/maxValid/maxAttempts 参数化、返回加权平均 accuracy） | 随小程序上传 |
| `miniprogram/miniprogram/pages/task/task.js` | 距离刷新定时器（正常档位）+指纹更新；天页签不触发刷新；tryLocate 写缓存；goCustomer 传 locKeyRefresh；ongoingDays 天页签蓝✓角标 | 随小程序上传 |
| `miniprogram/miniprogram/pages/task/task.wxml/.wxss` | 天页签「.day-check」蓝色 ✓ 圆角标 | 随小程序上传 |
| `miniprogram/miniprogram/pages/visit/visit.js` | 提交定位 v3（点提交刷新最多等2秒弹窗/晚到更新）；📍图标可多次点击收敛（成功刷新/失败6→1倒计时）；计时基准持久化（storage visitStart_{taskId}_{customerId}，取消/提交成功才清）；拜访页按**重要档位** 15s 定时刷缓存；onShow/onHide 生命周期 | 随小程序上传 |
| `miniprogram/miniprogram/pages/visit/visit.wxml/.wxss` | 弹窗纯距离+图标三态（📍/转圈/倒计时数字）；店名单行省略；提交按钮蓝（.btn-blue）；计时胶囊右上角深蓝 #1D4ED8 | 随小程序上传 |
| `miniprogram/miniprogram/pages/customer/customer.js` | goVisit 立即定位写缓存；prepCoord/applyCoord/onCoordRefresh 报错高精度流程（半径10/acc30/target8/maxValid12/maxAttempts16）；报错弹窗坐标**纬度在前**；refreshOngoing 云端核对拜访中 | 随小程序上传 |
| `miniprogram/miniprogram/pages/customer/customer.wxml/.wxss` | 开始拜访按钮拜访中变蓝；拜访历史拜访中卡蓝框辉光+「拜访中」蓝胶囊；报错弹窗图标三态+质量行+「🛰️ 正在精确定位 · 请保持不动」提醒 | 随小程序上传 |
| `miniprogram/miniprogram/pages/home/home.wxml/.wxss` | 任务卡拜访中蓝边框（item.hasOngoing） | 随小程序上传 |
| 根目录 | `查我的公网IP.bat` + `get-my-ip.html`（电影解密版交付）；`get-my-ip-v1.html` 圆环版备份 | 已交付 |

#### I. 撤回/删除任务拜访中悬挂修补（2026-09-06 老板拍板）
- 问题：撤回任务后残留「拜访中」记录——后台任务详情草稿里仍显示蓝色拜访中（与客户列表"待回访"口径打架）；删除草稿会留下孤儿拜访中卡片
- 修补：adminapi 新增 `purgeOngoingOfTask(taskId)`（分页取该任务全部 ongoing 记录批量删除）；**withdrawTask 与 deleteTask 均先清理再操作**——撤回后客户=已拜访保持已拜访、其余全部待回访、零悬挂；已完成拜访记录保留
- 前端撤回确认文案加「拜访中」清除说明；visit.js beginTimer 加 **24 小时新鲜度校验**（防撤回+重发同 key 沿用很早的计时基准）

#### J. 新建任务地图·手动规划（2026-09-06 老板定稿，已开发+冒烟测试）- 工具栏新增「✍ 手动规划」胶囊（⭐ 智能规划旁）：**按鼠标点选顺序**规划路线（框选批量加入的排已点选之后——点选时 _seq 递增天然保证）
- 新增「仓库」起点开关（店名开关左边，**默认亮=仓库起点**；关闭=第一家店铺为起点）；**对智能/手动两种规划统一生效**；切换后需重新点规划生效（提示文案已带）
- 云端 smartSortDay 加 `mode:'manual'` 分支（按给定 order + driving 验真真实道路折线；起点=第一家时 waypoints 去重防接口报错；失败直线兜底按顺序连点）；auto 分支同样加起点去重
- 前端 planOrigin/fallbackPts 公共函数（⚠️ fallbackPts 起点对象必须转 [lat,lng] 数组——曾发现混类型导致 SVG 画线失败，已修）；⭐ 起点标记跟随 pts[0] 自动定位
- 手动规划结果与智能规划同链路：_seq 重排 → ntDayMeta 里程时长 → ntRoutes 绿线+蚂蚁线 → 第 3 步预览 → createTask 顺序下发业务员
- 防缓存版本号 bump：`nt-map.js?v=0908`

#### K. 测试数据重置（2026-09-07 老板要重新开始测试）
- adminapi 新增 `resetTestData` 接口：清空 tasks/visits/coord_fix_requests/mall_claims 全部文档；customers 状态归零（reviewFlag=false、coord_status 按坐标有无重置 ok/pending），档案/账号/设置/商城名单保留；返回各集合删除统计
- 后台设置页新增「⚠️ 重置业务数据」红色按钮 + 功能说明（清空范围/保留范围/不可恢复/仅测试用）+ **双重 confirm 警告确认**；执行后自动刷新任务与客户列表
- 部署：重传 adminapi + 后台 Ctrl+F5；执行入口=系统设置页底部按钮（qingyan 登录后操作）

**部署顺序建议**：①重传 adminapi、tasks、visits 三个云函数 ②上传小程序体验版 ③后台 Ctrl+F5 ④真机按 §3.3 顶部新增部署项测试。

### 3.7 本段会话改动清单（2026-09-07 全天，老板逐项验收中）

#### A. 登录页（老板逐项定稿）
- 新增「记住我」勾选（细体文案）：勾选登录后凭据 base64 存本机 localStorage（`jh_rem`）；下次打开**只自动填充账号密码不自动登录**，仍需点「登录」进入；登录失败自动清除已存凭据防死循环
- 登录页文案全部细体：记住我、「超级管理员与管理员专用 · 账号由系统预置」、「登录账号」「密码」

#### B. 壳程序（packager/shell/Program.cs，全部已编译部署）
- 胶囊「管理后台」字体换**阿里巴巴普惠体 55 Regular**（内嵌 TTF 随包分发 `admin/AlibabaPuHuiTi-3-55-Regular.ttf`，缺失回退微软雅黑）；修复字形压扁（手动高度 22 + 垂直居中 + 标准雅黑）
- 窗体阴影：`CS_DROPSHADOW`（最简稳定方案）
- 最大化改自定义：`Screen.WorkingArea`（**不盖任务栏**），双击标题条同逻辑；最大化时拖标题条自动还原
- 退出确认弹窗：回退**朴素系统 MessageBox**，一句话「确定要退出聚火拜访后台吗？」（自定义弹窗方案已全部移除）
- 删除 3 个装饰图标：锁定/信封(消息)/问号(帮助)；保留 通知/设置/全屏/最小化/关闭
- 清理未使用字段（lblTitle/logoBox），编译 0 warning

#### C. 后台 UI 全局（admin.html）
- **所有按钮文字统一普通字重 400**（.btn/.btn-sm/.ctab/.nt-daytab/wb-tag/进入→/电梯钮/重置数据按钮）；需要加粗的地方老板逐一点名（如「← 返回」按钮 font-weight:600）
- 侧边栏菜单、表格空态「暂无任务…」改普通字重
- 新建任务向导：底部按钮行改为 左「← 返回」(600) ｜ 右「上一步/下一步/创建」；顶部旧标题栏整块删除
- 基础信息卡片：加高约 60px（gap 14→36 + 上下 padding 8）；字段排列定稿——**第1行 任务名称|拜访目的，第2行 业务员|完成天数，第3行 开始日期|截止日期**
- 文案改名：坐标「待确定」→「待定」、「待补标」→「补标」、「未下过单」→「未下单」

#### D. 拜访目的三选（2026-09-07 老板定稿）
- 回访工作台三种目的：**💤 激活增单（橙）/ 🔁 走访维护（绿）/ 🎉 活动推广（琥珀）**；新客工作台保持 🌱 新客开发（蓝）。值：activate/maintain/promote/develop
- 新建任务第一步点击切换（默认激活增单）；任务列表/详情按目的显示图标胶囊；回访工作台过滤=非 develop；服务号通知 thing5 按目的显示中文名（adminapi 已改，**需重传**）

#### E. 地图选店页（nt-map.js，版本号 v=0921）
- 天页签右上角 ✓ 角标按规划方式变色：**智能=绿、手动=蓝**（ntDayMeta.planMode）
- 智能/手动按钮图标定稿：**绿色 ➤ 旋转向上（CSS rotate -90°）＝智能**、**蓝色 ✎ 水平镜像（scaleX -1）＝手动**；规划完成/失败恢复文案同步；loading 仍 ⏳
- 客户列表头部（搜索行右侧）新增「✕ 清空当天」小按钮（灰胶囊悬停红）：静默清空当前天所有已选（无任何提示），天页签角标/路线/地图圆点同步复原；「已选 N 家」保持全部天数总计口径

#### F. 审计清理（2026-09-07 全面体检）
- 全部 JS 语法/HTML 结构/死代码/console 残留检查通过；云函数干净
- 清理：重复音频 `蹦蹦报错.wav`（与 bengbao.wav 字节相同）、历史垃圾 `9.3对话问题.txt`
- 体检建议清单（供后续取舍）：壳锁定真功能、node 崩溃自愈、shell.log 轮转、壳重启自动触发服务号 token 同步、任务列表按目的分组统计

#### G. 客户批次管理（2026-09-07 开发完成，老板逐项验收调整中；方案见 §7.12）
- 数据模型：customer_batches + batch_members（纯名单）+ customers.batchIds；初始归档「2026年9月1日第一批导入」
- **两层状态模型（晚间老板想通定稿）**：全局状态=任务中/无任务（实时推导）；任务内状态三态仅任务详情；批次不持有状态
- 批次卡丰富版（老板定稿）：一张卡内——头部（批次名+右上橙色「＋新建任务」主按钮）/副标题/创建行/四数字大区（客户总数/任务中蓝/无任务/已拜访过绿）/双进度条（已派过任务橙渐变/历史拜访率绿渐变）/底部分隔线次操作（✏️改名/📥追加导入/🗑删除）
- 批次详情页（独立页，像任务详情）：顶栏「←返回批次｜批次详情页」+ 按钮（✏️改名/🗑删除/＋添加客户/📥追加导入/＋新建任务）；批次头卡（名称/副标题/共N家橙色加粗）；客户表格（#/店名/状态/地址shortAddr/手机号/坐标/最近下单/最近拜访/详情/删除✕列）
- 状态列=全局二态（任务中蓝/无任务—）；删除✕=从批次移除（有拜访记录禁删）；「＋添加客户」=未分批客户勾选入批
- **隐藏（功能保留，display:none）**：手工建批按钮、批次卡🗑删除、详情页顶栏🗑删除、详情表删除列
- 弹窗统一规范：默认 520px（大表格弹窗 680）、.mh/.mb/.mf/.mx 统一样式、底部按钮 36px 高
- 新建任务向导第一行「客户批次」下拉（默认最近批次+记住上次）；点批次卡「＋新建任务」真跳转新建任务页（隐藏批次页/详情页/topActions，侧边栏高亮任务管理）
- 云函数新增：listCustomerBatches（含 visited 计数）/getCustomerBatchInfo/renameCustomerBatch/deleteCustomerBatch/createManualBatch/archiveInitialBatch/removeCustomerFromBatch/addCustomersToBatch；importCustomers 两阶段分级匹配合并（S/A/B/C 口径）；**adminapi+visits 均需重传**

### 3.8 本段会话改动清单（2026-09-08 凌晨，现场证据：拜访页拍照+录音，二期提前做，老板逐项拍板）

> 老板定稿参数：照片**最多 4 张**、**缩略图双轨本期做**（上传双份：原图 ≤300KB/≤1280 + 缩略 ≤40KB/≤320）、录音档位 **3/5/10 分钟后台可设**（默认 5）、手机端+后台**全打通**。设计要点：**提交时才上传**（取消/退出零残留）；录音 mp3 16kHz 单声道低码率源头压缩（每分钟 ≈240KB，10 分钟 ≈2.4MB，免转码）；免费额度账目（存储 5GB≈17000 张；CDN 5GB/月 大头是后台回放，缩略图后浏览流量降 ~90%）。

#### A. 手机端
- `utils/media.js` **新建**：OffscreenCanvas 压缩链（原图 ≤1280/JPEG 质量循环/≤300KB + 320px 缩略 ≤40KB、EXIF 旋转处理）；`uploadFile` 直传云存储 `evidence/{ts}-{rand}.jpg/.mp3`；`getTempURLs` 批量换临时链接；`createRecorder()` 封装（mp3/16k/单声道/32kbps、stopAtLimit 自动停、试听）；chooseImage/prepPhoto/fmtSec
- `pages/visit/`（三件套）：📷 现场拍照区块（3 列瓦片、✕ 删除、📷 添加至 4、缩略预览、点击大图）；🎙️ 现场录音状态机（idle 开始红钮 / rec 计时跳动+■ 停止 / done 试听+↻ 重录；到后台档位上限自动停并 toast）；提交弹窗加 📎 证据清单行；**confirmSubmit 改两段式**——录音中先自动停（await onDone）→「上传现场证据…」逐张上传（断网重试跳过已传，up 标记复用）→ 再提交落库；TOO_FAR 等提交失败证据保留可复用
- `pages/customer/`：拜访历史卡加证据区——照片缩略行（点开 wx.previewImage 大图左右滑）+ 播放录音条（页面级单 InnerAudioContext，切换自动停、onEnded 复位）；onUnload destroy
- `pages/task/task.js`：goCustomer 的 curCustomer 带 `recordingDurationLimit`（tasks detail 返回 → 拜访页显示上限分钟）

#### B. 云函数
- `visits`：submit 收 `photos`（≤4 条 {fileID,thumbID} 强校验）与 `audio`（{fileID,duration}，duration 钳 1~600）；落库 doc.photos/audio；history 返回两者；注释序号顺延 4/5/6
- `tasks`：detail 返回 `recordingDurationLimit`（秒；档位 [180,300,600] 钳制，默认 300）
- `adminapi`：**启用预留闲置设置项 `recordingDurationLimit`**（init 已种 300，无需迁移；setSetting 加档位规范化 180/300/600）；**新增 action `getTempFileURL`**（fileID 批量换临时链接，分批 ≤50 防超时）；listCustomerVisits 补 `audio` 返回（photos 此前已预留）；**editTask 修复**：服务号通知用编辑后新日期（原传旧 t.startDate/旧天数）

#### C. 后台（admin.html，已保持 CRLF）
- 设置页「拜访定位校验」行内加「拜访录音上限 3/5/10 分钟」下拉，并入 saveLocSetting 一并保存；loadSettings 回填
- 拜访详情弹窗（openVisitDetail，任务详情点客户）重写证据渲染：**删除旧占位代码**（photos 曾按字符串 URL 渲染 [object Object]，与新对象结构冲突）→ 批量 getTempFileURL 换链接 → 缩略图列表（点击 window.open 原图）+ `<audio controls preload="none">` 播放条+时长；CSS 补 .vd-audio/.vd-adur

#### D. 体检与部署
- 全量 node --check 通过（10 云函数 + 8 页面 + utils + admin.html 内嵌 JS 提取 93KB）
- WXML 事件↔方法接线自检零缺失；visit/customer 历史证据在无图 URL 时静默降级
- ⚠️ **部署**：重传 adminapi / visits / tasks → 小程序上传体验版（visit/customer/task + utils/media.js）→ 后台重启 Ctrl+F5
- ⏳ **真机验证清单**：①拍照压缩达标（相册大图+横竖拍）；②录音 iOS/Android mp3 参数生效、到上限自动停、重录/试听；③提交慢网重试不重复传；④取消拜访后云存储无孤儿文件；⑤后台拜访详情弹窗照片缩略/大图/录音播放；⑥历史卡回放；⑦缩略图流量生效（devtools Network）
- 已知边界（可接受）：提交请求失败（TOO_FAR 等）后用户放弃本页 → 已传证据成孤儿文件（量小，后续清理工具再议）；录音编码参数若真机异常，调整集中在 utils/media.js REC_* 常量

### 3.9 本段会话改动清单（2026-09-08 凌晨，任务管理「历史任务」板块，老板逐项拍板）

> 老板定稿：历史范围=**审核正常完成（done）自然收录 + 过期达设置档位（2/3/5 天，默认 3）自动归档**；档位后台可设；入口=**任务管理页顶部「当前任务｜历史任务」Tab**；历史卡点开完整档案（拜访记录/流程日志/审核情况）；归档=终态封存（业务员端移除，如需重做新建任务）。与 §7.8 audit_logs（全局操作审计，最后阶段做）定位不同：这是任务维度永久业务档案。

#### A. 数据与口径
- tasks 新增统一流水 `logs[]`：`{at, by, role(admin|salesman|system), type, detail}`；type=create/send/edit/extend/reassign/withdraw/finishReq/reviewApprove/reviewReject/autoDone/archive
- **旧散装 editLog/extendLog/reassignLog/withdrawLog 只读保留**，getTask 时 mergeTaskLogs 合并排序成一条时间线（老任务档案也完整）
- 审核留痕补全：reviewFinishRequest 同意/拒绝均写流水（操作人/时间/可选原因 note），**拒绝/同意不再清空 finishReq**（保留作档案）；原 rejectNote 废弃
- **归档机制**：settings.expireArchiveDays（2/3/5 默认 3）；adminapi `autoArchiveExpired` 幂等（published 且 deadline ≤ 今天-N 天 → archivedAt=now + archive 事件）；后台 15 秒轮询 autoTick 每轮触发，归档发生即刷新任务页
- **sendTask 不再重置 createdAt**（创建时间保持真实；顺带修正任务编号当日序号计数）；createTask 直接发布=create+send 两事件
- 完成（done）不加 archivedAt（天然历史）；已过期未归档仍在当前列表（可延期复活，窗口=N 天）
- 业务员端 tasks list 过滤 `archivedAt 不存在`（归档后业务员看不到）

#### B. 后台（admin.html，已保持 CRLF）
- 任务页顶部「当前任务｜历史任务」Tab（.ptab 样式）；历史不参与 15 秒轮询/响铃（autoTick 按 taskTab 分流）
- listTasks 后端分区：mode active（!done && 无 archivedAt）/ history（done 或 archivedAt）/ all；history 按 endedAt（archivedAt||finishedAt||createdAt）倒序，fetchAll 全量不受 100 条上限限制
- **历史任务卡片**（.hist-card，点整卡开档案）：状态徽（已完成绿/已归档·过期红）、任务名+编号、目的/业务员/排期、进度条（完成灰绿/归档红）、结束说明（提前结束剩 N 家/全部完成/超期归档）+ 结束日期
- **任务详情页新增「📜 任务流程档案」卡**（dtArchiveCard）：流程日志时间线（图标+文案+时间+操作人，edit 显示 from→to 明细）+「📋 提交审核情况」（结束申请提交时间/提前或全量 → 驳回记录 → 结束时间/由谁/自动或人工）
- renderStateCard 归档分支：「已归档 · 过期」红卡（归档日期）；detailOpsHtml 归档任务零操作按钮
- 设置页「任务与通知」组新增「过期任务自动归档」下拉（2/3/5 天）+ saveExpireArchive；loadSettings 回填
- 详情页顶部四格 grid 同步归档态（dtStateBtns 空）；拜访详情弹窗（openVisitDetail）沿用可看照片/录音

#### C. 体检与部署
- node --check 全过（adminapi/tasks 云函数 + admin.html 内嵌 JS 101KB）；CRLF 已还原；接线自检（switchTaskTab/renderHistoryCards/renderArchive 等全部定义与调用齐）
- ⚠️ **部署**：重传 adminapi / tasks（与 §3.8 同批）→ 后台重启 Ctrl+F5
- ⏳ **验证清单**：①任务页 Tab 切换/历史卡样式；②完成一个任务后出现在历史卡（档案含拜访记录/照片/录音入口）；③过期任务到档期自动归档（可用测试任务把 deadline 改到 N 天前验证，或临时把档位设 2 天）；④归档后业务员端消失、详情只读无按钮；⑤编辑/延期/改派/撤回/审核动作在档案时间线正确显示；⑥历史任务不触发 15 秒响铃；⑦设置档位保存生效
- 已知边界：已删除任务不进历史（deleteTask 真删）；归档后无「还原」操作（老板口径：重新新建）

## 3.12 本段会话改动清单（2026-09-11，录音真机 bug 修复 + 拜访页 UI 大迭代 + 全域检查修复 + 战况文案 + 版本号统一）

#### A. 真机致命 bug：录音 1 分钟就停（✅ 已修已推送 da59654）
- **现象**：老板真机录音录到 1 分钟被系统截停，无论后台档位设 3/5/10 分钟。
- **根因**：`utils/media.js` 的 `recStart()` 调 `wx.getRecorderManager().start()` 时**没传 `duration`**。
  微信该参数**单位是毫秒、默认 60000（1 分钟）** → 系统层 1 分钟就切；代码里算好的 `limitSec`（后台档位换算）只喂给前端计时器 / `stopAtLimit`，**从未下发给录音器**。
- **修复**：`recStart()` 内 `const durMs = Math.max(1000, Math.round(limitSec * 1000))`，mp3 与 iOS aac 两条分支都传 `duration: durMs`。
- **影响面**：属**历史遗留**——单段录音时代一直如此（后台档位形同虚设），前端倒计时显示"剩余 5 分钟"是假的。

#### B. 录音口径变更（老板 2026-09-11 定，前后端同步）
- **段数**：5 → **6 段**（`visit.js` `maxSegs` / `visits` 云函数 `> 6` / `transcribe` 取片上限 8 → 6 同源）
- **单条上限**：**跟后台「拜访录音上限」档位走**（`settings.recordingDurationLimit`，180/300/600 秒，兜底 300）——
  老板明确要求"后台改了时长，前端要相应算出正确结果"，故**不可写死**；文案 `最多 {{maxSegs}} 段，单条最长 {{recLimitMin}} 分钟，合计 30 分钟` **动态显示**、整行居中
  · `visits.submit` 也**真读**该档位校验单条时长（此前硬编码 600 秒、注释却写"跟后台档位"，属误导，已修）
- **合计**：30 分钟硬封顶不变（1800 秒）

#### C. 拜访页 UI 迭代（老板逐条验收）
- **区块配色**：只有**标题栏**带极淡色（雾蓝/雾紫/雾杏/雾绿/雾玫），内容区纯白，**无左侧色条**
- **5 块全部可折叠**；进页面**默认只展开【现场拍照】【现场录音】**，样品/拜访记录/拜访结果默认收起
- **页面底色**加深到 `#D0D6E0`（仅本页）；取消拜访按钮 `margin: 48px auto 0`
- **照片格**：上限 15 张（连拍 + 相册多选）、序号无底色靠左下（白字 + 阴影）、✕ 靠右上
- **录音段卡片**：白底 → **从下往上 深绿 `#1E7A45` → 中绿 `#46B173` 渐变**；**去掉声音波纹**；
  序号 **22px** 加粗纯白（老板要求"很明显，免得删错"）；播放键白底深绿图标；勾选框白底；删除键 24px 半透明白
- **「开始转录」**：朴素白底小按钮靠右，`margin-top: 20px`、录音卡底部留白 +10px（老板要求"松弛一些"）
- **确认弹窗**：「再看看」上边距 10 → 18px、卡片 padding `20/18/16` → `22/18/22`；
  **「确认取消」「确认删除」改浅色**：`#FDEDED` 浅粉底 + `#E5484D` 鲜艳红字 + 字重 800 + 去橙色光晕 + 1px 淡红描边
  （原来 `var(--danger)` 正红实底太扎眼）；全项目已无正红实底按钮

#### D. 全域检查（老板要求"大师级排查"）与修复 —— 共 6 项，已全修
1. **P1** `visits.submit` 单条上限硬编码 600 秒、注释与实现不符 → 真读后台档位（见 B）
2. **P1** 多段转写【录音 N】**编号错位**：原来用"已成功段的下标 i+1"，某段未转写/失败时后续编号前移
   （真实第 3 段被标成【录音 2】）→ 改用转录记录的**真实 `segIndex`** 编号；
   `visits.history` 与 `adminapi.listCustomerVisits` **两处同步**
3. `visit.js` 初值 `recCountText '0/5'` → `'0/6'`；`recLimitMin 10` → `5`（与 segMaxSec 一致）
4. `transcribe` 段数上限 8 → 6（与手机端同源）
5. `adminapi.expiredTaskTick` 幂等非原子（后台 15 秒轮询 + 10 分钟定时器并发有重复补记窗口）
   → 改**条件更新**（`where _id + expiredLoggedAt 不存在 → update`），天然原子
6. 备注：`expiredTaskTick` 函数体被插在 `visitTimeoutTick` 中间（靠函数声明提升才不报错），可读性差，**待整理**
- **核对过、无问题**：`trEdited` 两条读取链路逐字一致（人工修订优先，重转不覆盖人工版）；照片口径三处一致（前端 15 / 历史卡 3 / 后台 8）；后台档位钳制 `[180,300,600]`；过期判定口径一致

#### E. 老板模式首页「今日战况」（文案按天轮换）
- 深色总览条原显示"N 人在线 · 今日完成 X/Y 家 · 待审核 N 单"，**与上面四张统计卡完全重复**
- 改为**战况文案池**（B 组 + C 组共 10 条，街道取**杭州真实路名**：延安路/庆春路/中山北路/凤起路/文一路/古墩路/建国北路/湖滨/武林），
  标题「今日整体进度」→「**今日战况**」；`home.js` 的 `BOSS_NEWS` + `bossNewsOfDay()`
  **按「距 1970 的天数」取模轮换**（同一天固定、隔天换一条，不存库不请求）；`.ov-tx{flex:1;min-width:0}` 保证折行；`.ov-t2` 32→30rpx + line-height 1.5
- ⚠️ 文案里的数字/街道是**氛围文案**（非真实统计）

#### F. 版本号统一 + 一键上传工具（重要机制）
- **前后端版本号统一为 `0.9.11`**：小程序 `app.js` 的 `APP_VERSION`（原有）+ 后台 `admin.html` 的 `APP_VERSION`（0.9.08 → 0.9.11）
- ⚠️ **认知纠正（老板指出）**：版本号**不是**"纯展示"——它是**「检查更新」的判据**：
  后台设置页点「检查更新」→ `/admin-dist/check` 拿**云端分发版本**比**本地 `APP_VERSION`**，
  不一样才提示"发现新版本"并出现「⬇ 立即更新」。**改后台代码却不 bump 版本号 = 文员永远更新不到**。
- ✅ **新增固定工具**：
  · `admin/upload_dist.js` —— 自动从 `admin.html` 读 `APP_VERSION`、检测 8581 是否在跑、
    按 90000 字符分片调 `adminapi.uploadAdminDist`（写 `settings/admin_dist`）、上传后回查核对
  · `admin/上传后台到云端.bat` —— 双击即用（CRLF、内容纯 ASCII、`chcp 65001` 输出中文）
- **完整链路**：改后台代码 → bump `APP_VERSION` → 双击 bat 上传云端 → 文员后台设置页「检查更新 → 立即更新」

#### G. 部署状态
- ✅ 云函数 `visits` / `adminapi` / `transcribe` **已由老板重传**
- ✅ 云端分发已更新到 **v0.9.11**（本次已上传）
- ⏳ **小程序待上传体验版**（本轮前端改动多：`media.js` 录音修复、`visit` 页、`home` 页战况）
- ⏳ 后台 Ctrl+F5

## 4. 反复踩过的坑（务必遵守）

1. **admin.html 是 CRLF 行尾**：edit_file 的 old_string 常因行尾/上下文不匹配失败 → 先 read_file 确认原文再改。
2. **免费环境云函数 3 秒超时**（老板已调到 30s）：大数据导入必须**分片**（前端每片 100 条循环调），比对认领独立成 `runMallMatch`，别把全量操作塞一个函数。
3. **集合不存在报 -502005**：新集合要在 init COLLECTIONS 登记 + 函数内 try createCollection 自愈（adminapi importMallCustomers 已做）。
4. **微信云开发 HTTP API**：POST body 必须直传（不能包 {data:…}）；返回字段是 `resp_data`（字符串要 JSON.parse）；@cloudbase/node-sdk 不认微信环境（历史错误 100003）。
5. **临时文件放 `_scratch/` 用完即删**，别留进项目。
6. **python 批量替换危险**：曾误删 CSS 前缀导致 home.wxss 损坏——改文件优先 edit_file。
6.5 **curl/命令行传中文必乱码**（2026-09-04 踩两次）：Windows Git Bash 下 `curl -d '{"name":"青恩艳"}'` 中文按 GBK 字节发送，服务端按 UTF-8 解析成乱码——曾导致①addSalesman 姓名存成乱码（人员管理显示 ◆◆◆◆◆）②青恩艳语音按乱码名生成无缓存。**凡请求体含中文，一律 node/python 写 UTF-8 JSON 文件 + `curl --data-binary @file`**（tts 接口、addSalesman、setSetting 同理）。
7. **8080 被老板旧服务占用过**：server.js 支持 PORT 环境变量。
8. **手机端旧预览码/云端旧函数**是"功能不生效"的最高频原因——先重传+新码再怀疑代码。
8.5 **后台别开两个 server 实例**（曾因双击两次 bat 导致两个进程抢 access_token → 40001 invalid credential）。server.js 已改用 stable_token 接口（force_refresh:false），多实例不再互踢；若再报 40001：关掉所有后台窗口重新双击启动。
9. **WXML 循环用自定义变量必须写 `wx:for-item`**：曾漏写导致天页签"第 天"无数字、选中态/点击全失效。
10. **dataset 传值一律是字符串**：与数字字段做 `===` 比较前必须 `Number()` 转换。
11. **拜访状态口径（2026-09-03 老板拍板）**：已拜访**跨天保持**——只要该任务内有完成拜访记录就显示"已拜访"（绿勾），不能按"仅今天"判断；后台"已回访/拜访中/待回访"同理；新建任务选客户三档：已拜访>任务中>待回访。
12. **新建任务选人/选客户的可选性**：回访工作台中"已拜访/任务中"客户不可勾选（灰实心框）；"任务中"业务员不可选（淡灰+红字"任务中"）。判定基于云端 published 任务与完成拜访记录，实时算。
13. **云开发 update 嵌套对象坑（-502001）**：对象字段要整体替换必须用 `db.command.set(value)`（曾导致"拒绝后再提前交任务"失败）。置 null 本身没问题。
14. **本环境 wx.showModal 原生弹窗打开失败（弹"弹窗打开失败"）**：小程序一律用页内自定义弹层或 wx.showToast（api.toast 已统一处理，超 14 字截断）；别在 success 回调里挂导航逻辑（弹窗失败会卡死流程）。
15. **edge-tts 三个坑**：①中文不能走命令行参数（Windows 编码乱码）→ 写 UTF-8 临时文本文件给 tts_gen.py 读；②微软服务偶发 NoAudioReceived → server.js 已自动重试 3 次；③合成失败会留下 0 字节 mp3 → 缓存判定必须查文件大小（已修）。
16. **Windows/浏览器通知权限**：系统通知不弹先查「Windows 设置→系统→通知→Edge 开关 + 专注助手」与「Edge 地址栏铃铛图标权限」，再看推送链路（2026-09-04 排查经验）。
17. **日期统一东八区**：前后端 todayStr 都用 UTC+8 计算（别用电脑本地时区，防时区错乱）。
18. **拜访中单开口径（2026-09-04 老板定）**：同一任务同一时刻只允许 1 家「拜访中」（visits start 拦截：同任务当天已有其他客户 ongoing → 返回 ONGOING_OTHERS「XX 还未完成拜访，请先完成或取消」，前端 toast+自动返回；拜访中的那家仍可进入继续）；**拜访页底部有「取消本次拜访」小按钮**（自定义确认弹层 → visits `cancel` 动作 → status='cancelled' 留痕；该客户仍为待回访，不计入任何统计；**取消无需定位**，任何地点可取消；**取消=直接删除记录不留任何痕迹**（老板后来定稿：visits cancel 直接 remove；历史 cancelled 已全清，adminapi purgeCancelled 全库清理接口）；**不做自动作废**（超时不清除，必须主动取消或提交，防呆靠关闭后重进时仍显示「拜访中」）。
19. **坐标报错审核口径（2026-09-04 老板定 → 2026-09-06 改）**：手机端客户详情页「📍 报错」在**电话行最右**（提交当前定位 → 确认弹窗显示新坐标+与原坐标距离+**备注输入框（选填 ≤100 字）** → 提交后提示待审核，页面显示「坐标审核中」黄标签、按钮变「审核中」灰）。提交进 `coord_fix_requests`（pending；note+photos 字段已预留，photos=云存储 fileID 数组，**二期拍照功能只改前端上传**；同客户有 pending 时重复提交被拦）。**后台客户管理列表与任务详情列表的坐标列**：pending 时「正常/待补标」替换为**大号「审核」胶囊（pill-big 呼吸放大）**，点击弹窗显示原/新坐标+距离+业务员+备注+照片数。**⚠️ 2026-09-06 新口径（老板改）**：审核「同意」**不再写回客户坐标**——客户原坐标保持不变，仅将客户 `coord_status` 改为 `pending_confirm`（后台坐标列显示**暗红色「待确定」胶囊**，点击弹窗查看业务员上报的经纬度，只读参考）；`coord_fix_requests` 状态改为 confirmed 留痕。拒绝仍不改任何东西（可再报）。接口：adminapi `listCoordFixes`（支持传 customerId 查全部状态）/`reviewCoordFix`；tasks detail 返回 coordFixPending（手机端标签用）。
20. **8080 被 EnterpriseDB Apache 占用；8081 被 Windows 排除端口段占用（2026-09-06 换端口 8581）**：后台固定走 **8581**（bat 已写死 PORT=8581）；浏览器 http://localhost:8581；出现「Server is up and running」= 访问到 Apache 了，先查 server.js 是否在跑、端口是否被占（tasklist 里 node.exe 可能是 Adobe 的，别误杀）。**若某天后台突然起不来报 `EACCES: permission denied`**：大概率又是 Windows 排除端口段漂移（Hyper-V vmms/vmcompute/hns 每次开机动态预留）——先 `netsh int ipv4 show excludedportrange protocol=tcp` 查 8581 是否在排除段，在的话换一个不在段内的端口改 bat（或管理员权限删段，但重启可能复发）。
21. **小程序动画不动（2026-09-05 经验，需求已撤销）**：CSS animation 与 wx.createAnimation 两轮尝试在老板设备上均未生效（含模拟器），**动画类需求先撤销零残留**；今后做动效前先在开发者工具模拟器验证再谈真机；WXML 禁止 HTML 标签（div/br 白屏）、bat 必须 CRLF、命令行中文必须 UTF-8 文件法——均已踩坑并记入前文。

- **拜访中禁止交任务（2026-09-05 老板定）**：有客户「拜访中」时**不能提交任务结束（提前交也不行）**——前端 finishTask 拦截提示 + 云端 tasks finish 返回 ONGOING_VISIT 双层防御；先完成或取消拜访才能交。
- **后台 KPI 四卡配色（2026-09-05 老板定）**：已拜访=绿、待拜访=蓝、任务客户（新客版=已入商城）=橙、完成率=紫；新客/回访两工作台统一；客户表格状态胶囊保持绿/蓝/灰不动。
22. **GL PolylineStyle 的 dashArray 不生效（2026-09-06 老板发现）**：蓝实线无虚线无流动，与代码配置完全不符 → **路线彻底放弃 GL 绘制，改 SVG 自绘层（方案 C，老板拍板）**；SVG stroke-dasharray 是浏览器原生能力 100% 生效。今后 GL 上任何"不生效"的属性不要恋战，直接换 DOM/SVG 层。
23. **server.js 静态服务不认 query string（2026-09-06）**：`nt-map.js?v=0907` 这类带版本号的请求被当文件名 → 404。已修 `req.url.split('?')[0]` 剥 query；**后台防缓存机制=HTML 引用加版本号（?v=0907），改 js 后 bump 版本号**（no-store 响应头+版本号双保险）。
24. **server.js 默认端口是 8080**（代码 `process.env.PORT || 8080`）：老板环境必须 `PORT=8581`（bat 已写死）；**手动重启一定要带环境变量**，否则起在 8080 和 EnterpriseDB Apache 抢端口。2026-09-06 曾误起 8080 实例已 kill；同日 8081 被 Hyper-V 排除端口段吞掉（EACCES），已换 8581。
25. **照片缩略图与照片框显示【铁律，2026-09-08 老板花 30 元教训定稿，任何时候不得违反】**：①缩略图=**单次 canvas 绘制，零分支零循环**（中心裁切 320×240 固定像素 + quality 70 固定；**严禁质量降档循环/缩边循环/EXIF 旋转分支**，微信 canvas 自动处理方向）——曾因三条可变路径导致"一会儿对一会儿错"被反复退货；②原图=单次等比缩 ≤1280（小图直接用原文件）；③显示格=**宽高固定像素写死**（拜访页 212×159rpx、坐标弹窗 88×66px、历史卡 84×63px），**不依赖百分比/均分/padding-top hack**；④图与格同 4:3 → scaleToFill。**被否方案勿再用**：aspectFill 裁切、widthFix 高度自适应、letterbox 白底、1:1 方形裁切、min-width+absolute。老板最终认可口径：三格绝对一样大 + 缩略图中心裁切丢内容没关系 + 原图保留点开看。代码：`utils/media.js`（drawOnce/coverJpg/prepPhoto）、`visit.wxss`（.picgrid/.pictile）、`customer.wxss`（.c-ph/.hev-pic）。
26. **InnerAudioContext 播放录音【2026-09-08 修复双坑】**：①试听无声根因=封装的 onStop 箭头函数里 `this.path` 写到模块对象、`play()` 读实例 this.path——路径存储**必须用闭包变量**（recPath/savedPath）；②iOS 静音键→`obeyMuteSwitch=false`；③播放前先 `aud.stop()`；④wxfile://tmp 临时文件先 `saveFile` 转存再播。
26+. **iOS 录音平台分化【2026-09-08 老板反馈，鸿蒙正常 iPhone 显示失败】**：微信 iPhone 端 RecorderManager **不支持 mp3 格式**（只认 aac）→ 按 `wx.getSystemInfoSync().platform` 分化：iOS=aac 16kHz 单声道（不传码率）+ 上传后缀 .m4a；安卓/鸿蒙=mp3 16kHz/32kbps + .mp3；**mp3 启动失败自动降级 aac 重试一次**（兜底）；**iOS 录音上限单独封顶 5 分钟**（media.recLimit：后台档位 3→3 分钟，5/10→iOS 最多 5 分钟，安卓/鸿蒙按档位）。后台 <audio> 播 m4a 原生支持无需改。代码：`utils/media.js`（isIOS/recLimit/createRecorder uploadExt）、`visit.js`（上限显示/计时/上传后缀）。
26++. **iOS 录音提交失败【2026-09-08 老板实测二次修复】**：现象=试听第一次弹「播放失败」但实际有声（iOS 空实例 stop 误报 onError）；**试听后提交弹「证据上传失败」**。根因=`FileSystemManager.saveFile` 是**移动**临时文件（调用成功后原 wxfile://tmp 已失效），试听转存后上传仍用旧 tmp 路径 → 上传已消失文件失败。修复：①转存成功后**回写 recPath**；②上传统一走 `recorder.getUploadPath()`（ensureSaved：tmp 自动转存再返回持久路径，试听/上传共用）；③aud.onError 仅 `playingSrc` 非空时提示（消除误报）。

27. **微信 RecorderManager 的 `duration` 单位是毫秒、默认 60000（2026-09-11 真机踩坑，现象="录音 1 分钟就停"）**：
   `rm.start({ format, sampleRate, numberOfChannels, ... })` **不传 `duration`** → 系统层 1 分钟自动截停。
   前端算出的 `limitSec`（后台档位换算）只喂给了自己的倒计时 / `stopAtLimit`，**管不到系统层**。
   凡要限制录音时长，必须 `rm.start({ ..., duration: 秒数 * 1000 })`。排查"录音时长不对"先看这条。
28. **后台版本号 = 「检查更新」的判据，不是"纯展示"（2026-09-11 老板纠正我的错误认知）**：
   `admin.html` 的 `APP_VERSION` 与云端 `settings/admin_dist` 里的包版本比对，不一致才提示"发现新版本"并出现「⬇ 立即更新」。
   **改后台代码却不 bump `APP_VERSION` = 文员永远更新不到**（会显示"已是最新 ✓"）。
   完整链路：改代码 → bump `APP_VERSION` → 双击 `admin/上传后台到云端.bat` 上传云端 → 文员设置页「检查更新 → 立即更新」。
   ⚠️ 若本地版本已高于云端（如刚 bump 还没上传），文员点检查更新会看到"发现新版本 v<旧版本>"——**别点「立即更新」（会降级）**，直接重新上传即可。

### 3.10 本段会话改动清单（2026-09-08，手机端地图页·二期核心，老板逐项拍板）

> 老板定稿：**进入方式=home 页底部「🗺 地图」按钮直达（任务页「打开地图」按钮不动）**；地图范围=全天可切换；导航=地图内显示路线与距离+可跳外部原生导航（wx.openLocation）；顺序引导=要（下一家引导条）。任务页/首页零改动，地图页独立自取数据。

#### A. 数据链路
- 后台创建任务（admin.html doCreate）：收集每天规划路线 `ntRoutes[d]` → `dayRoutes` 数组（每项 {pts[[lat,lng]...], distanceMeters, durationMin} 或 null）随 createTask 上传
- adminapi createTask：`normRoute` 校验（pts≤800 点、6 位小数、字段数字化）→ 存入 `dayPlan[i].route`；未规划/null → 手机端按客户顺序**直线兜底**
- tasks detail 原样下发 dayPlan（自动带 route，零接口改动）

#### B. 地图页（pages/map 三件套重做，独立自取）
- 数据：tasks list → 取 published/reviewing 任务 → detail 全量（客户坐标/状态/route/设置）；无任务显示空态提示
- **天页签**：第 1 天~第 N 天（>1 天才显示），默认今天（todayDay 钳制）
- **地图**：`show-location` 自带我的位置蓝点；客户点按状态 label 分色（待拜访橙/已拜访绿✓/拜访中蓝）+ 顺序序号角标 + 店名 callout；**绿色路线**（route.pts 真实道路轨迹；无轨迹按顺序直线连点）
- **下一家引导条**：当天顺序第一家未完成 → `👉 下一家 · 店名 · 距你 X`，右侧「🧭 导航」直达；点条看卡片
- **底部客户卡**（点 marker/引导条滑出）：店名+状态胶囊+地址+距你；按钮=「🧭 导航」（蓝）／「去拜访」（橙，存 curCustomer 进客户详情页开始拜访，与 task 页同构传 locCheck/locKeyRefresh/recordingDurationLimit）
- 定位：进入立即刷 + 按任务 locRefresh 档位（默认 30 秒）刷新距离；onHide/onUnload 停止

#### C. 体检与部署
- node --check 全过（map.js/adminapi/admin.html 内嵌）；事件接线零缺失；CRLF 还原；文员包已重打
- ⚠️ 部署：重传 adminapi（createTask 存 route）→ 上传小程序体验版（map 三件套）→ 后台 Ctrl+F5
- ⏳ 真机验证：①从 home 底部「地图」进入；②天页签切换；③当天绿色路线与后台一致；④下一家引导+导航跳转；⑤点客户点卡片「去拜访」正常开始拜访；⑥无路线任务的直线兜底；⑦我的位置蓝点与距离刷新
- 已知边界：旧任务（创建时未存 route）手机端走直线兜底；重新创建/编辑后的任务才有真实路线
27. **工作方法论心得（2026-09-08 老板 30 元教训换来的四条）**：①**处理图片/音频等"生成结果不稳定"的功能，代码路径必须恒定**——禁止引入随数据变化的分支、循环、降级策略（质量循环/缩边循环/EXIF 旋转分支曾导致"一会儿对一会儿错"），宁可牺牲一点体积/质量，换行为 100% 可预测；②**布局尺寸能用固定像素写死就写死**，不依赖百分比/均分/padding-top hack（flex 内百分比参照曾变"细高"）；③**显示类问题反复修不好时，第一件事是要截图**，凭文字描述盲改=烧 token（本条教训请务必遵守）；④**删除数据库记录必须连带删除关联的云存储文件**（resetTestData 已升级为删库+cloud.deleteFile 文件级清理，仅"已无数据库引用的历史孤儿文件"无法程序枚举，需云开发控制台手动清）。

28. **壳程序 DPI/任务栏坑（2026-09-08 文员 Win10 2880×1800 最大化盖任务栏，已修）**：无边框 WinForms + WebView2 组合下，**必须在 `Main` 第一行声明 `SetProcessDpiAwarenessContext(-4)`（PMv2）**——否则 WebView2 初始化会**中途**把进程提升为 DPI 感知，窗口逻辑坐标与 `Screen.WorkingArea` 物理坐标错位 → 最大化盖任务栏；初始尺寸/最大化一律用 `WorkingArea` 物理像素 + **手动居中**（`CenterScreen` 在无边框大窗会偏到屏幕右下角——老板 2560×1440@110% 实测）；标题栏/按钮物理尺寸按 `DpiX/96` 放大（高缩放屏观感一致）；WebView2 `Settings.AreDefaultContextMenusEnabled=false`（禁右键）+ `AreBrowserAcceleratorKeysEnabled=false`（禁 F5/Ctrl+R，Ctrl+C/V 不受影响）。改壳后必须：csc 重编译 → **关掉壳再复制 exe**（Permission denied=壳在运行）→ 复制到 `admin/JuHuoVisitAdmin.exe` 与 `D:\JuHuoVisit\admin\` → 重打文员包。
29. **免费环境云函数 30 秒超时的三种解法（2026-09-08 一天踩完）**：①**批量写库必须并行**（`runPool(items, 15, fn)` 15 并发分批；串行 100 条 × 2 次 add 共 200 次往返必超时）；②**fetchAll 分页用 1000**（云函数端 limit 上限就是 1000）；③**纯计算类比对搬到浏览器本地**（云端只拉库+写决定，如商城比对 `listMallLibrary`+`applyMallMatch`）。前端导入分片仍是 100 条/片（IMP_PAGE）+断点续跑。
30. **TDZ 同名声明坑（2026-09-09 深夜，批次详情表格整页消失的元凶）**：函数内残留 `const unbatchMode` 而函数开头给"同名全局变量"赋值——**const 的暂时性死区覆盖整个函数作用域**，开头赋值直接 `ReferenceError`，函数一进就崩。`node --check` 只查语法查不出，必须肉眼查"全局变量与函数内 const/let 同名"。教训：把局部变量提升为全局时，**删干净旧局部声明**。
31. **MultiMarker 数组清理坑（2026-09-09 深夜，任务地图切天/开关全失效）**：任务地图客户点按 z 序拆成三组 MultiMarker 后，`tmMarkers` 是**数组**——清理必须 `(tmMarkers||[]).forEach(m=>m&&m.setMap(null))`；旧的单对象 `tmMarkers.setMap(null)` 会在**第二次渲染时抛 TypeError**（首次 null 侥幸通过），症状=首次正常、之后一切操作无反应。凡是"分层 marker"改造，所有引用点同步改数组语义。
32. **adminapi ACTIONS 白名单坑（2026-09-09 深夜全域体检发现）**：云函数 31 行 `ACTIONS.includes(action)` 校验在前、dispatch 在后——**新增 action 若只加 dispatch 不补 ACTIONS 数组，会静默返回 BAD_ACTION**（接口"正常返回"但功能永远失效，无报错无日志，最难查）。`getAdminDistMeta/getAdminDistPart` 是校验前的免鉴权特例。今后加云函数接口：ACTIONS 数组 + dispatch 两处必须同步。
33. **admin.html 行尾与编辑工具坑（2026-09-09 深夜总结，长期有效）**：admin.html 是 CRLF 文件，edit_file 替换会**不定量引入 LF 行**；文件里有 **13 个历史 LF 行必须原样保留**（否则 git diff 出现假差异）。每次改完跑行尾检查脚本：列出 LF 行 → 仅把这 13 行之外的新 LF 行加 `\r`（13 行特征：input,textarea{ / .badge{ / 注册审核角标注释 / .nav-badge{ / .reg-badge{ / .tk-empty{ / .tk-card .pill{ / .tk-card:hover .pill{ / .ts-tab{position / .ts-tab::after{ / .ts-tab:hover::after{ / let copyBatchBusy / 一个空行）。
34. **壳程序拦 F5（2026-09-09 深夜反复踩）**：壳程序 `AreBrowserAcceleratorKeysEnabled=false` **拦掉 F5/Ctrl+R**——后台前端改完"Ctrl+F5 刷新"是无效的，必须**重启壳程序**才会加载新代码；老板报"没动/没反应"先确认他重启壳了没。`nt-map.js` 改动还要 bump `?v=` 版本号（当前 v=0924）。

### 3.11 本段会话改动清单（2026-09-08 晚间，导入/比对/壳程序大修，老板逐项拍板）

> 背景：老板导入 199 家新批次连续踩超时（-601008）与弹窗死循环；比对认领也超时。老板拍板新架构：**比对计算本地化、数据云端化**（浏览器拉库→本地比对→只把最终决定上传）。随后文员 Win10 壳程序最大化盖任务栏、右键菜单、刷新回登录页一连串问题，壳程序大修。

#### A. 导入修复（adminapi + admin.html，全部待重传）
| # | 问题 | 根因与修复 |
|---|---|---|
| 1 | 导入新批次 -601008 超时 | 三处叠加：①写库串行（100 条 × 2 次 add = 200 次往返）；②`fetchAll` 分页 100（注释自相矛盾，上限是 1000）；③匹配循环内对同一条档案反复 `normCustName/normAddr`。修复：`fetchAll PAGE=1000`；`importCustomers` 改**两遍处理**——第一遍纯内存做分级决定（新建行用占位 id `@new{i}` 入池，**批内去重语义不变**），第二遍 `runPool(…, 15)` 并行写库，占位解析为真实 _id 后同一目标去重再入批；匹配池预规范化（`normPoolEntry`：nName/nAddr 一次性算好，`matchExistingCust` 直接读） |
| 2 | 疑似重复弹窗死循环（1/1 反复弹） | 三个 bug：①`decideConflict` 存值没带 index，`Object.values` 后云端 `decMap` 永远空 → 永远 unresolved；②预检 index 是全局行序、云端执行按片内行序（0~99）匹配；③执行中弹窗 `importConflictDecisions = {}` 清空预检决定。修复：决定值带 index；`doImportExecute` 传云端前**全局行序减片起点 i 转片内**；执行中弹窗**不清空**决定（累积）+ 过滤已决定冲突（全滤掉则提示「云端旧代码未重传」防死循环）；云端 needConfirm **只返回 unresolved** |
| 3 | 删除批次卡 -601008 | 客户 batchIds 移除是逐条 doc get+update 串行（200 次往返）→ 改**一次拉全 + 内存过滤 + 15 并发回写**；`addCustomersToBatch` 顺手并行化 |

#### B. 比对认领本地化（老板拍板「计算本地化、数据云端化」，任何电脑登录后台都能用）
- **新接口**：`listMallLibrary`（拉全量商城库，字段裁剪到比对+认领所需）；`applyMallMatch`（认领写档案策略与 runMallMatch 完全一致：未认领全量写/同 mallKey 只刷动态字段/换人全量换 + 待确认写 mall_claims 带 batchId + import_batches 批次记录）
- **前端**（admin.html）：本地比对算法 `lmatchMall`（与云端 `matchMall` 口径逐行一致，本地实测 15 万对 245ms）；商城库缓存 + 「🔄 刷新商城库」按钮；比对结果**预览总览表**——自动认领（≥75 分）列表 / 待确认（45~74 分）逐条「✓ 认领」「✕ 忽略」，未点的留待确认清单 / 忽略计数；底部「✅ 应用认领」只把决定传云端
- 弹窗加宽 880px、结果区 max-height 60vh 滚动；旧 `runMallMatch` 云端保留作备份（importMallCustomers 兼容路径仍用）
- **交互口径**：待确认点「认领」=直接写档案（不进 mall_claims）；点「忽略」=跳过；不点=进待确认清单（mall_claims，batchId 归属本批）

#### C. 壳程序大修（packager/shell/Program.cs，已 csc 编译 0 警告 + 复制到位 + 文员包重打）
| # | 问题 | 修复 |
|---|---|---|
| 4 | 文员 Win10 2880×1800 **最大化盖任务栏** | 根因：程序没声明 DPI 感知 → WebView2 初始化**中途**把进程提升为 PMv2 → 窗口逻辑坐标与 `Screen.WorkingArea` 物理坐标错位。修复：**Main 第一行 `SetProcessDpiAwarenessContext(PMv2=-4)`**，全程一套物理像素坐标系，最大化=WorkingArea 必然不盖任务栏 |
| 5 | 老板 2560×1440@110% 窗口跑右下角 | `CenterScreen` 在无边框大窗上会偏角落 → 改 `StartPosition=Manual` + 手动居中；**初始窗口=工作区 96%×92%**（任何分辨率/缩放自适应） |
| 6 | 高缩放屏标题栏太矮 | 标题栏高度/图标按钮/悬浮关闭键/胶囊全部按 `uiScale=DpiX/96` 放大（44→(int)(44*uiScale) 等），100% 屏行为不变 |
| 7 | 页面右键出现「另存为/刷新」菜单 | WebView2 `Settings.AreDefaultContextMenusEnabled=false` |
| 8 | F5/右键刷新回到登录页 | 双保险：①`Settings.AreBrowserAcceleratorKeysEnabled=false`（F5/Ctrl+R 无效，Ctrl+C/V 不受影响；刷新数据走标题栏「🔄」图标=页内刷新不掉登录）；②admin.html 登录会话恢复——登录成功写 `sessionStorage jh_session`，页面重载静默自动登录（关程序重开仍需手动登录，不违背「记住我」口径），登录失败自动清 |

#### D. 小改与交付
- 任务管理「当前任务」行高：`#taskTbody td` padding 12→17px（只影响当前任务表，其他表格不动）
- **壳 exe 已复制**：`admin/JuHuoVisitAdmin.exe` 与 `D:\JuHuoVisit\admin\JuHuoVisitAdmin.exe`（最终版 52,224 字节）；**文员包已重打**：`packager/output/聚火拜访-后台程序包.zip`（12.98MB，包内 exe+admin.html 已验证与源一致）；环境包 37.08MB 无变化
- 编译命令（Git Bash 用 `-` 前缀防 MSYS 转义，icon 在 ../inputs/）：`/mnt/c/Windows/Microsoft.NET/Framework64/v4.0.30319/csc.exe -target:winexe -platform:x64 -win32icon:../inputs/icon.ico -r:lib/Microsoft.Web.WebView2.Core.dll -r:lib/Microsoft.Web.WebView2.WinForms.dll -out:JuHuoVisitAdmin.exe Program.cs`；**exe 被占用时先关壳再复制**（Permission denied 即壳在运行）

#### E. 体检
- 全部 node --check 通过（adminapi + admin.html 内嵌 JS 120KB python 提取法）；admin.html CRLF 已还原；临时脚本用完即删
- 离线模拟验证：importCustomers 占位去重语义（批内同电话两行→只建 1 档案+1 并入）、分级 A/B/null、池 4000×100 行匹配 12ms；matchMall 新旧一致性抽检 1000 对全一致、15 万对 245ms（旧 466ms）
- csc 编译 0 错误 0 警告

#### F. 下一步（老板侧）
1. **重传 adminapi**（本次全部云函数改动）
2. 后台 Ctrl+F5
3. 老板本机重启壳程序验证：窗口居中 / 右键无菜单 / F5 无效 / 点全屏后任务栏可见
4. 文员包发给文员覆盖安装，验证 2880×1800 屏最大化不盖任务栏
5. 上批遗留：`visits`/`tasks`/`coordfix`/`init` 重传 + 小程序体验版上传（现场证据/历史任务/批次管理）；上线前清单（改名/头像/密码）；服务号两个新模板

## 5. 口径字典（改名记录，别再写错）

- **任务编号（2026-09-04 老板定，格式定稿：区域码-混码）**：如 **`JH05-42379761`**。分段含义：**JH05**=城市区域编码（JH=金华，05=永康；区域码存 `settings.taskRegionCode`，设置页可改，以后统一整理全部区域编号表再逐个更新）；**混码 8 位纯数字**=「年尾数月日5位 ×1000 + 当日序号3位」（如 60904-001 → 60904001）× `2654435761` 取后 8 位——**不可逆**，外人看不出日期与当日任务量，只有后台查库能对上；生成时查重顺延防撞号。历史任务无编号显示「—」不补号；改派/延期/编辑不换号；后台列表/详情标题/创建成功提示/服务号通知（thing2 前缀 `[编号]`）均展示；小程序数据层已返回 taskNo（UI 展示待定）。

- **截止日期口径（2026-09-03 老板定）**：开始日期=任务执行第 1 天（当天凌晨 0 点起开始）；截止日期=任务结束的那一天，**从截止日当天凌晨 0 点起任务即进入"截止"**——此时全部拜访完=「已完成」；没拜访完=「已过期/未完成」（统计为没完成）；后台延期后=「已延期」（按新日期重新执行状态）。任务状态机：草稿→进行中→（到期）已完成/已过期；延期=延长截止日期（留痕）。
- **任务结束自动审核通过（2026-09-04 老板定，key=autoApproveFinish）**：勾选=业务员全部拜访完提交后自动通过直接结束；不勾选=提交后必须人工审核（同意/拒绝）；**提前交（还有客户未拜访）无论勾不勾都人工审核**。旧 key needFinishReview 已废弃。
- **拜访定位校验（2026-09-04 老板定）**：阈值 0~500 米；**填 0 或不勾选=关闭校验**；勾选+填 1~500 整数=按该值校验；**默认 100 米**；业务员定位失败=直接拦截提交（提示检查网络与定位），不再降级"待复核"（旧 pending_review 数据只读兼容）。
- **任务状态展示六态（后台状态卡）**：草稿 / 已发放（已派发未到开始日，附开始日期）/ 进行中 / 审核中（卡内同意拒绝+剩 N 家）/ 已过期（待延期处理）/ 已完成（附"X月X日任务已结束"）。
- 「商城客户」→ **「回访客户」**；列表类型胶囊显示 **「回访」**（新客显示"新客户"）
- 「激活回访」→ **「回访激活」**（全项目已改含订阅推送文案）
- 「添加商城时间」→ **「注册商城时间」**
- 「业务负责人」→ **「签约业务员」**
- 「全部客户」页 → **「回访客户列表」**（回访工作台）/ **「新客户列表」**（新客工作台）
- 导入按钮：回访=「📥 导入回访客户」；新客=「📥 导入新客户」；商城库=「🛒 导入所有客户列表」（仅回访工作台顶部显示，带"上次导入时间"）；「🔎 比对认领」仅回访工作台客户页显示
- **新客户与回访客户严格各自独立开发**（列、弹窗、按钮、文案都不共用一套模板）——老板特别强调过的原则
- 双工作台无「全部总览」；客户/任务按工作台严格隔离
- 电话不做去重（连锁店同老板多店铺正常）
- **拜访目的三选（2026-09-07 老板定）**：回访工作台 = 💤 激活增单 / 🔁 走访维护 / 🎉 活动推广（值 activate/maintain/promote）；新客工作台 = 🌱 新客开发（develop）。历史 activate 数据自动显示新名「激活增单」。「沉睡激活」为曾用名，已废
- **坐标状态文案（2026-09-07 老板定）**：ok=正常、pending=补标、pending_confirm=待定（原「待确定/待补标」废弃）；同意报错不写回坐标 → pending_confirm
- **客户批次口径（2026-09-07 老板定稿，方案见 §7.12）**：客户档案全局唯一；**合并判定=多字段分级匹配**（S 编号 / A 电话+店名规范化、电话+地址 / B 电话同店名异、店名同区域同——B 一律弹窗人工定 / C 新建）；店名规范化去公司后缀；弹窗逐个处理（合并/新建/忽略）；设置页 mergeMode 只影响 A 级（**mergeMode 待开发**）；批次=名单多对多（batch_members 纯名单不持状态）；**全局状态**任务中/无任务实时推导（任务结束立即释放）；删批次=删名单不删客户（回「未分批」）；未分批卡片不可删；初始归档「2026年9月1日第一批导入」；**隐藏按钮（功能保留）**：手工建批/批次卡删除/详情页删除/详情表删除列；批次卡丰富版+弹窗统一规范见 §3.7-G

## 6. 数据实况

- 回访客户 49 家（customerType=mall）；商城客户 1219 条（mall_customers）；商城比对认领已生效（任务内 18 家带出 mallJoinedAt/mallSalesman/mallLevel/lastBrowseAt）
- 待确认认领清单在 `mall_claims` 集合（pending/confirmed/rejected；重新比对只重建 pending）
- 任务 3 个：第一批（18 家范宇琨 published）、第二批（2 家范宇琨）、第三批「沉睡客户激活-第三批」JH05-42379761（15 家青恩艳，9/5 开始）；已拜访 7 家、无拜访中
- 业务员 5 人：丰炳全/程绍君/宋子军/范宇琨/青恩艳(15067951632)；**服务号 mpOpenid 5 人已全绑**；青恩艳**尚未登录小程序绑定微信**（首次打开选姓名即可）
- 本地语音缓存：`admin/voice/` md5(名字+类型).mp3（青恩艳 review 版已合成；coordfix 版首次触发时自动合成）
- 管理员 qingyan/123456（**上线前必须改**）
- 坐标报错：coord_fix_requests 有 pending 待老板后台审核（明早验证用）；历史 cancelled 已全清

## 7. 待办 backlog（按建议顺序）

1. ~~待确认清单页~~ ✅ 已完成
2. ~~任务编辑/延期/改派/撤回/删除/发送（全套任务操作）~~ ✅ 已完成（2026-09-04 复核定稿：复制/结束已按老板要求撤除）
3. **客户批次管理（2026-09-07 方案定稿见 §7.12，待开工——当前最高优先）**
4. 客户导出（36 列档案）+ 双口径报表（依赖三期销售导入）
5. 上线前：小程序改名"聚火拜访"、上传头像、修改 qingyan 初始密码 123456
6. 二期地图·多媒体（照片/录音）
7. 服务号长期通知（§7.6）~~明天做~~ → ✅ 2026-09-04 代码已完成，待重传+白名单清空+模板字段核对+OpenID 绑定后联调
8. **操作档案 audit_logs（最后阶段实现，方案已定稿见 §7.8）**：重要操作建档可查 + 90 天/10000 条保留 + 本地备份导出
9. **实时距离刷新 + 报错防漂移（方案定稿见 §7.9，待开工）**：四档定时距离刷新（默认15s）+ 报错五点收敛校准（10/20/30m）+ 圆环✕取消交互
10. **AI 日报与员工评分（远期设想定稿见 §7.10）**：混元/智谱双接口可切换 + 员工每人一份 AI 日报后台页 + 规则算分&AI评语（轨迹二期后落地）
11. ~~新建任务三步向导·地图选店~~ ✅ 已完成（§7.11，2026-09-06 老板验收通过）

## 7.5 二期多媒体注意事项（2026-09-03 调研，免费套餐限制结论）
- **照片功能不受云存储权限限制影响**：统一用 `cloud.getTempFileURL({ fileList })` 把 fileID 换成临时 https 链接再展示/下载，无需升级套餐。
- **录音功能本身免费**：`wx.getRecorderManager()` 走云存储客户端 `wx.cloud.uploadFile` 直传（不要经云函数中转）。
- **免费额度**：云存储 5GB、CDN 5GB/月、数据库 2GB——初期够用。
- **照片压缩标准（老板定）**：单张 ≤300KB、最长边 ≤1280px、JPEG 质量 70；上传前逐张检查达标才 uploadFile。
- **录音体积控制**：16kHz 单声道 mp3，单段限 5 分钟 ≈2.5MB。
- LOGO 已回本地 28KB（images/logo.png）。

## 7.6 服务号长期通知方案（2026-09-03 老板拍板；2026-09-04 已开发完成，待联调）

- **目标**：业务员关注老板认证服务号一次 → 永久免授权收"新任务提醒"。
- **链路**：后台派任务 → adminapi 用服务号 access_token 调 `message/template/send` → 发到业务员 mpOpenid。
- **已实现（2026-09-04 会话）**：
  - adminapi：settings `mpConfig`（appid/appsecret/templateId/enabled，getSettings/setSetting 对 AppSecret 脱敏、留空=保留旧值）；`setMpOpenid`（人员服务号 OpenID 绑定/解绑）；`testMpSend`（指定 OpenID 发测试消息，返回微信原始 errcode/errmsg）；`getMpAccessToken` 缓存于 settings `mpAccessToken`（提前 5 分钟过期重取）；`sendNotify` 改为**服务号模板消息优先 → 失败/未绑定回退小程序一次性订阅**，返回 `channel: 'mp' | 'subscribe'`；createTask/sendTask/editTask/extendTask/reassignTask 全部自动生效。
  - 后台：系统设置页新增「服务号长期通知」区块（启用/AppID/AppSecret/模板ID/测试发送+测试 OpenID）；人员管理业务员表新增「服务号绑定」列（＋绑定/更换/解绑弹层，OpenID 打码显示）；创建/发送成功提示区分「已通过服务号通知业务员/已推送订阅消息」。
  - 手机端：tasks `subStatus` 返回 `mpBound`；home 订阅横幅在服务号已绑定+启用时隐藏；mine 页提醒开关旁显示「服务号已开通」。
- **资料已齐**：AppID `wx9fdecf661d499607`；AppSecret 已生成（`聚火拜访服务号SecretKey.csv`）；**服务号模板 ID（2026-09-05 五词重配版，正式）：`kdgr7e7C-E8a3DZARR_zzmZMeFyFhAySs0XUk8VdDf4`**——词序=订单编号/服务人员/服务用户/服务时间/地点；实测字段 character_string1/thing2/thing6/time5/thing7；服务时间=任务开始日期 00:00（微信时间类型强制时分，无法去掉）；地点行方案 B 文案「共X家客户，X天时长，X月X日开始」。
- ⚠️ ⚠️ **旧模板 ID 作废**：`4XdmIcirUFlLJod5FQQtc-zeGfVIf-CzWMTDIpH1dYM`（40037 无效）与 09-04 的 4 词版 `kdgr7e7C-E8a3DZARR_zzrh94Yc6IJ2OR3sWV0-k2q8`（已停用）均**勿再使用**。
- **模板字段结构（2026-09-05 实测探测确认，buildMpData 已按此实现）**：`character_string1` 订单编号（任务编号）、`thing2` 服务人员（业务员姓名）、`thing6` 服务用户（任务名称）、`time5` 服务时间（开始日期）、`thing7` 地点（工作量文案）——**勿增删字段，47003 即字段不符**；thing ≤20 字。
- **服务号消息端到端已通**：测试消息已发范宇琨 `errcode:0` ✅（老板手机确认）。
- **IP 白名单已设（2026-09-04 实测通过）**：老板本机 IP `183.208.27.136` 已加入白名单，本机调 token 接口实测 `OK expires_in=7200`。⚠️ 该 IP 为动态宽带 IP，**变化后需重新在公众平台更新**（本机/云端报 40164 时第一排查项）。
- **双通道架构（因白名单只认本机 IP）**：
  - 主通道：后台登录成功 → admin.html 调 server.js `/mpTokenRefresh` → server.js 用 config.json `mpAppId/mpAppSecret` 本机取 token → adminapi `mpTokenPush` 写入云端 `settings.mpAccessToken`；随后 server.js 每 108 分钟自动刷新（server 重启后老板重新登录一次即恢复，凭据存内存）；
  - 兜底：adminapi `getMpAccessToken` 缓存有效即用；缓存失效时才尝试云函数直连（白名单未清空时必然 40164，报错信息带「请重新登录后台触发同步」提示）；
  - 设置页显示 token 同步状态（✅ 已同步 / ⚠️ 待同步）。
- **⚠️ 联调前必须处理（按顺序）**：
  1. **重传 `adminapi` 云函数**（2026-09-04 已传过一版，但 buildMpData 字段结构是本轮实测后改的，**需再传一次**；`tasks` 如还没传也一起）；
  2. 后台**重启 server.js + Ctrl+F5**（新增 /mpTokenRefresh 与 108 分钟定时器；config.json 已含 mpAppId/mpAppSecret）——✅ 已在老板机器生效（设置页显示「服务号 token：✅ 已同步」）；
  3. 重传后用 qingyan 登录后台 → 日志出现「服务号 access_token 已同步云端」即通过——✅ 已验证；
  4. ~~模板字段名核对~~ ✅ 已核对落地（kdgr7e7C…，thing2/thing7/time4/time5，本机脚本实测发送成功）；
  5. **业务员 mpOpenid 绑定**：让 4 名业务员（+青恩艳）微信关注服务号 → 老板在用户管理里**按昵称认人 →「修改备注」写姓名**（remark 是 API 唯一可读标识，昵称 API 不返回）→ 脚本按 remark 批量识别 openid → adminapi `setMpOpenid` 绑定；
  6. 小程序新预览码（home/mine 改动），然后「测试发送」端到端验证——✅ 本机脚本已先行验证通过。
- **口径**：服务号发送失败不阻断任务创建（尽力而为+回退）；未关注服务号的业务员收不到模板消息（43004），自动回退订阅或静默。

## 7.7 通知体系现状（2026-09-04 老板满意定稿：A+B）

- **全局 15 秒事件检测**（后台任何页面）：拉 listTasks → 指纹 diff → 新审核/新过期才触发：**响铃（admin/notify.wav，Winner game sound）→ 1.6 秒后云希男声播报「X提交了任务审核，请及时处理。」→ 铃铛角标+浮窗**；页面不可见时补 Windows 系统通知（点击直达任务详情）。
- **手机端审核观察员**：有审核中任务才 15 秒轮询 reviewStatus；结果出来自动刷新+toast；无审核中任务零轮询。
- **刻意移除**：Web Push（标签页关闭也收）——老板决定"关闭标签页收不到无所谓"，代码已全清（勿再重做，除非老板改主意）。
- 铃声 3.2MB 长音频：语音延迟 1.6 秒错开；若想更干净可裁剪铃声（老板未定）。

## 7.8 操作档案 audit_logs 方案（2026-09-05 老板拍板定稿，**最后阶段实现，勿提前动手**）

- **目标**：重要操作（谁/何时/对什么/做了什么/结果）建档可查；业务层保持干净（如取消拜访业务不留痕），审计层留痕。
- **三个已定决策**：①取消拜访**记轻量档案**（actor=业务员、type=visit、action=cancel，不含业务残留）；②管理员操作**记 IP**；③保留 **90 天 / 10000 条封顶**，且**必须有备份到本地方案**。
- **分轻重**：
  - 🟥 必记：任务创建/发送/编辑/延期/改派/撤回/删除；审核同意/拒绝；坐标报错提交/审核同意/拒绝；商城认领确认/拒绝；客户导入批次；人员新增/停用/删除/服务号绑定；设置项修改（记 before/after）；管理员登录（含 IP）；数据清理（cancelOngoing/purgeCancelled）。
  - 🟨 轻量记：业务员取消拜访；拜访提交失败（超限/定位拦截）；重复操作被拦截。
  - ⬜ 不记：查询/刷新/详情查看/心跳/订阅推送成功等噪音。
- **数据结构**：`audit_logs` 集合 `{ at, actor, role(admin|salesman|system), type(task|visit|coordfix|claim|user|setting|import|auth), action, targetId, targetName, detail{before/after 摘要}, ip }`。
- **埋点**：只在云函数动作成功路径末尾追加 add（adminapi 写操作、visits submit/cancel、coordfix 提交、tasks finish；尽力而为，失败不影响主流程）；前端零改动。
- **查阅**：后台「📁 操作档案」页——筛选（时间/人/类型/关键词）+ 一句话摘要列表 + detail 展开 + 按任务/客户时间线视图。
- **本地备份方案**：①档案页「导出 CSV/JSON」按钮一键下载到本地（server.js 生成文件下载）；②并入每日备份 timer（导出 audit_logs JSON 进 backups/ 归档 zip）。
- **保留策略**：timer 定期清理超 90 天记录（清理前已进每日备份，本地永远可查）。
- 实现时集合自愈（init COLLECTIONS + 函数内 try createCollection）。

## 7.9 实时距离刷新 + 报错防漂移方案（2026-09-05 老板拍板定稿，**待开工，勿提前动手**）

> 背景：持续定位接口（wx.startLocationUpdate/onLocationChange）**无权限不可用**（老板已查）。采用普通 wx.getLocation 方案。

### A. 实时距离刷新（任务详情页）
- **技术**：普通 `wx.getLocation`（`isHighAccuracy: true` 高精度优先）；**定时刷新定位失败完全静默**（保留旧数字，下一轮自动重试，不弹任何提示）。
- **档位**：10/15/20/30 秒四档，**默认 15 秒**；后台「拜访定位校验」区块**下边**新增下拉 `locRefreshInterval`（与阈值/收敛半径合并一个保存按钮）。
- **触发时机**：①进入任务详情立即刷一次；②点天页签立即刷一次；③前台停留按档位定时刷。
- **生命周期**：进页面启动定时器、切后台/离页停止、回页面重启+立即刷一次（人已移动）。
- **更新策略**：重算全部客户距离 → 指纹对比 → 只更新变化的卡片数字；距离格式不变（Xm / X.Xkm）、**不加 📍 图标**。
- **数据链路**：settings → tasks detail 返回 locRefresh → task 页按档位 setInterval。
- 性能：O(18) 纯算术 + 指纹更新，零风险；频率低于微信定位节流线。

### B. 报错防漂移校准（客户详情「📍 报错」）
- **流程**：点报错 → 按钮变**圆环转圈 + 中心 ✕**（catchtap 取消：停取点、按钮还原、可立即重试）→ 校准约 9 秒 → 稳定则弹报错窗口；不稳定则中央黑色半透明弹窗 2.5s 消失（页面内实现，非系统 toast）→ 允许立即重试。
- **取点规则**：连续取 5 点（间隔 1.8s），**丢前 2 点**（冷启动），`accuracy` ≤50 米才计入（accuracy=0 未知不计）；后 3 个有效点**两两距离 ≤收敛半径**判定稳定，取质心为报错坐标。
- **提前收敛**（已批准）：满 3 个有效点且两两 ≤5 米 → 提前结束（约 6 秒）。
- **收敛半径**：三档 **10/20/30 米**后台可设（默认 20），`locConvergeRadius`。
- **失败弹窗文案两种**：完全取不到位置 →「定位失败，请检查定位权限或网络」；点飘不收敛 →「定位不稳定，请到开阔处重试」。
- **报错弹窗内**：新坐标 + 距离两行（与原坐标相距 / 约 X 米）+ **定位质量行**（✅稳定·精度X米 / ⚠️一般·精度Y米）+ 备注两行 + 照片三宫格占位 + 确认提交。
- **转圈动画风险预案**：CSS 转圈若模拟器不动（有 09-05 动画失效前科）→ 降级 `wx.showLoading`（系统转圈必定转）+ 空白点击取消；校准逻辑不依赖动画。

## 7.10 AI 日报与员工评分方案（2026-09-05 老板远期设想，**远期实现，勿提前动手**）

### A. AI 接口选型（老板拍板方向）
- **主用**：腾讯混元 hunyuan-lite/turbo（当前全量免费，截止时间官方未公布）；**备用**：智谱 glm-4-flash（长期免费）；DeepSeek 便宜可作第三备选。
- **架构**：模型配置做进后台设置页（API 地址 + Key + 模型名可改），哪个免费用哪个，可无缝切换；Key 由老板注册提供。
- **智谱 Key（老板 2026-09-06 提供，⚠️ 凭据保密）**：`e0cada7a67db4e8a99baae77d29bc018.8kb2u28SXZDpMmyp`（id.secret 格式）。实现时作 settings 默认值（同 mpKey 先例），**不硬编码进前端**；调用前查智谱官网当前免费/价格政策（免费额度可能变化）。
- 微信云函数可直连这些公网 API；数据喂给 AI 前**脱敏**（去掉客户电话，保留店名）。

### B. 员工日报（远期一期：先拜访数据，后轨迹）
- **粒度**：业务员每人一份（老板拍板）；**查看**：后台新增「👥 员工日报」页。
- **数据来源**：第一期=当天拜访记录（时间/客户/结果/文字/样品/进度）；轨迹数据随 §11.8 位置上报二期加入。
- **触发**：后台手动「📄 一键生成全部」+ 单人「🔄 重新生成」（自动定时稳定后再议）；生成状态徽标：待生成/生成中/已生成/失败/无数据。
- **日报结构（AI 输出模板）**：①今日概况 ②重点客户动态 ③问题与风险 ④明日建议。
- **页面构思（待老板拍板）**：员工卡片横排 vs 表格列表；日报长度 200~300/500 字；是否显示「今日无拜访记录」摸鱼提醒。三个待定点老板说晚点再定。

### C. 员工评分（远期二期：AI 辅助评分，老板拍板方向）
- **核心原则**：**规则算分（可解释、稳定、可调权重）+ AI 写评语（60~80 字+改进建议）**，不让 AI 直接凭感觉打总分。
- **评分维度（权重后台可调，存 settings.scoreWeights）**：
  - 拜访量：完成当天计划 X 家满分，少 1 家扣分
  - 拜访结果：极有意向+8/有意向+5/已下单+10/无需求+2/联系不上+0/闭店-1（示例，可调）
  - 文字质量：字数+关键词判详实度，**封顶 5 分**（防误伤文化水平差异）
  - 样品执行：带样品加分（或改必带扣分项，老板定）
  - 轨迹勤劳度（二期）：首访时间、拜访间移动/停留、长时间原地不动检测——**权重 ≤15 分**（防"表演性拜访"）
  - 订单促成（三期销售数据接入后）：成交额/单量高分
  - 纪律：取消拜访 -2/次等
- **技术路径**：先规则引擎（adminapi `scoreSalesman` → daily_scores 集合，后台权重设置页可视化），跑 1~2 周校准；再接入 AI 生成评语；分数趋势曲线（日/周/月）作管理视图。
- **管理纪律（老板须知）**：分数只对老板可见，不与工资直接挂钩，先参考后奖惩；轨迹分低权重、结果分高权重（结果才是钱）。

### 远期蓝图
```
轨迹采集(§11.8) → 实时距离刷新(§7.9) → 员工日报(AI) → 员工评分(规则+AI评语) → 月度趋势报表
```

## 7.11 新建任务三步向导·地图选店方案（2026-09-05 老板拍板定稿；**2026-09-06 已开发完成**，实现变更见本节末，改动清单见 §3.5）

> 目标：把新建任务第 2 步升级为地图选店（腾讯地图），第 3 步增加智能排序。回访客户都有坐标，本期只做回访；新客户无坐标问题以后再说。

### 第 1 步：基础信息（保持现状）
任务名/业务员/开始日期/天数/目的，不变。

### 第 2 步：地图选店（全新）
- **地图**：腾讯地图 JS API GL（Web 后台 admin.html 内嵌），KEY `SQWBZ-K326U-MU3VH-GWUHA-HGNES-S7F2D`（已绑小程序 wxcfdfcfffcf321fc2；**需在 lbs.qq.com 控制台把 localhost 加入域名白名单**，否则 Unauthorized）。KEY 存 settings.mpKey（默认填好，可换）。
- **标记颜色规则（老板定）**：未选中=灰；选中=灰底+按天色彩描边；**当前操作的天=全色**；每天颜色与天页签「第X天」文字色一致，7 色循环。
- **选店方式**：逐个点选/取消 + 鼠标**矩形框选**（按住拖框圈选一批）。
- **店名显示**：标记旁显示店名，右下角「⚙ 设置」可开/关店名显示。
- **天页签条**：地图上方「第1天 [8/15] · 第2天 [0/15] …」（N=基础信息天数）；**每天上限 15 家**（腾讯 driving 免费 16 点限制：起点+终点+14 途经；付费才 30 个，不用付费）；选满 15 家**停留在当天**（不自动跳），管理员可取消某家再选；**点天卡片=锁定该天并切换**到该天操作；已选店**跨天不可重复**（其他天已选店灰显）。
- **商户列表**：地图下方，与地图**双向联动**（地图勾选→列表自动打勾；列表勾选→地图点亮）；选中店置顶+按选择顺序编号，未选按名称排序；地图占上 2/3、列表下 1/3（**先按此做出来给老板看再调**）。

### 第 3 步：排期预览（升级）
- 各天卡片+店名胶囊（与现在类似，顺序=选择顺序）。
- 底部四个按钮：「上一步」「⭐ 智能排序」「创建草稿」「创建并发送给业务员」；**默认焦点在「智能排序」**。
- **智能排序算法（老板定）**：
  - 起点**固定仓库 28.970802, 120.154526**（纬度在前！腾讯接口 lat,lng 顺序）；
  - 第 1 层：云端贪心排候选顺序（从仓库出发，每次选直线距离最近的下一家，排 3 个不同起点的候选）；
  - 第 2 层：对每个候选调腾讯 `apis.map.qq.com/ws/direction/v1/driving`（from=仓库、to=最后一家、waypoints=其余店）取**真实驾车距离**，选距离最短的候选为最终顺序（指标=距离最短）；
  - 结果写回 dayPlan 顺序；每天卡片显示「驾车 X 公里 · 预计 X 分钟」；全部排完提示「**当前智能排序完毕**」；
  - 预案：起点 20 公里内无道路算路失败 → 退回直线距离贪心顺序；个人 Key 免费配额 1 万次/天，排序调用 3~6 次，绰绰有余。
- **创建草稿**：createTask 支持 status='draft'（不通知不进手机端；发送用现有 sendTask）。
- **创建并发送给业务员**：published + 服务号通知，**使用排序后的顺序**（手机端按路线顺序拜访）。

- **地图选店样式（2026-09-05 实测经验）**：GL 的 `mapStyleId` 只认腾讯官方内置名，**自定义样式（永康餐饮01/编辑器 36828）与 style1~3 在我们 Key 上均报「并未找到」**；**最终方案=`baseMap: { type: 'vector', features: ['base', 'building3d'] }` 要素裁剪**（裁掉 point/label 等杂乱要素，任何 Key 有效，老板验收「很干净」）。别再折腾样式 ID。
- **腾讯 driving 接口**：WebService Key 配了 localhost 白名单后**请求必须带 `Referer: https://localhost/` 头**，否则 110 来源域名未授权；坐标格式 lat,lng（纬度在前）。

### 2026-09-06 实现变更与定稿（与上方原方案的差异）

- **五层圆点**取代原三层标记：①当天已选（天彩+白序号，点击取消）②待回访（浅灰+深灰描边，点击选入）③其他天已选（天彩减淡，点击提示跨天）④任务中⑤已拜访（深灰蓝不可点）；切天时①层整体换天、旧天店落③层。
- **店名开关**改为双圆点（左=可选默认亮、右=不可选），不再叫「⚙ 设置」。
- **智能规划**从第 3 步挪到**第 2 步工具栏**（⭐ 智能规划胶囊，规划当前天），第 3 步保留结果预览；规划结果含真实道路轨迹 polyline（云端 smartSortDay 返回差分解压数组，`decodePolyline` 解压；无 polyline 直线兜底）。
- **天数规则**：第 2 步「＋」胶囊可加天数（≤7）；**天数与初始设置不同必须修正**（否则存草稿/发送都被拦截）；「🔧 修正任务天数」=去空天紧凑重排+全量数据迁移。
- **绿色导航路线（方案 C，2026-09-06 老板拍板）**：原 GL Polyline 双层方案废弃（dashArray 不生效）→ **SVG 自绘层**：绿 #16A34A 4px 实线 + 白 3px 蚂蚁虚线（`stroke-dasharray="16 16"`、`stroke-opacity="0.5"` 50% 透明、dashoffset 每 150ms +2px 流动）；SVG z-index 7 垫在店名/⭐/框选之下；mask 挖孔保证圆点压在线上；bounds_changed/resize 重投影。实现细节与备份见 §3.5。

## 8. 常用命令

```bash
cd /d/WFR/visit-miniapp
node --check miniprogram/cloudfunctions/adminapi/index.js        # 云函数语法
node --check admin/nt-map.js                                      # 地图模块（CRLF，先转 LF 或直接 check）
node --check admin/server.js                                      # 后台服务
node -e "const fs=require('fs');const h=fs.readFileSync('admin/admin.html','utf8');const m=h.match(/<script>([\s\S]*?)<\/script>/);fs.writeFileSync(process.env.TEMP+'/a.js',m[1]);console.log('ok')" && node --check "$TEMP/a.js"   # admin.html 内嵌 JS 语法
python -m json.tool app.json > /dev/null                          # JSON 校验
cd admin && PORT=8581 node server.js                              # 后台启动（固定 8581；双击 bat 即可）
edge-tts --voice zh-CN-YunxiNeural --text "测试" --write-media t.mp3   # 语音合成手动测试
curl -X POST http://localhost:8581/tts -H "Content-Type: application/json" -d '{"name":"范宇琨","type":"review"}'   # 语音缓存（type=coordfix 坐标报错文案）
curl -X POST http://localhost:8581/api -H "Content-Type: application/json" -d '{"action":"ping","username":"qingyan","password":"123456"}'   # 后台 API 自检
# 调接口含中文：node 写 UTF-8 JSON 文件 + curl --data-binary @file（命令行直接传中文必乱码）
```

## 7.13 老板手机端方案（2026-09-09 老板拍板定稿；四页：首页/任务地图/战况地图/我的）

### 目标
老板用自己微信进小程序 → 全局视角：看所有人的任务、点任意任务看地图与客户、虚拟体验业务员全流程操作（**只算不写**）；另设**战况地图**专页实时盯人（位置+今日动态+自动预警）。

### 身份与入口（复用现有链路，零新 users 角色）
- 老板微信已绑 `super_admin`（bindadmin 云函数，开通码 `JuhuoAdmin2026`）→ login 第 0 步识别 isAdmin。
- login 页管理员提示卡加「👑 进入老板模式」按钮 → app.globalData.bossMode=true（storage 持久）→ redirectTo 首页。底部 TabBar 四栏与业务员同构：**首页 / 任务地图 / 战况地图 / 我的**。
- 云函数识别 boss：`me.role ∈ ['super_admin','admin']` 且请求带 `boss:true`（或按 role 直接判定）→ 查询全量、写操作全虚拟。
- 安全保证：云函数层 boss 分支**绝不写业务集合**，不存在误改真实数据路径。

### 四页定稿
1. **首页（bossHome，常规卡片风）**：顶栏「聚火拜访 · 老板」；数字胶囊带=今日完成 x/y · 拜访中 n · 在线 n · 待审核 n（红字=异常）；任务总览列表=每任务一张卡（任务名+**业务员名**+进度条 x/y+截止倒计时+状态胶囊），点卡进该任务的任务地图。
2. **任务地图（复用业务员 map 页 + bossMode 分支）**：某任务当天客户点+路线+全览；客户卡**电话不打码可拨**；智能重排=云端**算不存**（返回新顺序与公里数，前端展示效果，不写 dayPlan）；点客户进拜访页走虚拟全流程。
3. **战况地图（bossWar，新页面）**：见下。
4. **我的（bossMine，改版）**：老板模式标识 +「退出老板模式」（回管理员提示页）+ 版本号（沿用 mine 页布局）。

### 战况地图（优化版，老板 2026-09-09 认可方向）
- **地图主体**：每业务员一点，蓝=拜访中 / 橙=在移动 / 灰=静止>10 分钟 / **红圈=异常**；拜访中者点旁挂气泡（店名+已待时长）；仓库⭐；⊡全览；30 秒自动刷+下拉刷新。
- **今日动态流**（页面灵魂，地图下方时间线）：开始拜访/提交拜访（带结果标签）/结束/交任务/报错/静止预警；颜色=绿完成✓/蓝进行中▶/黄待处理/红异常⚠️；点条目跳对应详情。
- **三个自动预警（首版）**：①静止异常=工作时间内静止>40 分钟（红圈+动态红条+顶栏🔔）；②零完成=15:00 后今日完成 0 家（数字胶囊变红）；③拜访超时=计时超上限未提交（气泡变黄+动态黄条）。
- **点业务员=半屏抽屉卡**：状态+当前店+计时；今日进度条；按钮=📜今日轨迹/📞拨店里电话/🏪该店历史/🗓他的任务；「只看他」开关。
- 数据来源与后台位置监控同源（latest 文档），手机端走新只读接口。

### 虚拟操作清单（老板拍板：全部）
拜访全流程（开始/计时/拍照录音/提交/取消，云端假成功不落库）、定位报错（coordfix 模拟）、交任务（finish 模拟）、地图重排（replanDay 算不存）。前端不动业务员页逻辑，靠云函数 boss 分支天然实现"点了有效果、刷新即复原"。
- 附加：boss 的 reportLocation/reportTrack **一律丢弃**（防污染位置监控数据）；trial 打码/禁拨逻辑不适用于 boss（电话不打码）。

### 实施顺序
①云函数层：tasks/visits/coordfix 加 boss 判定（list/detail 全量、写操作虚拟、定位丢弃）+ adminapi 新增只读 `bossBoard`（今日统计+任务进度+待审数）+ `bossWar`（位置点+今日动态+预警数据）；②login 页入口+全局 bossMode；③首页 bossHome；④战况地图 bossWar；⑤我的页 bossMine+任务地图复用联调；⑥真机验证+提审注意（老板模式仅供绑定的管理员微信，业务员无感）。

## 7.14 老板欢迎仪式（2026-09-10 老板拍板定稿；已开发完成待部署）

### 定稿口径
- 老板微信进小程序（老板模式）→ 全屏礼花仪式：深色遮罩 + 「👑 欢迎老板」金色大字（放大入场+光晕）+ 120 片礼花（彩带长条飘摆 + 纸屑方块）从中央炸开，重力飞散、旋转、缓缓飘落；结束前 0.4 秒整体淡出 → 自动清除显示正常首页。全程 `pointer-events:none` 不挡操作；业务员完全无感。
- **后台可配三件套**（设置页「老板欢迎仪式」区块，改完即时生效，不用重传体验版）：
  - 播放频率：每天第一次（默认）/ 每次进入 / 只显示一次
  - 时长：2 / 3（默认）/ 5 秒
  - 风格：金色系（默认）/ 彩色系
- 配置随 login 下发：老板登录/注册返回携带 `welcome`；管理员手动进老板模式走新接口 `login/welcomeCfg`；前端有等待兜底（在途标识 `welcomePending`，最多等 1 秒）+ 配置归一（非法值回默认，防"三判定全 false 每次进都播"）。
- 频率判定在前端本地 storage（东八区日期）：`wel_once`/`wel_date`。

### 实现要点（2026-09-10）
- 手机端：`home.wxml` 全屏 `<canvas type="2d" id="welcomeCv" wx:if="{{welShow}}">`；`home.js` maybePlayWelcome/_welGo/_startWelAnim（两风格共用一套引擎仅颜色数组不同；33ms 定时器驱动，onHide/onUnload 清理定时器；文字静止后关 shadowBlur 省渲染）。
- 后台：admin.html 设置页三下拉（welMode/welDuration/welStyle）+ saveWelcome；adminapi `setSetting` 白名单补 `welcomeConfig`（mode/duration/style 三字段钳制）。
- 清理链：`clearUser()` 清 welcome/welcomePending（退出身份后下次必拉新配置）。
- 部署：重传 login + adminapi + 上传体验版；老板手机实测看动画观感（粒子数/爆发力度可再调）。

## 7.12 客户批次管理方案（2026-09-07 老板拍板定稿；**代码已按两层状态模型改造完成待部署，部署见 §3.3**）

### 背景与目标
回访客户会分多批次导入；每批应是一张可管理的"批次卡片"（可改名、可写说明、可按日期排列、卡片上有操作按钮），点卡片看批内客户；新建任务时先选批次，针对某批客户建任务。**一个任务 = 一个批次**。

### 数据模型（3 集合）
- `customer_batches`：批次卡片。字段：`name`（批次名，可改）、`subtitle`（副标题/说明，可改）、`createdAt`、`createdBy`、`customerCount`（统计按 batch_members 实时计算，不存冗余 stats）
- `batch_members`：名单关系（多对多）。字段：`batchId`、`customerId`、`status`（**批内状态**：todo 待回访 / in_task 拜访中 / visited 已回访）
- `customers`：客户档案全局唯一，加冗余 `batchIds`（所在批次 id 列表；空=未分批）

### 核心口径（2026-09-07 老板逐条拍板；**晚间重大模型修正：批内状态独立 → 全局状态统一**）
0. **两层状态模型（2026-09-07 晚老板想通定稿，取代"批内状态独立"）**：
   - **全局状态**（customers，实时推导不存字段）：**任务中**（在任一 published/reviewing 任务中）＝所有批次/新任务不可选；**无任务**＝可选。任务完成/撤回/删除/过期**立即释放**（新任务都是次日出发，无当天重派问题）
   - **任务内状态**（任务详情内推导）：待回访/拜访中/已回访（现成逻辑不变）
   - batch_members 退化为**纯名单**（batchId+customerId，status 字段废弃）；批次只是分组工具不持有状态
   - 批次详情表「批内状态」列 → 「状态」列（任务中蓝胶囊/无任务—）+ 新增「最近拜访」列（最后拜访日期，全局可见回访痕迹）
   - 批次卡统计：任务中 N / 无任务 N；客户档案弹窗：所在批次 + 全局状态
1. **客户档案唯一**：合并判定走**多字段分级匹配**（编号 > 电话+店名 > 电话+地址 > 电话 > 店名+区域，逐级），**不以电话为唯一依据**；店名规范化（去「有限公司/有限责任公司/餐饮管理/个体工商户」后缀、全角半角、去空格标点、大小写）。分级结论：
   - **S 级**：编号 key 相同 → 同一客户（绝对）
   - **A 级（高置信）**：电话相同且店名规范化相同；或电话相同且地址相同（换招牌）→ 同一客户
   - **B 级（疑似）**：电话相同但店名地址都不同（连锁店共用电话/换址换招牌）；或电话缺失/不同但店名规范化相同且同区域（防"沙县小吃"同名误合）→ **一律弹窗人工定**
   - **C 级**：都不中 → 新客户
   - **弹窗**：左=新导入行（店名/电话/地址），右=命中旧客户候选（可多个）；按钮：合并进旧档案 / 新建独立客户 / 忽略（本批先不导）；**逐个弹窗**（不做批量应用）；A 级按设置页 mergeMode（auto 静默合并 / confirm 弹窗）执行，B 级无论开关一律弹窗
2. **批内状态独立**：新入某批一律「待回访」；该批任务推进时独立变化（待回访→拜访中→已回访）；同一客户在 A 批已回访、B 批待回访互不影响
3. **删除批次卡片 = 解散名单**：删 `customer_batches` + 对应 `batch_members`，客户档案/拜访历史/任务记录全部保留，客户回到「未分批」
4. **「未分批」卡片**：系统固定，不可删不可改名；有未分批客户才显示；点进去就是普通客户列表
5. **初始归档**：上线时一次性建「2026年9月1日第一批导入」批次，现有全部客户入批，现有状态即该批批内状态；历史任务无 batchId 照常显示（业务员端无感）
6. **批次自动命名**：导入新批自动「YYYY年M月D日第一批导入」；同一天再导=第二批…递增；名称可改
7. **手工建批**：选人界面列出**全部客户**，第二列=状态列（「已入批」胶囊 或「未分批」灰字）；点「已入批」胶囊弹窗显示该客户所在各批次名；勾选客户组成新批次（批内状态一律待回访）
8. **跨批冲突不拦截（2026-09-07 老板定）**：同一客户可同时出现在两个批次的任务里（允许重复拜访，业务上接受）；**批内拦截保留**——本批内状态为「拜访中/已回访」的客户在本批新任务里不可勾选（沿用现有三态禁选逻辑）
9. **批内状态生命周期（2026-09-07 老板定）**：新入批=待回访；本批任务**发布/发送**后批内状态=拜访中；拜访完成=已回访；任务撤回/删除=退回待回访（沿用 purgeOngoing）；草稿不改状态
10. **统计实时计算（2026-09-07 老板定）**：批次卡片三色统计不存冗余字段，列表时按 batch_members 内存分组计数（数据量小无压力）
11. **「未分批」卡片只显示总数**（无三态统计——未分批客户不进任务，状态恒为待回访）；**业务员小程序端不显示批次名**（批次是老板侧管理概念）
12. **范围**：批次管理先只做回访工作台；新客（商城名单）批次化二期再说；**「重复客户体检」功能**（扫描电话相同店名不同/店名相同区域同的疑似重复，供老板批量合并或标记）作为批次上线后的下一迭代

### 页面设计
- **客户管理页 级1 批次卡片列表**（按日期倒序）：卡片=批次名（✏️改名）+ 副标题 + 导入日期 + 客户数 + 三色统计；操作按钮：＋新建任务（预选该批）/ 📥追加导入 / 📤导出 / 🗑删除卡片（解散；二次确认弹窗写明"客户不删除"）；顶部「📥 导入新批次」「＋ 手工建批」
- **级2 批次客户列表**（点卡片进入）：← 返回 + 批次名/副标题 + 统计 + 现有客户表格全功能（搜索/状态胶囊/坐标列/编辑/删单客/导出）
- **客户档案弹窗**（点客户名）：店名/地址/电话/坐标状态 + 所在批次列表（批次名 + 该批状态胶囊 + 该批拜访次数）
- **新建任务向导第 1 行**：新增「客户批次」选择（默认最近批次，显示批次名+副标题）；选定后第 2 步地图只显示该批客户、按批内状态显示圆点层
- 设置页新增「客户合并策略」：自动合并 / 弹窗确认（两选一）

### 云函数改动（adminapi）
- 新增：`listCustomerBatches`、`renameCustomerBatch`（含 subtitle）、`deleteCustomerBatch`（解散）、`createManualBatch`（手工建批）、`appendImportCustomer`（追加导入）、`exportBatch`
- 改造：`importCustomers`（自动建批+自动命名+按 mergeMode 合并）、`listCustomers`（按 batchId 过滤 + 返回 batchIds/所在批状态）、`createTask`（校验 batchId、任务绑定批次）、任务推进时更新 batch_members.status

### 实施顺序（开工后按此推进）
1. adminapi：集合初始化 + 全部批次接口 + importCustomers 改造 + createTask 校验
2. 一次性初始归档脚本（经 adminapi 手动触发：建「2026年9月1日第一批导入」+ 全量入批）
3. 后台客户管理两级页面 + 档案弹窗 + 手工建批选人界面
4. 新建任务向导批次选择 + nt-map.js 按批次过滤与批内状态圆点
5. 设置页合并策略开关
6. 全链路自测 + 重传 adminapi + 老板验收

## 7.15 语音转写（录音 → 文字）方案（2026-09-11 老板拍板；**M2a/M2c 已上线跑通，M2b 全量开发中**）

### 目标
拜访录音自动转成文字，员工端与后台都能看；费用可控（按段勾选 + 月度额度）。

### 技术选型（连被排除的一起记，避免重走）
- **腾讯云 ASR「录音文件识别」**：`asr.tencentcloudapi.com` / service `asr` / version `2019-06-14`；**异步**（CreateRecTask → DescribeTaskStatus 轮询），绕开云函数 30s 超时
- **支持 m4a + mp3** ← 关键：iPhone 录的是 aac(.m4a)
- **已排除**：智谱 glm-asr 只吃 wav/mp3，iPhone 的 m4a 不可用
- 上传方式：**URL 方式**（云存储临时链接给腾讯云抓取，`SourceType=0`）；`SourceType=1` 是 Base64 直传兜底（≤5MB，按编码前算）
- 计费：免费 10 小时/月，超量约 1.75 元/小时起

### 配置（云端 settings，key = `asrConfig`）
`{ "enabled": true, "secretId": "...", "secretKey": "...", "monthlyQuotaMin": 600 }`
⚠️ 腾讯云密钥不要贴进聊天/文档；已提醒老板轮换过一次暴露的密钥。

### 数据结构
- 新增集合 **`transcripts`**（`init` 的 COLLECTIONS 已含）
  字段：`visitId / taskId / customerId / salesmanId / salesmanName / segIndex / audioFileID / duration / status(pending|processing|done|failed) / asrTaskId / text / errorMsg / requestedAt / updatedAt / doneAt / byOpenid`
- `visits` 新增 **`audios: [{fileID, duration, transcribe}]`**（多段）；`audio` 保留为第一段（兼容旧前端）
- ⚠️ 归属一律取 **`visit.salesmanId`**（后台触发也不会写成管理员）

### 接口
- **云函数 `transcribe`**（新建）：`start`（提交识别，支持 `visitId`+`segIndexes` 或 `fileIDs`）/ `poll`（查结果；**无 OPENID 时走 pollAll** 给定时触发器）/ `retry`（重转单段）/ `list`（查某次拜访）/ `usage`（本月用量）
  - 定时触发器 **`transcribePoll`** cron `0 */5 * * * * *`（**部署后必须手动确认/新建**，与 `visitWindowTick` 同样踩过）
  - 保险：提交超过 **30 分钟**未出结果 → 判 `failed`（防僵尸任务被无限轮询）
  - 老板模式（isBoss）一律**虚拟成功**（不写库不计费）
  - **M2c 新增「管理端入口」**：无 OPENID（adminapi 转发进来）时用管理员 username/password 校验，标记 `e.real=true` 走**真实执行**（跳过虚拟成功）
- **adminapi**：`transcribeVisit`（后台手动触发/重转）、`transcribeUsage`（本月用量 + 各状态条数）；`listCustomerVisits` 返回带 `audios` + `transcribe`
- **visits**：`history` 返回带 `audios` + `transcribe`（批量查 transcripts 聚合成 processing/done/partial/failed）

### 各端口径
- **手机端（业务员）**：可看**自己**那条的文字，且**可以编辑**（2026-09-11 老板改口：前后台都可编辑，**推翻此前"只读不可改"口径**）；老板/管理员可看全部
- **后台**：拜访详情显示文字（只读 + 复制）+ **三图标摘要**（📷照片 / 🎙录音 / 📝转写）+ 标题栏「本月转写 X/600 分钟」；可点「🎙 转文字」/「↻ 重转」
- 文字**不加时间戳**（清洗成纯文本）；员工侧**不做手动重试按钮**（系统自动重试 + 后台可重转）
- 历史卡只显示 **2 行摘要**，**点文字**开页内弹层看全文（整篇复制）；不加「详情」按钮

### 进度
- ✅ **M2a 后端链路**：`transcribe` + 配置 + `visits/init` 改造；已部署跑通
- ✅ **M2b 小步**（手机端提交自动转写 + 客户详情展示）：代码完成，**待上传体验版**
- ✅ **M2c 后台**：已完成，**老板实测通过（能看文字、能听录音）**
- ✅ **M2b 全量（2026-09-11 已完成）**：手机端多段录音 —— **≤6 段 / 单条上限跟后台「拜访录音上限」档位（180/300/600 秒，iOS 另有封顶）/ 合计 30 分钟硬封顶** / 倒计时剩 30 秒红字放大闪动 / 录完一条条排着不覆盖 / 试听互斥 / ✕ 删除（二次确认）/ **按段勾选转写**（默认全选，未勾选只留档不计费）/ 顶部「**开始转录**」= 上传 + 触发转写
  · 2026-09-11 追加：多段文字之间用 **【录音 N】分隔**（编号取转录记录的**真实 `segIndex`**，避免某段失败时编号错位；见 §3.12-D-2）
  · 2026-09-11 追加：文字**前后台都可人工编辑**（`visits.trEdited` 人工版优先，重转不覆盖）
  · ⚠️ 录音时长真机 bug 见 §4 坑 27（`duration` 未传 → "录音 1 分钟就停"）—— **必须重传小程序体验版才生效**

### 部署队列（语音转写相关）
- ✅ 云函数 `transcribe` / `adminapi` / `visits` **已重传**（老板 2026-09-11）
- ⏳ **小程序待上传体验版**（含多段录音 + duration 修复 + 转写可编辑 + 战况文案）
- ⏳ 后台 Ctrl+F5；核对 `transcribe` 触发器 `transcribePoll`（cron 每 5 分钟）

### 新增的坑
- **微信开发者工具模拟器的录音基本不可用** → 转写链路只能在真机验；后台侧可先用「🎙 转文字」对**已有录音**补转，不必等真机
- `config.json` 的 `triggers` **不一定随部署自动创建** → 控制台手动确认/新建 `transcribePoll`
- 云函数间调用（`cloud.callFunction`）**不透传 OPENID** → 用账号密码鉴权区分"定时触发器"与"后台管理端"两条无身份路径
- 开发者工具**多端模式**（`projectArchitecture: multiPlatform`）：项目根下有 `i18n/`、`miniapp/`、`project.miniapp.json` 等多端产物；**小程序正确打开目录是 `D:\WFR\visit-miniapp\miniprogram`**（不是 `admin`，那是电脑后台）
- 小程序版本号在 `app.js` 的 `APP_VERSION`（当前 **0.9.11**），显示在「我的」页左下角 → 用它自检是否加载到了新代码
- 用 `write_file` 生成的 `.bat` 是 **LF 行尾**，cmd 会解析出错（`echo` 被吃成 `ho`）→ 需要 bat 时要**单行**写，或事后转 CRLF
