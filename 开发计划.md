# 客户回访管理系统 —— 需求详述与开发计划

> 项目目录：`D:\WFR\visit-miniapp`（小程序名：聚火拜访）
> 文档版本：v0.6 正式版（2026-09-01 定稿）
> 状态：**双工作台版正式成稿**——v0.5 回访客户版与 v0.6 双工作台版均已封存至 `versions/`，两版本并存、最终由老板选择；本文档为 v0.6 的开发权威依据
>
> **开发进度（2026-09-02）**：第一期 · 批次 1「小程序端主链路」代码完成已交付验证——云函数 8 个（init/ping/login/tasks/visits/subscribe/coordfix/seed/bindadmin）+ 小程序 5 页（登录绑定/任务列表/任务详情/客户详情分流/拜访记录）；**批次 2「管理后台」第一批已交付**：adminapi 云函数（登录/任务 CRUD/客户/人员/订阅推送 stub）+ admin.html（登录→双工作台→任务管理真实创建→客户/人员查看）+ server.js 零依赖代理（双击启动）；下批：客户导入导出/任务编辑撤回/认领核对/双口径报表；批次 3「地图·多媒体」（二期）待开工。

---

# 第一部分：需求详述

## 1. 项目背景与目标

**业务背景**：老板经营一家一次性餐盒/餐具的商城（"聚火配送"），面向永康市及周边的餐饮商户（面馆、小吃店、饭堂、菜场摊位等）供货。现有客户约 1000~2000 家，每周订单量 500~1000 单，业务员 3~5 人。

**业务痛点**：业务员需要定期上门回访客户，但当前缺乏数字化工具——客户位置分散、拜访路径靠经验、拜访记录靠口头/微信、回访效果（是否带来订单）无法量化。

**项目目标**：建设一套"客户回访管理系统"，包含**业务员微信小程序**和**独立 Web 管理后台**，实现：

1. 客户位置地图化管理，业务员按规划路径上门拜访；
2. 每次拜访完整留痕（拍照、录音、文字），支持多次拜访与历史调阅；
3. 每周导入销售订单，与回访记录比对，量化"回访带来的订单增量"，评估回访效果；
4. **（0.6 新增）纯新客户开发**：知道位置但未加入商城的客户，走"开发"链路——拜访引导注册商城 → 每周名单导入时综合模糊比对自动认领（手动标记兜底）→ 统计开发转化率与首单率，与回访激活业务并存。

## 2. 用户角色与权限

| 角色 | 使用端 | 权限 |
|---|---|---|
| 管理员（卿燕及管理员） | Web 管理后台 | 登录后进入**工作台**（🔥回访工作台 / 🌱新客开发工作台 / 全部总览，页面逻辑随工作台切换）；全部功能：客户管理、人员管理、任务管理、销售导入、效果报表、系统设置 |
| 业务员（3~5 人） | 微信小程序 | 查看分配给我的任务与客户（**任务内可见客户手机号**）、规划路径、提交拜访记录、查看客户的拜访历史 |

权限原则：
- 业务员只能看到分配给自己的任务及任务内的客户，任务外客户不可见；
- 客户手机号在任务内可见（已确认）；系统设置中可切换为"拜访时才可见"或"不可见"；
- 管理后台仅管理员可登录（账号密码）。

## 3. 核心业务流程

```
【每周循环 · 双工作台版】
① 管理员导入客户（原始客户表 / 商城筛选名单 / 新客户表；追加/替换两种模式；按类型标记 mall/new） ──► 客户库（自动按地址解析补坐标）
② 管理员进入对应工作台建回访任务：选业务员 + 选客户（列表筛选勾选 / 导入名单文件 / 地图点选，可混用）
③ 管理员选择完成天数（1~7 天），系统按路线/列表顺序自动把客户分摊到每天
④ 管理员可选：在地图上预规划拜访路线；定好设置后点"发送给业务员" ──► 业务员收到任务（含每天计划与预规划路线）
⑤ 业务员按天计划拜访（当天该去哪些店在任务内直接看），可按实时位置重规划/调整顺序
⑥ 到店 → 提交拜访记录（定位校验 + 拍照 + 录音 + 文字 + 样品 + 时长自动记录；结果集按客户类型动态显示）
⑦ 管理员每周导入销售表 ──► 系统按编号/店名/手机号逐级关联客户
⑧ 报表：回访工作台=回访后下单率；新客开发工作台=开发转化率与首单率
⑨ 新客户开发闭环：拜访新客户 → 引导注册商城 → 每周商城名单导入综合比对认领 → 自动换轨商城客户 → 开发指标自动更新
```

## 4. 数据源分析（已实测两个附件）

### 4.1 客户表（xls，887 行含表头 = 886 条数据）

| 列名 | 样例 | 说明 |
|---|---|---|
| 序号 | 1 | |
| 客户单位 | `a103 兰州拉面(邵宅店)` | 店名，**带编号前缀**（a/c/d/AA + 数字），是关联主键 |
| 区域 | 浙江省>金华市>永康市 | |
| 地址 | 永康市东清线附近 | 文本地址，部分很模糊 |
| 地图经纬度 | `120.0785,28.932013` | **格式：经度,纬度**（注意顺序）；精度参差不齐（有 6 位也有 14 位小数） |
| 备注 | | |

实测统计：
- 有店名 837 行，唯一店名 836（有 1 条重复）；
- **没有手机号列** —— 手机号需从销售表回填（见 4.3）；
- **48 家经纬度缺失或格式非法** —— 需要在地图上补标；
- 编号前缀分布：c×575、a×182、d×46、aa×34，共 836 个唯一编号。

### 4.2 销售表（csv，本次 4000 行，约覆盖 2026-07-19 ~ 07-23）

32 列，关键列：

| 列名 | 样例 | 说明 |
|---|---|---|
| 订单号 | 2334120118410908 | 唯一 |
| 客户单位 | `c1519 遵义宋记豆花面` | 与客户表同编号体系 |
| 联系信息 | `宋其豪-16620771142` | **100% 行含 11 位手机号**，格式：`姓名-手机号` 或 `店名-手机号` |
| 订单金额 | 58 | 数字 |
| 下单时间 | 2026-07-22 | YYYY-MM-DD，100% 规范 |
| 备注 | 部分行有业务备注（补货、换破损等） | 有回访价值的信息 |

实测统计：
- 唯一客户单位 968 家；
- **编码问题**：文件为 UTF-8 为主、个别行 GBK 的混合编码（如含 "★" 等字符的行），导入时必须逐行容错解码；
- 3 行客户单位以特殊符号开头（无编号），需按名称模糊匹配兜底。

### 4.3 两表关联方案（关键结论）

| 方案 | 可行性 | 结论 |
|---|---|---|
| **按店名编号关联**（a/c/d/AA+数字） | 实测命中率 **98.7%**（731/741） | ✅ 主关联方案 |
| 按手机号关联 | 客户表无手机号列，不可行 | ❌ 需先回填 |
| 按完整店名匹配 | 两表店名写法基本一致，可作兜底 | 辅助 |

**手机号回填机制**：销售表"联系信息"列 100% 含手机号，且 98.7% 的客户编号能对上 → 系统在每次导入销售表时，把该客户最新出现的手机号回填到客户档案，逐步补齐客户联系方式（实测仅 13 个客户单位出现过多个手机号，取最近一次/出现次数最多的为主号，其余存为备选）。

**编号前缀的真实含义（已确认）**：a/c/d/AA 前缀只是历史业务员代码，**系统不按前缀区分客户**；后续商城导入的客户名称普遍不带编号，关联时"有编号用编号，无编号用店名+手机号匹配"。

**待办**：原始客户表 48 家缺坐标 + 商城名单客户普遍无坐标——导入时对有地址的客户自动调用地址解析接口补坐标（coord_status=auto），解析失败/无地址的进"待补标"清单，管理员在地图上补标。

### 4.4 商城客户名单（已实测两个附件）

你放的 `三个月未下单-客户列表.xls`（483 行）和 `未购买-客户列表.xls`（254 行）都是商城导出的 47 列名单，结构相同。关键列：

| 商城列名 | 用途 |
|---|---|
| 客户名称 | 店名（**不带 a/c/d/AA 编号**，如"正宗淮南牛肉汤西站店"） |
| 地区 / 公司地址 | 省市区 + 详细地址（用于地址解析补坐标） |
| 客户联系人 / 联系人联系手机 | 联系人姓名与手机号（回填客户档案） |
| 添加时间 / 最后下单 / 最后浏览商城 | 商城档案信息（业务员回访时可见） |
| 最后拜访 | 商城侧历史拜访时间（首次导入可作参考） |
| 客户分类 / 等级 / 业务负责人 / 客单价 / 购买频次 / 购买次数 | 参考信息（入库留档，暂不参与逻辑） |

实测注意：日期列是 **Excel 数字序列**（如 46266.5 ≈ 2026-08-30），导入时须转换；"导入编码/客户编码"列为空；两张名单有重叠客户（未购买 ⊂ 三个月未下单），导入时按名称去重。

### 4.5 新客户表（0.6 新增，老板提供）

纯新客户：知道位置、尚未加入商城。以**表格导入为主**（管理员均可导入），模板列：

| 列名 | 说明 |
|---|---|
| 店名 | 必填 |
| 区域 / 地址 | 用于地址解析补坐标（无经纬度时自动解析，失败进待补标） |
| 联系人 / 手机号 | **手机号可空**（新客户常见缺联系方式，拜访时补录） |
| 备注 | 可选（如"朋友介绍"） |

导入时选择"本次导入类型 = 新客户"，客户入库即 `customerType=new`。也可后台手动新增（含地图标点）。

## 5. 功能需求

### 5.1 业务员微信小程序

**5.1.1 登录与身份**
- 微信授权登录；首次登录绑定姓名/手机号（由管理员预建账号，微信 openid 与账号绑定）。
- 登录后仅见分配给自己的任务。

**5.1.2 任务列表（首页）**
- 我的任务卡片：任务名称、剩余未拜访客户数、总客户数、截止日期、进度条。
- 任务状态：未开始 / 进行中 / 已完成。
- **今日计划**：顶部显示"今天计划 N 家"（按管理员排期），点击进入任务按天查看；不做每日推送，计划在小程序内直接看。

**5.1.3 任务地图**
- 进入任务后展示地图，任务内全部客户以标记点显示（红色=未拜访，绿色=已拜访，当前=蓝色）。
- 点击标记弹气泡：店名、地址、（任务内可见）手机号、最近拜访时间。
- 支持列表/地图切换，客户列表可按"未拜访/已拜访"筛选。

**5.1.4 拜访路径规划**
- **查看管理员预规划的路线**：任务若已带管理员规划的路线，业务员进入地图直接看到整条路线、拜访顺序列表（每段距离/时长）与总时长；
- **一键重新规划**：业务员可随时按自己实时位置重新规划（贪心排序 + waypoints 批量路线规划），覆盖预规划路线；
- 拜访顺序支持手动调整（上移/下移、拖拽），调整后自动重算该段路线；
- 拜访完一家自动标记，并高亮"下一家"（提供"导航到下一家"跳转微信导航）。

**5.1.5 拜访记录**
- 进入客户详情 → "开始拜访"。
- **拜访时长自动记录**：点"开始拜访"时系统记下开始时间，点"提交"时自动算出时长（提交时间 − 开始时间），业务员不需要任何额外操作；
- 记录内容：
  - **拍照**：门头照/现场照，可拍多张，可预览删除；
  - **录音（分段，录了不能删）**：**点击开始、再点一下停止**，每录一次固定为一段——第 1 段、第 2 段、第 3 段……，可录多段；录音中按钮变红并显示计时，单段限时（默认 5 分钟，微信上限 10 分钟，后台可配置），到点自动停止；**录过的片段业务员不能删除、不能重录（录了就留档），仅管理员可在后台删除**；
  - **文字**：拜访情况、客户反馈、注意事项（备注列里已有补货/退换货类信息，可参考）；
  - **样品/物料记录**（选填，单独字段）：如"带 24 标准白 1 件样品给客户"；
  - **拜访结果**（单选，**按客户类型动态显示**）：商城客户 8 项（极有意向 / 有意向 / 已下单 / 无需求 / 有抵触 / 联系不上 / 闭店·搬迁 / 其他）；新客户 9 项（同上 + **已注册商城**——开发第一里程碑：先注册、后下单）；
  - **下次跟进日期**（选填）。
- **定位校验**：提交时自动获取业务员实时位置，与客户坐标计算距离，超过管理员配置的阈值（默认 500 米，可调可关）则阻止提交并提示；**客户缺坐标时自动跳过校验**（只记录位置，不拦提交）。
- 提交后生成一条拜访记录；**同一天对同一客户只允许一次**（防重复提交，可配置）。
- **拜访结果=闭店·搬迁**：提交后该客户自动标记"待管理员处理"，管理员后台确认后停用客户。
- **"已下单"的含义（已确认）**：拜访时客户正好想下单，业务员**协助客户自己在商城下单**；小程序不做代客下单功能，是否真下单以销售表导入数据为准。
- **录音上传策略**：录音文件随拜访记录一并上传云存储（永久保留），不依赖手机本地；若当时无网络，自动进入本地待传队列（最多保留 24 小时），联网后自动补传并在下次打开小程序时提醒（详见 5.4）。

**5.1.6 客户详情与拜访历史（多次拜访，0.6 按类型分流）**
- 客户基本信息：店名、地址、**管理员备注**（后台填写/导入，业务员只读）；
- **状态标签按类型**：商城客户 = 沉睡客户（橙）/ 观望（黄）；新客户 = **新客户（蓝）**，已入商城后变绿"已入商城 ✓"；
- **联系方式**：有手机号 → 一键拨号；新客户无手机号 → 显示"暂无手机号"，拜访记录页提供"**补录联系方式**"（联系人/手机号，选填），提交后后台生成"待更新档案"提醒，管理员确认后写入客户档案（业务员不能直接改客户档案）；
- **商城档案区按类型**：商城客户 = 添加商城时间/最近下单/最近浏览 + 订单概况（第三期后）；新客户 = **开发卡**：未入商城显示"未加入商城 · 引导注册商城 / 争取首单"；已入商城显示"已入商城（X月X日）" + 订单概况；
- 历史拜访时间线：每次拜访的日期、业务员、结果、文字、样品记录、照片（可全屏看）、录音（**多段可逐段播放**）、拜访时长、当时定位与距离；
- 不限拜访次数，每次独立成档，可继续新增拜访。

**5.1.7 我的统计（轻量）**
- 个人本月拜访数、完成率、本月带回订单数（若有导入数据）。

**5.1.8 微信订阅消息：新任务通知（第一期做）**
- 管理员点"发送给业务员"时，业务员微信收到"您有新的拜访任务"提醒（一次性订阅消息）；**管理员修改进行中任务保存时，推送"任务已更新"提醒**（0.6，实时同步有感知）；
- 订阅入口常驻首页/任务页：业务员点一次授权可收到一条，授权失效后再次引导续订；
- 不做"今日待拜访"每日推送（已确认），当天计划在任务内直接看；"拜访完成回执"留作可选扩展。

**5.1.9 位置报错与坐标纠错**
- 客户详情"位置报错"按钮：业务员站到客户店门口点一下，用**当前定位**直接上报新坐标（可附文字说明），操作越简单越好；
- 提交后进入后台"坐标纠错"审核列表：管理员地图预览新旧位置，批准后更新客户坐标（标记手动修正），驳回则忽略；
- 管理员也可随时在后台地图上直接改坐标（原有功能）。

**5.1.10 位置自动上报（业务员无感，Q23 已定）**
- 小程序**前台期间**每 5 分钟自动上报一次位置（业务员无感，仅前台采集，切后台即停；系统设置可关闭）；
- 管理员后台点"查询位置"时，业务员小程序前台轮询到后立即回一次当前位置；
- 说明：微信对非物流/出行类目不开放后台持续定位，故不做"关掉小程序仍上报"。

### 5.2 Web 管理后台

**5.2.1 登录**
- 管理员账号密码登录；**系统预置唯一超级管理员账号「卿燕」**（初始化脚本创建），其余管理员账号由她在"人员管理"中动态增删。

**5.2.2 仪表盘**
- 今日/本周拜访量、进行中任务数、客户总数、本周订单数；
- 回访转化率（有数据后显示）。

**5.2.3 客户管理**
- 客户列表：搜索（店名/编号/手机号）、按区域/有无坐标筛选、分页。
- **导入客户，兼容两种格式**：① 原始客户表（序号、客户单位、区域、地址、地图经纬度、备注）；② 商城客户名单（47 列，自动识别关键列：客户名称/地区/公司地址/联系人手机/添加时间/最后下单/最后浏览商城/最后拜访等）。上传后预览解析结果（成功/失败/缺坐标/重复）再确认入库；
- **导入模式二选一（已确认）**：**追加**（默认，按编号/店名匹配：已存在则更新，不存在则新增）或 **替换**（旧客户全部停用后导入新表，历史拜访与订单记录全部保留）；
- **自动地址解析补坐标**：商城名单没有经纬度列，导入时对有地址的客户自动调用腾讯位置服务地址解析接口补坐标（coord_status=auto），解析失败/无地址的进"待补标"清单；
- **地图标点**：对待补标或坐标不准的客户，在地图上搜索地址并手动打点修正；
- **导出客户列表**：一键导出为 Excel（.xlsx），**36 列详细分列留档**——基础信息（序号/编号/店名/客户类型/状态/联系人/手机号/备选手机号/来源）、区域坐标（省/市/区/地址/经度/纬度/坐标状态）、商城档案（添加商城时间/最近下单/最近浏览商城/入商城时间/入商城方式/等级/分类/商城业务负责人/客单价/购买频次/购买次数）、拜访统计（累计拜访次数/最近拜访时间/最近拜访结果/最近拜访业务员）、订单统计（回访后订单数/累计订单数/累计订单金额）、其他（管理员备注/创建时间/更新时间）；**导出范围跟随当前筛选**（可按类型/区域/坐标/状态导出子集）；文件命名"聚火客户列表-YYYY-MM-DD.xlsx"；
- **坐标纠错审核**：处理业务员上报的"位置报错"申请（地图预览新旧位置，批准/驳回）；
- **客户类型（0.6）**：列表加"类型"列与筛选（全部 / 商城客户 / 新客户）；新客户状态标签 = 待开发（蓝）/ 已入商城（绿）；
- **认领核对（0.6）**：每周导入商城名单后自动对新客户做综合模糊比对（手机号精确命中 > 经纬度距离 ≤300 米 > 店名相似度 > 联系人/地址辅助），高分自动认领（标记已入商城+时间），中分进"待确认认领"清单（并排对比两边信息：确认 / 驳回 / 改关联），低分忽略；另有"手动标记已入商城"与"撤销/修正认领"兜底；
- 编辑：店名、手机号（回填/手改）、区域、地址、商城档案时间、备注；停用/启用；
- 客户详情：基础信息 + 全部拜访时间线 + 订单历史（导入后）。

**5.2.4 人员管理**
- **业务员**：新增 / 编辑 / 停用 / 删除——姓名、手机号、微信 openid 绑定（业务员首次登录小程序自动绑定）；列表展示本月拜访数、绑定状态；
- **管理员**：新增 / 删除管理员账号——登录账号、密码（可重置）、姓名；**角色分"超级管理员（卿燕，唯一，预置）"与"管理员"**，其余管理员由卿燕动态调整；
- **权限**：本菜单**仅超级管理员可见**，普通管理员登录后没有"人员管理"入口；
- 删除均为软删除（保留历史拜访与操作留痕）。

**5.2.5 任务管理（三层结构：列表 → 详情 → 新建/编辑向导，0.6 扩展）**
- **任务列表（默认视图）**：筛选（状态 / 业务员 / 拜访目的 / 任务名）+ 列表（任务名 / 业务员 / 目的标签 / 客户数 / 进度条 / 截止日 / 状态 / 操作）；操作按状态：**草稿** = 编辑·发送·删除；**进行中** = 查看·编辑·延期·改派·撤回·复制·结束；**已完成/已过期** = 查看·复制；过期任务自动标记，可延期恢复进行中；
- **任务详情**：概览卡（进度环 / 发送时间 / 截止日）+ **任务效果小结**（拜访 X 家 → 回访后下单 Y 家 → 金额 Z，销售导入上线后自动统计）+ 每天计划（可拖拽调户、挪天）+ 地图视图（预规划路线 + 已拜访状态 + **业务员实际轨迹叠加**）+ 本任务拜访记录（含待复核审核）+ 按状态显示操作按钮；
- **编辑规则（0.6 已拍板）**：进行中任务可实时编辑（客户 / 排期 / 延期 / 改派），**保存即同步业务员端，并推送"任务已更新"订阅消息**；**撤回 → 回草稿可改可重发**，已提交的拜访记录全部保留；删除客户时其拜访记录保留；已完成任务只读+复制（不篡改历史，保证报表口径）；
- 创建/编辑任务（**草稿 → 发送**两步，编辑时预填）：
  - 任务设置：任务名称、选择业务员、截止日期、**完成天数（1~7 天）**、**拜访目的（激活回访 / 新客开发，按所选客户类型自动建议，可手改）**；
  - **选择客户，三种方式可混用**：
    - ① 列表筛选批量勾选（按区域/编号/店名筛选）；
    - ② **导入名单文件**：上传商城筛选名单（如"三个月未下单客户列表"），按店名+手机号匹配客户，未匹配的行列出可手动处理（可一键新建客户）；
    - ③ **地图点选**：地图上显示全部客户标点（千级标点缩放时自动聚合），点击标点选中/取消，支持框选批量选，随时查看已选清单；
  - **按天排期（自动）**：选好客户后，系统按（路线/列表）顺序把客户**平均分摊到所选天数的每天**，展示每天名单预览，管理员可微调（把某家店挪到别的天）；
  - **客户重叠提示**：勾选时高亮"已在其他进行中任务"的客户（允许强制分配，但让管理员知情）；
  - **管理员预规划路线**（可选）：选好客户后在地图上生成拜访路线（起点可设仓库/首店/自定点，贪心排序 + waypoints 批量路线规划，与 11.1 同方案），预览整条路线与总距离/总时长；不预规划则业务员端自行规划；
- **发送给业务员**：点击后任务由草稿变为已发布（业务员小程序立即可见，含每天计划与预规划路线），并推送"新任务通知"订阅消息；草稿状态业务员不可见；
- **已发送任务可撤回/改派**：管理员可撤回重发、可改派给另一个业务员（操作留痕）；到期未完成的自动提醒管理员、可延期，不自动关闭；
- **复制任务**：一键复制历史任务（同业务员+同批客户+同路线+同排期，改个截止日期即可），用于每周重复回访；

**5.2.6 销售数据导入**
- 每周上传销售表 csv/xls，预览解析（列映射自动识别 + 可手动调整），确认后入库为订单数据。
- 导入过程自动完成：手机号回填客户档案、订单关联客户（按编号，兜底模糊匹配）、生成导入批次记录（可追溯、可回滚）。

**5.2.7 回访效果报表**（核心价值）
- **回访后下单率（主指标，已确认先只做这一个）**：回访客户数、其中回访后窗口期内（默认 30 天，可配置）下单的客户数、下单率；
- **按客户类型分组（0.6）**：筛选条加"客户类型"（全部 / 商城客户 / 新客户）；商城客户组 = 回访后下单率（现有口径不变）；新客户组 = **开发转化**：拜访新客户数 → 已入商城数 → 开发转化率 → 其中首单数/首单率（入商城后窗口期内下单）；
- **业务员排行**：拜访量、带回订单客户数、带回订单金额、转化率、**开发客户数（0.6 新增）**；
- **客户级明细**：任意客户 → 全部拜访记录 + 其后订单，可点"详情"查看；新客户额外显示**开发轨迹**（拜访记录 + 入商城时间 + 首单订单）；
- **按拜访结果过滤**：报表可按拜访结果筛选（如只看"极有意向/有意向"客户的转化），看哪类回访最有价值；
- **时间趋势**：每周导入后自动更新；
- **导出**：报表支持导出 Excel（可选）；
- **口径（已确认）**：金额用"订单金额"列（缺货金额单独展示）；不过滤订单状态，全部订单计入；"回访后下单"按下单时间判定；客户被多个业务员回访过，功劳算最近一次回访的业务员；
- **对照组与回访前后对比**：先不做（Q22 已确认），后续版本再加；
- **数据积累期说明**：上线初期（约 4 周）"回访后下单"样本少，转化率波动属正常。

**5.2.8 系统设置**
- 定位校验：开关 + 阈值（米）；
- 回访效果比对窗口天数（默认 30）；
- 同客户每日拜访次数限制（默认 1）；
- 手机号可见性（默认任务内可见）；
- 单次录音时长上限（默认 5 分钟，最大 10 分钟）。

**5.2.9 拜访记录管理**
- 列表：按业务员/客户/日期/拜访结果筛选，查看全部拜访记录；
- 详情：文字、照片（原图查看）、录音（在线播放）、提交时定位与距离；
- 管理：管理员可**编辑/删除**拜访记录（业务员提交后不可自行修改删除）；"待复核"记录（定位校验失败的降级记录）在此审核通过或驳回。

**5.2.10 业务员位置地图（Q23 已定：A+B 组合）**
- 地图上查看每个业务员**当天轨迹点**（前台每 5 分钟自动留点，连点成线），显示各点时间；
- **点名查询**：点"查询位置"按钮，业务员小程序在前台时立即回传当前位置，地图上高亮显示"位置+时间"；
- 轨迹数据只保留最近 90 天，控制存储成本。

**5.2.11 客户批次管理（2026-09-07 方案定稿，详见《项目交接-当前进度.md》§7.12）**
- 回访客户按导入批次形成「批次卡片」列表（按日期排列、可改名、可写副标题说明），点卡片进批内客户列表；
- 卡片操作：＋新建任务（预选该批）/ 📥追加导入 / 📤导出 / 🗑删除卡片（=解散名单，客户保留回「未分批」）；
- **手工建批**：从全部客户勾选组批（第二列显示已入批/未分批状态，点已入批胶囊看所在批次），新批内状态一律待回访；
- **客户档案弹窗**：任一客户可查所在批次及批内状态（待回访/拜访中/已回访）；
- **新建任务向导第一行选批次**，一个任务=一个批次，地图与选店按批过滤、按批内状态着色；
- 同电话合并策略在设置页切换（自动合并/弹窗确认）；初始归档「2026年9月1日第一批导入」收纳全部历史客户。

### 5.3 数据导入与比对规则（汇总）

1. **客户导入**：兼容三种格式（原始客户表 / 商城客户名单 / **新客户表**，自动识别列）；导入模式：追加（默认，匹配则更新、不匹配则新增）或替换（旧客户全部停用、历史保留）；**新客户表导入时选择类型 = 新客户（customerType=new）**；缺坐标不阻断（标记待补标 + 自动地址解析补坐标）；**手动修正过的坐标（coord_status=manual）不被导入覆盖**。
2. **销售表导入**：每周一次；混合编码容错；列名自动识别；**按订单号去重**（orderNo 唯一索引，重复导入自动跳过）；**报表统计不过滤订单状态**（全部计入，状态留档备查）。
3. **关联规则**：有编号用编号（a/c/d/AA+数字，忽略大小写与空格）→ 无编号或未命中时用"店名精确/模糊匹配"→ 再兜底"手机号匹配"（商城名单导入场景）；仍未命中的进入"未关联订单/未匹配客户"清单，管理员手动处理。
4. **回访效果判定**：某客户的拜访日期为 D，若窗口期内（D, D+N]（N 默认 30 天）存在该客户订单（按下单时间判定），则该次回访记为"有效回访"；订单金额用"订单金额"列，缺货金额单独展示；客户被多个业务员回访过，功劳算最近一次回访的业务员。
5. **对照口径（后续版本）**：对照组与回访前后对比先不做（Q22），后续版本再补。
6. **入商城认领（0.6）**：每次导入商城名单后，对新客户跑综合模糊比对（手机号精确命中权重最高 > 经纬度距离 ≤300 米 > 店名相似度 > 联系人/地址辅助加分），分三档处理：高分**自动认领**（标记已入商城 + 入商城时间，joinedVia=import）、中分进**待确认清单**（后台并排核对）、低分忽略；**手动标记 / 撤销 / 修正认领**兜底。

### 5.4 录音保留与备份方案（含调研结论）

**微信官方限制（已查证）**：

| 项目 | 结论 |
|---|---|
| 单次录音时长 | `RecorderManager.start` 的 `duration` 参数最大 **600000ms（10 分钟）**，默认 60000ms（1 分钟）；不传不会无限录，须显式限时 + 到点自动停止 |
| iOS/Android 差异 | 时长无平台差异；`audioSource` 参数有平台差异（部分选项仅 iOS/仅 Android），录音主流程双端一致 |
| 本地临时文件 | 录音产生的 tempFilePath **只保证当前小程序生命周期内有效，冷启动后可能被清理** —— 不能依赖本地保存 |
| 本地缓存文件 | `wx.saveFile` 缓存文件"仅在代码包被清理时清理"，比临时文件稳定；但官方旧文档记载缓存总上限 10MB（该 API 页面已下线，未在线复核）；社区流传 200MB 指本地用户文件目录，未获官方原文证实 |
| 云存储 | 单文件上限约 5GB（分片上传，以控制台为准）；按量付费：存储约 0.0004 元/GB/天、下载流量约 0.35 元/GB，新环境含免费额度（以控制台账单为准） |
| 定时任务 | 云函数支持 cron 定时触发器（7 段，UTC+8），每日自动备份可行 |

**结论与方案**：
1. 单次录音限时默认 5 分钟（微信上限 10 分钟，后台可配置），到点自动停止；
2. **录音不依赖本地保留**：录制完成后随拜访记录立即上传云存储，云端永久保存 —— 这解决了"本地保留 24 小时"不可靠的问题；
3. 无网络时进入**本地待传队列**（存 `wx.env.USER_DATA_PATH`，缓冲 24 小时），联网自动补传；本地文件只作临时缓冲，不视为存储手段；
4. **每日备份到本地**：云函数定时触发器（每天 02:00）把当天新增录音及全量数据导出到云存储 `backups/` 目录，管理后台提供"下载备份"按钮（生成 zip 下载到管理员电脑），满足"每天后台备份到本地"；
5. **容量估算**：5 分钟 mp3（约 32kbps）≈ 1.2MB/段；3~5 业务员 × 每天约 10 家 × 1.2MB ≈ 50MB/天，一个月约 1.5GB，云存储费用每月约 0.02 元量级，可忽略；24 小时本地缓冲远小于手机容量。

## 6. 非功能需求

| 类别 | 要求 |
|---|---|
| 数据量 | 客户 ≤3000，订单每周 ≤2000（导入后累计），拜访记录每周 ≤500 条 —— 云开发完全够用 |
| 性能 | 地图加载 100+ 标记点流畅；报表查询 <3 秒（必要时聚合缓存） |
| 安全 | 云函数鉴权（openid 校验）；业务员仅能读自己任务数据；手机号字段敏感，接口按权限过滤 |
| 隐私合规 | 隐私政策声明收集手机号/位置/照片用途；录音与照片上传前用户知情 |
| 可靠性 | 提交拜访记录幂等（防重复）；导入支持回滚 |
| 兼容性 | 微信基础库 ≥3.0；iOS/Android 双端 |

## 7. MVP 范围边界（第一期先不做）

- ❌ 业务员端不做客户自主认领、抢单；
- ❌ 不做拜访轨迹回放（仅记录点）；
- ❌ 不做多仓库/多城市扩展（当前仅永康）；
- ❌ 不做订单明细商品维度分析（报表先到客户/金额维度）；
- ❌ 不做录音转文字（已调研：微信同声传译仅支持 60 秒实时流式、无法转写已录文件；腾讯云 ASR 支持文件转写但江浙方言模型按小时计费约 0.8~2.4 元/小时，且实地交流方言口音多、准确率没保障——决定不做，后续需要时按已调研方案随时启用）。

---

# 第二部分：开发计划

## 8. 总体技术架构

```
┌─────────────────────┐      ┌──────────────────────┐
│ 业务员微信小程序      │      │ Web 管理后台           │
│ (原生 + 云开发SDK)    │      │ (Vue3 + Element Plus)  │
└──────────┬──────────┘      └──────────┬───────────┘
           │ 云函数调用                   │ 云开发 Web SDK / HTTP API
           ▼                            ▼
┌──────────────────────────────────────────────────────┐
│           微信云开发（env: 生产环境）                   │
│  云数据库：customers/users/tasks/visits/orders/...    │
│  云存储：photos/recordings                           │
│  云函数：login/importSales/planRoute/checkDistance/   │
│          reportStats/...                            │
└──────────────────────────────────────────────────────┘
                        │
                        ▼
           腾讯位置服务（WebService 路线规划，key 由云函数代理）
```

**技术选型明细**：
- 小程序：微信原生框架（WXML/WXSS/JS），`wx.cloud` SDK；
- 管理后台：Vue 3 + Vite + Element Plus，部署到微信云开发静态托管（免服务器，默认 HTTPS 域名；绑定自有域名需 ICP 备案）；登录鉴权走云函数（账号密码 → 会话 token）；后台地图用腾讯位置服务 JavaScript API GL（同 key，域名白名单含后台域名）；
- 云开发：按量付费；数据库集合级权限（仅云函数可写，客户端受控读）；
- 地图：小程序 `map` 组件（腾讯地图），路线规划走云函数代理调用腾讯位置服务 WebService API（key 不暴露给客户端）；已研读官方插件文档并评估："路线规划"小程序插件为整页跳转、仅支持单起终点、不支持途经点与批量，不适用本项目多客户顺序路径场景，故不采用（详见 11.1）。

## 9. 数据模型设计（云数据库集合）

**customers 客户**
```
_id, code(编号key, 可选：如 c1519；商城名单客户可能无编号), name(店名),
customerType(mall商城客户/new新客户，0.6), mallJoinedAt(入商城时间), joinedVia(import/manual),
phone(主手机号), phones(备选数组), contactName(联系人),
province/city/district(从区域拆分), address, lat(纬度), lng(经度),
coord_status(enums: ok/missing/auto/manual), status(active/disabled),
source(import/manual), remark,
mallAddedAt(添加商城时间), lastOrderAt(最近下单时间), lastBrowseAt(最近浏览商城时间),
mallExtra(商城名单其他字段留档: 等级/分类/业务负责人/客单价/购买频次/购买次数),
reviewFlag(拜访结果"闭店·搬迁"后待管理员处理), createdAt, updatedAt
```

> **2026-09-07 新增（客户批次管理，方案定稿见交接文档 §7.12）**：customers 加冗余 `batchIds[]`（所在批次 id 列表，空=未分批）；新增两个集合：
> - **customer_batches 批次卡片**：`name`（批次名可改）、`subtitle`（副标题/说明可改）、`createdAt`、`createdBy`、`customerCount`、`stats{待回访/拜访中/已回访}`
> - **batch_members 名单关系（多对多）**：`batchId`、`customerId`、`status`（批内状态：todo 待回访 / in_task 拜访中 / visited 已回访）
> 口径：客户档案唯一（同电话合并，设置页 mergeMode 自动/弹窗确认两档）；批内状态独立（新入批=待回访）；删批次=解散名单不删客户；tasks 加 `batchId`（一个任务=一个批次）；初始归档「2026年9月1日第一批导入」收纳现有全部客户。

**users 用户**
```
_id, openid(业务员微信绑定), username/passwordHash(管理员账号密码),
name, phone, role(salesman/admin/super_admin), active, lastLoginAt
```

**tasks 任务**
```
_id, name, salesmanId, salesmanName, customerIds[], deadline,
status(draft草稿/published已发布/done完成), createdBy, createdAt,
sentAt(发送给业务员的时间), doneAt,
plannedDays(完成天数 1~7), dayPlan: [{day:1, customerIds[]}](按路线/列表顺序自动分摊，管理员可微调),
purpose(拜访目的: activate激活回访/develop新客开发，0.6),
routeInfo(管理员预规划路线，可选): {
  order: customerId[](按拜访顺序), startPoint{lat,lng}, plannedBy(admin/salesman),
  segments: [{from,to, polylinePoints[], distance, duration}],
  totalDistance, totalDuration, plannedAt
}
```

**visits 拜访记录**
```
_id, taskId, customerId, salesmanId, visitedAt(拜访日期), result(enum),
text, samples(样品/物料记录), photoFileIds[],
recordings: [{fileId, duration}](多段录音，按第1/2/3段顺序追加，业务员不可删),
durationSeconds(拜访时长 = 提交时间 − 开始拜访时间，自动计算),
submitLat/submitLng(业务员提交时定位), distanceToCustomer(米),
status(normal/pending_review, 定位校验失败时降级待复核),
nextFollowUpDate, createdAt, _uniq(customerId+date, 全局唯一索引防重复)
```

**orders 订单**
```
_id, orderNo(唯一), customerId(关联后), customerCode, customerName(原始),
phone, amount, orderDate, note, batchId(导入批次), raw(原始行JSON, 可追溯)
```

**import_batches 导入批次**
```
_id, type(customer/sales), fileName, totalRows, okRows, failRows, createdAt, status
```

**coord_fix_requests 坐标纠错申请**
```
_id, customerId, salesmanId, newLat, newLng, note,
status(pending/approved/rejected), createdAt, handledAt
```

**salesman_locations 业务员位置点**
```
_id, salesmanId, lat, lng, reportType(periodic前台定时/query点名查询),
createdAt(TTL: 保留 90 天)
```

**mall_claims 入商城认领记录（0.6）**
```
_id, customerId, mallName(名单侧店名), mallPhone, mallLat/mallLng(名单侧坐标),
score(综合匹配分), status(auto自动/待确认/confirmed确认/rejected驳回/manual手动),
createdAt, handledAt
```

**settings 系统设置**
```
_id, key, value   // locationCheck{enabled,threshold} / compareWindowDays /
                  // dailyVisitLimit / phoneVisibility / recordingDurationLimit
```

**indexes**：customers(code 唯一)、visits(customerId+visitedAt 唯一)、orders(orderNo 唯一)、orders(customerId+orderDate)、orders(batchId)

## 10. 开发里程碑与验收标准

> 工期为单人开发估算（不含测试缓冲）；每期结束都有**可演示的可用版本**。

### 第一期：基础平台（约 2 周）—— 跑通"任务→拜访→记录"主链路

**范围**：
- 云开发环境初始化、集合建表、初始化管理员脚本（预置唯一超级管理员「卿燕」）；
- 小程序：登录绑定、任务列表（含"今日计划"）、任务内客户列表/详情（**按客户类型分流：商城档案卡 / 新客户开发卡；一键拨号，无手机号显示"暂无+补录"；位置报错上报**）、拜访记录（文字+结果+样品+时长自动记录、**结果集按类型动态显示：新客户多"已注册商城"**）、拜访历史时间线；
- **订阅消息**："新任务通知" + "任务已更新"推送（不做每日推送）；
- 管理后台：**登录后双工作台选择页（🔥回访工作台 / 🌱新客开发工作台 / 全部总览）+ 各页面随工作台切换框架（列表过滤/文案/指标联动）**、人员管理（业务员+管理员）、客户导入（**三种格式：原始客户表/商城名单/新客户表 + 追加/替换 + 自动地址解析补坐标**）、客户管理列表（类型列/筛选、导入按钮随工作台改名）、任务管理三层结构（列表→详情→新建/编辑向导，含拜访目的按工作台收窄、草稿→发送、名单导入选客户、1~7 天排期、复制任务）、客户导出（36 列）；
- 手机号自动回填在第三期销售导入后生效（本期先支持手动维护）。

**验收标准**：
1. 管理员能导入原始客户表 886 条，缺失坐标的 48 条被标记；能导入商城名单（三个月未下单/未购买样例），自动地址解析补坐标成功率可查，失败进待补标清单；
2. 管理员能建任务（选客户 + 完成天数 1~7 自动分摊到天）并发送给业务员，业务员小程序能看见每天计划；
3. 业务员能对客户提交文字+结果+样品+时长的拜访记录，同一客户同一天只能一次；
4. 客户详情能展示历史拜访时间线（含时长），能看到商城档案信息（添加时间/最近下单/最近浏览）并可一键拨号；
5. 数据权限：业务员只能访问自己任务内数据；
6. 管理员发送任务后，订阅过的业务员能收到"新任务通知"订阅消息（测试模板可验证）；
7. 任务支持草稿→发送流程：未发送的任务业务员不可见；支持用商城名单文件选客户；
8. 业务员可提交"位置报错"（当前定位上报），后台能看到申请列表；
9. **登录后可选择两个工作台，进入后客户/任务列表按类型过滤、按钮文案随工作台变化**；
10. **新客户详情显示开发卡（"未加入商城 · 引导注册商城/争取首单"），拜访结果集多出"已注册商城"；拜访目的选项按工作台收窄为唯一项**。

### 第二期：地图与路径（约 2 周）—— 核心体验

**范围**：
- 小程序任务地图（标记点、气泡、已/未拜访着色）+ 展示管理员预规划路线与每天计划；
- **后台地图选客户**：全部客户标点（缩放自动聚合）+ 点击/框选 + 与列表筛选、名单导入混用；
- **管理员预规划路线**：选好客户后一键规划（起点可设），预览总距离/时长，随"发送给业务员"下发给小程序；
- **业务员位置（Q23，A+B）**：小程序前台每 5 分钟自动留点 + 后台点名查询；后台地图看当天轨迹；
- **后台地图补标与坐标纠错审核**：待补标客户地图标点（含新客户手动新增）；"位置报错"申请地图预览新旧位置、批准/驳回；
- **任务详情地图视图**：预规划路线 + 已拜访状态 + 业务员实际轨迹叠加（Q23 留点数据复用）；
- 腾讯位置服务 key 接入 + 云函数代理路线规划（贪心排序 + waypoints 途经点批量规划，后台/小程序共用）；
- 拜访记录接入**拍照、录音**与云存储（含本地待传队列 24 小时缓冲 + 整条记录离线提交）；
- **每日自动备份**：云函数定时导出当日录音与数据到 backups/，后台可下载到本地；
- **定位校验**（提交时算距离 + 后台阈值配置，缺坐标自动跳过）；
- 后台任务地图总览：任务地图、管理员路线与拜访进度。

**验收标准**：
1. 100 家客户的标记点地图加载流畅，点击气泡显示店名/地址/手机号；
2. 规划路线返回真实路径与总时长，拜访顺序可调，已拜访标记绿色；
3. 拍照多张、录音可回放，均可随记录提交并在历史中回看；
4. 距客户超阈值提交被拦截；后台可关校验/调阈值；缺坐标客户自动跳过校验；
5. 提交记录时校验失败给出明确提示；
6. 断网时整条拜访记录（文字+照片+录音）可离线暂存，联网后自动补传；每日备份 zip 可从后台下载到本地；
7. 管理员能在地图上点击/框选客户建任务，选中数实时可见，可与表格筛选混用；
8. 管理员预规划路线后发送任务，业务员进入任务即看到预规划路线，且可一键按实时位置重规划；
9. 后台可审核"位置报错"申请（地图预览新旧位置，批准后客户坐标更新）；待补标客户可在地图上打点；
10. 后台能看到业务员当天轨迹点（前台每 5 分钟自动留点），点名查询时小程序在前台立即回传当前位置。

### 第三期：销售导入与回访效果（约 1.5~2 周）—— 数据闭环

**范围**：
- 销售表导入：混合编码容错解析、列映射自动识别、预览确认；
- 订单入库 + 自动关联客户（编号为主、名称兜底）+ 手机号回填 + 未关联清单；
- **入商城认领（0.6）**：导入商城名单后对新客户综合模糊比对（手机号精确 > 经纬度≤300米 > 店名相似 > 联系人/地址辅助，三档处理），待确认核对清单（并排对比+确认/驳回）+ 手动标记/撤销/修正；
- **双口径效果报表**：回访工作台 = 回访效果（回访后下单率、业务员排行、客户级明细、按拜访结果过滤、时间趋势）；新客开发工作台 = 拜访效果（开发转化率、首单率、新客户开发明细、业务员开发排行）；**仪表盘双版本**（新客开发工作台：开发漏斗、添加商城成功率、新客下单率，随每周导入自动更新）；
- 系统设置项（比对窗口等）接入报表；
- 仪表盘数据汇总。

**验收标准**：
1. 导入本次 4000 行销售数据，关联率 ≥98%，未关联的进入清单可手动处理；
2. 手机号回填：有订单的客户档案出现联系方式；
3. 报表能回答：回访客户下单率多少、哪个业务员效果最好、任何一家客户的回访与订单明细；
4. 比对窗口可在后台改（如 14/30/60 天），报表随之变化；
5. 客户级明细能点“详情”查看拜访记录+订单；
6. **导入商城名单后自动认领高分组新客户，中分组进待确认清单可并排核对确认/驳回**；
7. **新客开发工作台仪表盘显示添加成功率与下单率（含开发漏斗），随每周名单/销售导入自动更新；新客户认领后自动换轨商城口径**。

### 第四期：上线打磨（约 1 周）

**范围**：真机测试、权限与隐私复核、云函数限流、批量导入性能、错误提示与空态、加载体验、**双工作台切换全流程回归（两个工作台各走一遍完整业务闭环）**、正式环境发布、初始化真实数据、管理员使用培训（一次演示）。

**验收标准**：全流程真机走通；发布审核通过；真实客户数据导入后无异常。

> 合计约 6.5~7 周（1 人全职）。若压缩：第一期与第二期可合并并行做地图部分，总工期可压到 5 周左右。

## 11. 关键实现方案

### 11.1 路径规划（贪心排序 + 途经点批量规划）
- 排序起点：业务员端默认**当前定位**（`wx.getLocation`）；管理员预规划时可设起点（仓库/首店/自定点）；
- 先用客户坐标做**贪心最近邻排序**（从起点依次选最近的未拜访点，O(n²)，千级以内无压力）得到拜访顺序；
- 调用腾讯位置服务**驾车路线规划 WebService API**：`from`（起点）+ `waypoints`（途经点，按排序后的客户坐标，**单次最多 30 个**）+ `to`（终点），一次返回整条真实路线 polyline 与分段时长；任务客户 >30 家时按每批 30 个分段调用再拼接；
- 选型依据（已研读官方文档：pluginOverview / pluginStart / ai-map / routePlan 四页）：官方**"路线规划"小程序插件仅支持单起点+终点**（参数表无 waypoints 字段，四次官方文档均无途经点表述）、整页跳转式调用、无批量接口，**不满足"多客户顺序拜访一条完整路线"的需求**；故主方案采用 **map 组件 + 云函数代理 WebService API**（key 不暴露客户端，云函数侧天然可做缓存/限流/鉴权）；
- 同体系可选插件（非主路径，后续按需接入）：**地图选点插件**（小程序端坐标修正）、**地址输入插件**（地址录入）、**AI 地图生成插件**（限时免费，适合攻略类图文地图，与任务地图场景不同）——第一期均不做；
- 顺序调整 = 局部重排 + 重算受影响段落，性能可控；
- 路线 polyline 由云函数解析后返回，小程序 `map` 组件 `polyline` 绘制，拜访顺序列表展示每段距离/时长；
- **后台与小程序共用同一 planRoute 云函数**：管理员预规划与业务员重规划口径一致，路线结果存入 task.routeInfo 随任务下发。

### 11.2 定位校验
- 客户端 `wx.getLocation` 取实时坐标 → 提交云函数时带上；
- 云函数内用**球面距离公式**（Haversine）算业务员与客户的直线距离；
- 超过阈值返回错误，前端提示"距离客户过远（X 米）"；
- **客户缺坐标时自动跳过校验**（只记录业务员位置，不拦提交）；
- 开关关闭时仅记录距离不拦截。

> **2026-09-04 老板定稿口径（已实现）**：阈值 0~500 米；**填 0 或不勾选=关闭校验**；勾选+填 1~500 整数=按该值校验（前端阻止非法值，云端再钳制）；**默认 100 米**；**业务员定位失败=直接拦截提交**（提示"定位失败，无法校验距离，请检查网络与定位后重新提交"，不再降级"待复核"）。

### 11.3 导入解析容错
- 读取：逐行尝试 UTF-8 → 失败回退 GB18030（本次文件实测为混合编码）；xls 用 xlrd 解析；
- 列映射：按表头名匹配，别名表兜底（如"客户单位/店名/单位名称"）；商城名单自动识别关键列（客户名称/地区/公司地址/联系人手机/添加时间/最后下单/最后浏览商城/最后拜访）；
- **商城名单日期转换**：添加时间等列是 Excel 数字序列（如 46266.5），导入时转换为日期；
- 经纬度解析：客户表 `经度,纬度` 逗号分隔，**第 1 个数是经度、第 2 个是纬度**，入库拆为独立字段 lng/lat，校验数值范围（经度 73~135、纬度 18~54，超范围或缺失标为待补标）；
- **商城名单补坐标**：无经纬度列 → 对有地址的客户调用腾讯位置服务**地址解析**接口自动转坐标（coord_status=auto；配额足够，千级客户无压力），失败/无地址进待补标清单；
- **⚠️ 坐标顺序坑**：腾讯位置服务 API 的坐标参数格式是 `lat,lng`（纬度在前），与客户表相反 —— 云函数调用路线规划/地址解析 API 时必须转换，禁止直接透传客户表原始字符串；
- **导入模式**：追加（匹配则更新/不匹配则新增）或替换（旧客户停用、历史保留）；预览阶段不写库，确认后事务性入库；批次记录支持回滚。

### 11.4 客户-订单/名单关联算法
1. 提取编号 key：`/^([a-zA-Z]{1,3})(\d+)/`，小写归一、去空格（`C1519` → `c1519`）；**编号只是历史业务员代码，系统不按前缀分组，且新客户可能无编号**；
2. 有编号且命中 → 直接关联；
3. 无编号/未命中 → 店名规范化（去空格/括号差异）精确匹配 → 模糊匹配（包含关系）→ 手机号匹配（商城名单导入场景）；
4. 仍未命中 → 未关联清单，后台手动处理（名单导入场景可一键新建客户）。

### 11.5 坐标系说明
- 客户表经纬度来源标注为"地图经纬度"，若取自高德/腾讯（GCJ-02），与微信 map 组件一致可直接使用；
- **上线前抽样验证**：地图上抽查 10~20 个点是否落在真实店址；若系统偏差大（GPS WGS-84 数据），云函数统一做 GCJ-02 转换或重新标点。

### 11.6 权限与安全
- 云数据库权限设为"仅云函数可写"，客户端读取走云函数按 openid 过滤；
- 客户手机号字段：按 settings.phoneVisibility 在云函数层脱敏/放行；
- 管理后台会话：登录换取 token，云函数校验。

### 11.7 录音上传、每日备份与订阅消息
- 录音流程：**多段录制**——每段录完即固定（业务员不可删、不可重录），按第 1/2/3 段顺序追加；单段 ≤设置时长、到点自动停，**format 统一 mp3**；存本地待传队列（USER_DATA_PATH）→ 随拜访记录整体上传云存储 `recordings/{customerId}/{date}/seg{N}.mp3`；仅管理员可在后台删除拜访记录或其录音；
- 照片流程：拍摄/选择后 `wx.compressImage` 压缩（单张目标 ≤1MB）再上传，节省存储与流量；
- 断网处理：**整条拜访记录可离线暂存**（文字+照片+录音），有网自动补传；超 24 小时未传的在任务页提示；
- 每日备份：云函数 timer 触发器（`0 0 2 * * * *`，UTC+8 每日 02:00）整理当日新增录音到 `backups/YYYY-MM-DD/` 并导出各集合 JSON；管理后台"数据备份"页列出历史备份并一键打包下载 zip（管理员每周五手动下载）；
- 订阅消息（新任务通知）：管理员点"发送给业务员"时，云函数向该业务员推送一次性订阅消息（经 `getWXContext().OPENID` 下发，用消息 ID 去重）；**无每日定时推送**。

### 11.8 业务员位置上报（Q23 已定：A+B 组合）
- **前台定时留点**：小程序前台期间每 5 分钟 `wx.getLocation` 上报一次（settings 可关）；仅前台采集，切后台即停；
- **点名查询**：管理员后台点"查询位置"→ 云函数写入待查标记 → 业务员小程序前台轮询到后立即上报一次当前位置；
- 数据写入 salesman_locations，只保留 90 天（定时清理），后台地图按天回放轨迹并显示"位置+时间"；
- 边界说明：微信对非物流/出行类目不开放后台持续定位，本方案**不包含"关掉小程序仍上报"**；位置缺失不影响拜访提交。

## 12. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 客户表 48 家缺坐标、部分地址模糊 | 无法规划路线 | 第一期后台地图补标功能；模糊地址用搜索定位 |
| 销售表编码/格式每周可能变化 | 导入失败 | 容错解析 + 预览确认 + 未关联清单 |
| 商城名单客户无编号、无坐标 | 关联失败、无法上图 | 店名+手机号匹配兜底；导入自动地址解析补坐标，失败进待补标 |
| 路线规划 API 额度/费用 | 超量付费 | 免费额度足够（查询量 = 客户数×2）；云函数加缓存与频率限制 |
| 微信小程序类目/审核 | 上线受阻 | 企业主体；类目选"生活服务 > 商业服务"；提前准备隐私政策 |
| 业务员手机定位权限被拒 | 无法校验 | 引导开启定位；校验失败可降级为"待复核"记录，后台可审核 |
| 客户表"经度,纬度"与腾讯 API"lat,lng"顺序相反 | 路线全错 | 11.3 已明确拆解入库与调用转换，联调时抽样校验路线端点 |
| 订阅消息一次性授权次日失效 | 业务员收不到提醒 | 订阅按钮常驻首页；推送失败次日再提示订阅 |
| 业务员未开小程序/未授权定位 | 位置点缺失 | 仅前台采集+授权引导；位置缺失不影响拜访提交 |
| 云开发费用失控 | 成本 | 设置预算告警；照片/录音压缩上传 |

## 13. 上线前置清单（需要老板准备）

1. **微信小程序账号**：企业主体（营业执照）注册，AppID（**第一期开工前提**，创建项目即需要）；小程序名称建议"聚火拜访"（以注册时实际审核为准）；
2. **云开发环境**：小程序后台开通按量付费环境（**第一期开工前提**，开通免费、不产生费用）；
3. **订阅消息模板**：mp 后台"订阅消息"中选用"新任务提醒/待办提醒"类公共模板，拿到模板 ID（第一期新任务通知联调时需要）；
4. **腾讯位置服务 key**：lbs.qq.com 注册 → 创建 key → 勾选"微信小程序"并授权绑定小程序 AppID → 勾选 WebServiceAPI（路线规划走云函数代理，域名白名单需加 `servicewechat.com`）→ 企业认证后申请配额（免费额度不够时可购买调用量/并发量）；
5. **小程序权限与隐私配置**（第二期拍照/录音/定位前完成）：`app.json` 声明 `requiredPrivateInfos: ["getLocation"]`；小程序后台配置"用户隐私保护指引"（手机号/位置/相机/麦克风用途），隐私弹窗授权走 `wx.requirePrivacyAuthorize`；
6. **隐私政策文本**：说明收集手机号/位置/照片/录音的用途（可代写）；
7. **客户坐标补标**：上线后首周，把 48 家缺坐标客户在地图上补完（也可先放着，缺坐标的客户不进路线规划但可正常拜访记录）；
8. 真实销售表：每周五固定导出上传。

## 14. 需求确认清单（除 Q23 外全部已拍板）

1. ✅ 技术底座：微信云开发 + 微信原生小程序 + 独立 Web 管理后台 + 腾讯位置服务真实路线规划；小程序名"聚火拜访"；
2. ✅ 定位校验：开启，阈值后台可配（默认 500 米，可调可关）；客户缺坐标自动跳过校验；
3. ✅ 销售表：每周导入全量订单；比对窗口可配置（默认 30 天）；订单金额用"订单金额"列、不过滤订单状态、按下单时间判定；
4. ✅ 手机号：业务员在分配任务内可见；
5. ✅ 拜访结果枚举：极有意向 / 有意向 / 已下单 / 无需求 / 有抵触 / 联系不上 / 闭店·搬迁 / 其他（闭店·搬迁提交后客户标记待管理员停用）；
6. ✅ 录音：保留；**多段录制（第1段/第2段/第3段…，每段录完即固定，业务员不可删不可重录，仅管理员后台可删）**；单段默认限时 5 分钟（上限 10 分钟）；上传云存储永久保存，本地仅 24 小时断网缓冲；每日自动备份、后台每周五下载到本地；
7. ✅ 订阅消息："新任务通知"（管理员发送任务时推送）第一期实现；不做"今日待拜访"每日推送；
8. ✅ 任务选客户三种方式（列表筛选 / 导入名单文件 / 地图点选，可混用）+ 管理员可预规划路线后"发送给业务员"，业务员收到预规划路线且可自行按实时位置重规划；
9. ✅ 任务排期：管理员选完成天数（1~7 天），系统按路线/列表顺序自动分摊到每天，管理员可微调；业务员小程序按天查看当天计划；
10. ✅ 任务生命周期：到期未完成自动提醒管理员、可延期；已发送任务可撤回重发、可改派业务员（留痕）；支持复制任务；
11. ✅ 客户导入：兼容原始客户表与商城客户名单两种格式；追加/替换两种模式（替换时历史保留）；商城名单无坐标 → 自动地址解析补坐标，失败进待补标清单；编号前缀无业务含义（历史业务员代码），新客户可能无编号，关联按"编号→店名→手机号"逐级兜底；
12. ✅ 业务员端客户详情：一键拨号、**管理员备注**可见（后台填写/导入，业务员只读）、商城档案信息可见（添加商城时间/最近下单/最近浏览商城）、订单概况（第三期后）；位置报错可用当前定位上报新坐标，后台审核修正；
13. ✅ 拜访记录：时长自动记录（开始拜访→提交）；样品/物料单独字段；整条记录可离线提交、有网自动补传；拜访结果"已下单"仅记录（业务员协助客户在商城下单），小程序不做代客下单；
14. ✅ 报表口径（MVP）：主指标=回访后下单率；多业务员功劳归最近一次回访者；对照组与回访前后对比后续版本再做；
15. ✅ 人员管理：业务员可增删改停（微信自动绑定）；**预置唯一超级管理员「卿燕」，其余管理员由她动态增删；人员管理菜单仅超级管理员可见**；不做"N 天未回访"筛选（回访客户由你在商城筛好导入）；
16. ✅ 位置监控（Q23，A+B 组合）：业务员小程序前台每 5 分钟自动留点（当天轨迹可看）+ 管理员后台点名查询当前位置；不做后台持续定位（微信非物流类目不开放）；轨迹数据保留 90 天。
17. ✅ **客户类型双线（0.6）**：customerType = mall / new；新客户以表格导入为主（模板：店名/区域/地址/联系人/手机号可空/备注），管理员均可录入，后台也可手动新增（地图标点）；
18. ✅ **拜访结果按类型动态显示（0.6）**：新客户比商城客户多一项"已注册商城"（开发第一里程碑：先注册、后下单）；
19. ✅ **入商城认领（0.6）**：每周名单导入时对新客户做综合模糊对比（手机号精确 > 经纬度≤300米 > 店名相似 > 联系人/地址辅助），高分自动认领、中分待确认清单、低分忽略；手动标记与修正兜底；
20. ✅ **任务拜访目的（0.6）**：激活回访 / 新客开发；**按工作台收窄：回访工作台仅"激活回访"、新客开发工作台仅"新客开发"（唯一项不可选），全部总览才二选一**；报表按类型分组（新客户：开发转化率 + 首单率）；业务员排行加"开发客户数"。
21. ✅ **任务管理三层结构（0.6）**：任务列表（筛选 + 状态化操作）→ 任务详情（效果小结 / 天计划 / 地图叠加轨迹 / 本任务拜访记录）→ 新建/编辑向导（编辑预填）；进行中任务实时编辑、保存即同步业务员端并推送"任务已更新"；撤回回草稿可重发；已完成只读 + 复制。
22. ✅ **导出客户列表（0.6）**：客户管理一键导出 Excel，36 列详细分列留档（基础/区域坐标/商城档案/拜访统计/订单统计/其他），导出范围跟随当前筛选。
23. ✅ **双轨组织方式（0.6，双工作台入口）**：一套系统 + 登录后工作台选择页（🔥回访工作台 / 🌱新客开发工作台 / 全部总览）——业务员端无感分流（只认任务）；进入工作台后客户/任务列表、报表、导入默认类型全部跟随过滤，顶部可随时切换；**菜单随工作台改名：回访工作台="回访效果"，新客开发工作台与全部总览="拜访效果"，报表卡组同步显隐**；新客户入商城认领后自动换轨。
24. ✅ **双工作台差异矩阵（0.6 已甄别）**：两边差异远大于表面相似度——仪表盘（回访指标 vs 开发漏斗/添加成功率/下单率）、效果报表（回访口径 vs 开发口径+入商城明细）、客户管理（导入客户 vs 导入新客户、开发状态列、认领核对显隐）、任务管理（新建任务 vs 新建开发任务、目的默认、效果小结口径）、拜访结果（8 vs 9 项）、销售导入（+认领+首单统计）；全量差异已写入 16.8 矩阵，开发逐页对照。

## 15. 开工节奏（已定）

- **顺序**：第一期（基础平台+订阅消息）→ 第二期（地图与路径+多媒体+备份）→ 第三期（销售导入与回访效果）→ 第四期（上线打磨），按第 10 节验收标准逐期交付、逐期演示；
- **里程碑 1（第一期结束）**：管理员能导入客户（两种格式+追加/替换）、建任务（含 1~7 天排期）并发送；业务员能登录、看任务与每天计划、提交拜访记录（文字+结果+样品+时长+历史）；订阅后能收到"新任务通知"订阅消息；
- **开工前提（第一期必需）**：小程序 AppID（企业主体）+ 云开发环境开通（均为几分钟可完成、不花钱）；订阅消息模板 ID 第一期联调前提供；腾讯位置服务 key 第二期前给到即可；
- **当前可立即启动**：AppID 到位后即可开始第一期编码（登录、任务、拜访、订阅消息、后台基础模块均不依赖地图 key）。

---

## 16. 界面设计规范（聚火橙，已定稿）

> **视觉参照原型**：`design-demo/demo-miniapp.html`（小程序端，手机模拟器可交互）、`design-demo/demo-admin.html`（管理后台，桌面端可交互）。双击浏览器打开即可查看；**开发时以这两个文件为视觉基准**，老板已确认满意。

### 16.1 设计基调

- 定位：户外工具 + 管理驾驶舱。小程序端"高对比、大按钮、大字号"（阳光直射可读、单手可操作）；后台"白底商务风 + 橙色点缀"（数据优先、专业沉稳）；
- 品牌：聚火橙，全端统一，语义色永不换义。

### 16.2 色彩规范

| 名称 | 色值 | 用途 |
|---|---|---|
| 主色·火焰橙 | `#F5531C` | 主按钮、未拜访标记、选中态、品牌元素 |
| 主色·深 | `#D93E0E` | 按压态、渐变尾色 |
| 主色·浅 | `#FFEDE5` | 选中背景、标签底、沉睡客户高亮底 |
| 成功绿 | `#16A34A`（浅底 `#E7F6EC`） | 已拜访、成功、拨号按钮、通过 |
| 进行中蓝 | `#2F80ED`（浅底 `#E8F1FD`） | 当前拜访点、信息、编辑 |
| 警示黄 | `#F5A623`（浅底 `#FDF3E2`） | 过期、待处理 |
| 危险红 | `#E5484D`（浅底 `#FCEBEC`） | 错误、删除、有抵触 |
| 背景 / 卡片 / 分割线 | `#F6F7F9` / `#FFFFFF` / `#EEF0F3` | 页面底、卡片、线条 |
| 文字主 / 次 | `#1F2430` / `#66707A` | 标题正文 / 辅助说明 |
| 侧边栏深墨蓝 | `#1F2430` | 后台左侧导航底 |

**语义色规则（全端贯穿）**：橙=主操作/未完成，绿=已完成，蓝=当前，红=错误/删除，黄=提醒。任何页面不改变含义。

### 16.3 字体、尺寸与控件

- 字体：系统中文栈 `MiSans / HarmonyOS Sans SC / PingFang SC / 微软雅黑`，数字用等宽数字（tabular-nums）；
- 字号：小程序（户外偏大）页面标题 22 / 卡片标题 17 / 正文 15 / 辅助 12；后台 20 / 16 / 14 / 13；
- 圆角：按钮 12~14px，卡片 14~16px，标签全圆胶囊；
- 按钮：小程序通栏主按钮高 **52px**（触控友好）；后台按钮高 44px / 次级 38px；
- 阴影：卡片统一 `0 2px 10px rgba(31,36,48,.06)`；主按钮橙色投影 `0 8px 20px rgba(245,83,28,.32)`；
- 图标：线性圆润风格（stroke 1.8px），关键动作配 emoji 点缀（仅演示，正式版用统一图标库）。

### 16.4 小程序端页面要点（对应演示 5 屏）

1. **首页·任务**：橙色渐变大卡（今日计划 N 家 + 环形进度）；订阅横幅；任务卡片（进度条 橙→绿）；
2. **任务详情**：天页签（第1天~第N天横滑胶囊，当天橙、完成绿勾）；客户列表（编号序位块：橙=未拜访、绿勾=已拜访、蓝=当前）；底部固定"打开地图"大按钮；
3. **地图页**：全屏地图；标记点=橙底白数字（序号）/绿底白勾/蓝底脉动；路线=白衬 7px + 橙芯 4.5px 流动虚线；底部顺序卡（第 X/N 家、下一家名称、距离时长、导航/到达按钮），上滑展开完整顺序清单；
4. **客户详情**：大店名 + **类型状态标签**（商城客户：沉睡客户橙/观望黄；新客户：新客户蓝 → 已入商城绿 ✓）；地址、电话均**独占一行完整显示**（新客户无手机号时显示"暂无手机号"+拜访时补录）；**电话行右侧小方块按钮（约 36×36，红色图钉 📍 + "报错"两字，低调不抢眼）**，右边缘与店名行标签列对齐；绿色大圆钮一键拨号（有手机号时）；**档案区按类型**：商城客户=商城档案卡（最近下单超 90 天橙色高亮"沉睡客户"）；新客户=开发卡（未加入商城提示"引导注册商城/争取首单"，已入商城显示时间）；**管理员备注卡**（后台填写/导入，业务员只读）；拜访时间线卡片（结果彩色标签 + 照片缩略 + 波形录音条）；底部"开始拜访"大按钮；
5. **拜访记录**：顶部店名 + 深色计时器（自动走动）；拍照九宫格 + 虚线加号；**"点击开始/再点停止"录音条**（录音中变红 + 计时 + 进度填充，停止后生成固定段落并带 🔒 不可删）；拜访结果 8 个两行大标签单选；样品输入；文字多行；底部"提交拜访"→ 确认弹窗（距客户 X 米校验 ✓）→ 绿勾 + 撒花成功动画；
6. **我的**：头像、新任务提醒订阅开关（一次性订阅）、本月统计三格、设置。

### 16.5 后台页面要点（对应演示）

1. **布局**：左侧深墨蓝窄边栏（橙色选中高亮）+ 白色内容区；顶部页面标题/搜索/**当前工作台标签（点击可随时切换工作台）**/日期/通知；**登录后工作台选择页（0.6 双工作台）**：🔥 回访工作台（商城客户激活/追单/回访效果）/ 🌱 新客开发工作台（新客户拜访/注册引导/入商城认领/开发转化）/ 全部总览（混合视图），选择后客户列表、任务列表、报表、导入默认类型全部跟随该工作台过滤；**菜单分组**：工作台（仪表盘 / 任务管理 / 拜访记录 / 回访效果 / 客户管理 / 销售导入）+ 系统（人员管理 / 数据备份 / 系统设置）；
2. **仪表盘**：4 个 KPI 卡（数字滚动动画）+ 下单率趋势折线（逐笔绘制动画）+ 业务员任务进度条 + 今日动态 + 待办事项 + 今日轨迹小地图；
3. **任务管理（三步向导）**：① 基础信息（任务名/业务员选择卡/**拜访目的：激活回访·新客开发**/完成天数 1~7 胶囊/截止日）→ ② 选择客户（列表勾选 / 导入名单 / 地图点选三页签，右侧"已选客户"实时计数）→ ③ 排期预览（每天一栏可拖拽微调 + 汇总卡：总数/天数/里程/时长/业务员）→ 存草稿或"发送给业务员"；
4. **效果报表（随工作台切换，0.6）**：**回访工作台 = 菜单名"回访效果"**（顶部筛选 + 回访客户大数字卡：回访总数/回访后下单/下单率/带回金额 + 趋势 + 排行 + 明细）；**新客开发工作台 = 菜单名"拜访效果"**（突出新客户开发卡组：拜访新客户/已入商城/开发转化率/首单率，隐藏回访卡组）；**全部总览 = "拜访效果"** 两组都显示；顶部筛选条（客户类型/窗口/业务员/拜访结果/时间）+ 业务员排行（含开发客户数）+ 客户明细表（行尾"详情 ›"直达该客户拜访与订单；新客户含开发轨迹）；
5. **人员管理**：业务员 / 管理员双页签表格 + 新增弹窗 + 启停开关 + 删除；仅超级管理员（卿燕）可见；
6. **其他页**：客户管理、拜访记录、销售导入、数据备份、系统设置按开发计划实现，风格同上述。

### 16.6 动效规范

- 页面切换：300ms 滑入（cubic-bezier(.22,.8,.3,1)）；
- 关键动效：KPI 数字滚动 1.1s、折线逐笔绘制 1.6s、提交成功绿勾弹跳 + 撒花、地图当前点脉动圈、路线流动虚线；
- 全部动效 ≤300ms（大动效除外），低端安卓流畅优先，不做花哨无用动效。

### 16.7 布局防遮挡规范（重要，避免正式开发踩坑）

- **滚动容器规则**：flex 纵向布局中，可滚动区域必须同时设 `flex:1 + min-height:0 + overflow-y:auto`，否则内容溢出时滚动失效、底部内容不可达；
- **底部固定栏避让**：凡页面底部有固定元素（Tab 栏 66px / 底部提交按钮栏约 90px），滚动区必须留出 `padding-bottom = 固定栏高度 + 20~40px` 余量（本演示实测值：有提交栏的页面 130px、无提交栏有 Tab 的页面 96px、客户详情 110px）；**禁止用最后一个元素的 margin 代替容器 padding**（改内容时容易漏）；
- **管理后台**：页面容器底部 padding 64px；悬浮角标/提示一律 `pointer-events:none` 且放在侧边栏右侧，不遮挡表格操作区；
- **地图页专则**：底部悬浮卡片（拜访顺序卡）的 `bottom` 必须 ≥ 底部 Tab 栏高度 + 间距（本演示实测 88px）；上滑展开的弹出层 `z-index` 必须高于 Tab 栏（本演示 60 > 55），打开时完整遮住底栏；
- **验收动作**：每个新页面做完，手动把内容填满一屏半以上，滚动到底部确认最后一项完整可见、可点。
- **样式残留自查**：改版时删旧样式必须先删干净（本项目已踩过：旧 `.posfix` 规则残留覆盖新规则，右侧距差 22px 导致标签列错位）；开发时每次改动后确认同名选择器只有一处定义，避免重复定义互相覆盖。

### 16.8 双工作台页面差异矩阵（0.6 重要，开发时逐页对照）

> 老板已明确：新客户开发与回访激活的业务差异**远大于**表面相似度，必须按两条业务线分别设计页面逻辑，仅底层机制共用。以下矩阵为权威口径。

| 页面 / 元素 | 🔥 回访工作台 | 🌱 新客开发工作台 |
|---|---|---|
| 仪表盘 KPI | 今日拜访 / 本周完成率 / 回访后下单率 / 待处理 | 今日新客拜访 / 本月拜访新客户 / **添加商城成功率**（入商城÷拜访）/ **新客下单率**（首单÷入商城） |
| 仪表盘图表 | 回访下单率趋势 + 业务员任务进度 | **开发漏斗**（拜访→入商城→首单三级漏斗）+ 业务员开发排行（入商城数/首单数） |
| 待办区 | 待复核 / 待补标 / 纠错申请 | **认领核对 / 待补录手机号 / 待补标 / 待复核** |
| 动态区 | 拜访提交动态 | 注册商城 / 认领成功 / 补录联系方式动态 |
| 效果菜单 | 菜单名"回访效果"，页头同步 | 菜单名"拜访效果"，页头同步 |
| 效果报表 | 回访总数 / 回访后下单数 / 下单率 / 带回金额 + 客户级明细（拜访+订单） | 拜访新客户 / 已入商城 / 开发转化率 / 首单率 + **新客户开发明细**（最近拜访 / 结果 / 入商城时间 / 入商城方式 / 首单时间金额） |
| 客户管理 | 按钮"**导入回访客户**"；商城档案列；沉睡客户/观望标签 | 按钮"**导入新客户**"；**开发状态**列（待开发/已入商城）；认领核对卡显示（回访工作台隐藏） |
| 任务管理 | 按钮"新建任务"；**拜访目的仅"激活回访"一项**；任务详情效果小结=拜访→回访后下单→带回金额 | 按钮"**新建开发任务**"；**拜访目的仅"新客开发"一项**；任务详情效果小结=拜访→入商城→首单（含添加成功率/下单率） |
| 拜访结果集 | 8 项 | 9 项（+已注册商城） |
| 销售导入 | 订单关联 + 手机号回填 | + **新客户认领入商城（综合比对）+ 新客首单/下单率统计**，说明文案同步 |
| 两工作台通用 | 客户档案底层、地图/路线/标记、拜访记录机制（拍照/录音/时长/定位校验）、位置上报、人员管理、备份、系统设置 | 同左（业务员端对所有类型无感，只认任务） |

### 16.9 附：双工作台数据联动

- 每周导入商城名单 → 自动认领比对（综合分三档）→ 新客户标记已入商城（时间+方式）→ 该客户**自动换轨**：从新客开发工作台转到回访工作台（后续按商城客户口径统计）；
- 仪表盘"添加成功率/下单率"随每次名单导入与销售导入自动更新；
- 各工作台页头副标题随工作台切换（如"新客开发工作台 · 拜访 / 入商城 / 首单转化"）。

---

## 12. 实现进展备注（2026-09-04）

- **任务操作定稿**：编辑/延期/改派/撤回/发送/删除已全部实现；「复制」「结束」经老板决定**已撤除**（前端与 adminapi 接口全清）。
- **任务详情状态卡**：顶部信息卡+KPI 同 4 列 grid，右侧状态卡（草稿/已发放/进行中/审核中[含同意拒绝按钮]/已过期/已完成[附完成日期]），与 KPI 卡同尺寸。
- **任务结束审核**：设置项「任务结束自动审核通过」（key=autoApproveFinish）——勾选=全部拜访完自动结束；不勾选=人工审核；提前交始终人工审核。
- **后台通知体系（A+B 定稿）**：全局 15 秒轮询（指纹增量渲染，无变化不碰 DOM）→ 新审核/新过期：响铃 → 1.6 秒后云希男声播报（edge-tts 免费接口，每人一份 mp3 缓存+失败重试）→ 消息浮窗；页面不可见补 Windows 系统通知。Web Push（关标签页也能收）经老板决定**已撤除**。
- **手机端审核观察员**：有审核中任务才 15 秒轮询（reviewStatus），审批结果自动刷新+提示，无审核中零轮询。
- **本环境 wx.showModal 原生弹窗失败**：小程序全部提示改用 wx.showToast/页内弹层。
- 详细改动清单与待验证队列见《项目交接-当前进度.md》§3.3/§3.4。

---

*本文档随讨论推进持续更新；开发按第一期验收标准为第一个里程碑。*
