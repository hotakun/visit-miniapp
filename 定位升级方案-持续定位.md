# 定位升级方案 · 持续定位三接口（2026-09-08 需求对齐定稿）

> 状态：**需求已对齐，方案定稿，待分期开工**。入口指针见《项目交接-当前进度.md》§3.3。
> 已获批接口：`wx.startLocationUpdate` / `wx.onLocationChange` / `wx.startLocationUpdateBackground`

## 1. 需求全景（老板最终需求，2026-09-08 对话确认）

老板的核心诉求 = **全部五项**，典型使用画面 = **实时可见 + 事后可查**：

| # | 需求 | 业务场景 |
|---|---|---|
| ① | 后台实时位置/轨迹监督 | 业务员出去拜访，老板在后台地图上随时知道每个人在哪、是否拜访中 |
| ② | 拜访证据强化 | 每次拜访自动留轨迹证据：到店时间、店内停留时长、行动路径（防假拜访） |
| ③ | 手机端体验 | 距离实时更新、提交定位秒出、报错收敛提速、重排免等待 |
| ④ | 防作弊/覆盖分析 | 轨迹速度异常点过滤；扫街覆盖区域分析（哪里跑过、哪里空白） |
| ⑤ | 轨迹云端可回看 | 拜访轨迹上传云端保存，后台按天/按拜访回放路线图 |

## 2. 已拍板口径（全部经老板确认）

| 项 | 口径 |
|---|---|
| 采集密度 | **工作时间：拜访中 5 秒/点、非拜访 30 秒/点；非工作时段独立四档**（见 §2.1） |
| 上传策略 | 端上缓存，按档位**发包上传**（工作档 60 秒/批；非工作档跟随档位，绝不空转） |
| 云端保留 | **系统设置页新增设置项，手动输入天数，默认 30 天**；到期自动清理 |
| 实时看板 | **后台新增「位置监控」页面，全业务员**（当前位置 + 今天轨迹） |
| 看板刷新 | 15 秒（与现有全局轮询一致） |
| 后台定位 | **仅拜访中开启**（老板前次拍板）；首次授权弹引导说明"为记录拜访轨迹"，**拒绝不影响基本功能** |
| 拜访时长上限 | **30 分钟 / 1 小时（默认）/ 2 小时三档**；到点前 5 分钟提醒（页内+服务号）；到点双态自动处理：**已选结果→超时自动提交（跳过距离校验、无照片录音）；未选→自动取消**；均写任务流程档案（并入 M1） |
| M1 交付补充 | **最新位置上传随 M1 交付**（覆盖写，M1 完成后台即可见实时位置点）；**移动阈值：工作时段正常写；非工作时段位置变化超 10 米才写**（静止不写省写量）；mine 页加「工作时段记录位置轨迹，仅管理员可见」 |
| 实施节奏 | M1→M2→M3 分期（老板前次拍板）；**M1 含拜访时长上限** |

## 2.1 时间分层口径（2026-09-08 老板追加定稿）

把一天分成两段，非工作时段降频，省电省写量：

| 时段 | 场景 | 抽样间隔 |
|---|---|---|
| **工作时间**（默认 7:00~20:00，可配） | 拜访中 | 5 秒/点 |
|  | 非拜访 | 30 秒/点 |
| **非工作时段**（默认 20:00~次日 7:00，可配） | 独立四档（拜访中/平时同时设定） | 见下表 |

**非工作时段四档**（后台设置下拉选择，默认 10S/60S）：

| 档位 | 拜访中抽样 | 平时抽样 | 上传发包 |
|---|---|---|---|
| 5S/30S（不降频） | 5 秒 | 30 秒 | 60 秒/批 |
| **10S/60S（默认）** | 10 秒 | 60 秒 | 60 秒/批 |
| 20S/120S | 20 秒 | 120 秒 | 120 秒/批 |
| 30S/180S | 30 秒 | 180 秒 | 180 秒/批 |

配套规则：
- **上传跟随档位**：发包间隔 = 档位平时间隔；攒够至少一个点才发，绝不空转发包
- **防抖**：跨工作/非工作边界（如 19:55~20:05）不来回切模式，切换后保持至少 10 分钟
- **后台看板下班态**：非工作时段位置点超 2× 档位间隔无更新 → 该业务员点变灰显示"已下班/静止（X 分钟前）"，避免误判失联

**后台设置页新增项（共 6 个）**：
1. 工作开始时间（小时下拉 0~23，默认 7:00）
2. 工作结束时间（小时下拉 0~23，默认 20:00）
3. 非工作时段档位（4 档下拉，默认 10S/60S）
4. 轨迹保留天数（手动输入，默认 30 天）

（另有既有项：定位校验阈值、正常/重要页刷新档位——不动）

## 3. 三接口能力与用途

| 接口 | 用途 |
|---|---|
| `wx.startLocationUpdate` | 前台持续定位（全局，进入定位相关页面即开） |
| `wx.onLocationChange` | 位置流唯一入口（lat/lng/accuracy/speed），喂给定位总线 |
| `wx.startLocationUpdateBackground` | 仅"拜访中"期间开启：切后台轨迹不断，补店内停留证据 |

权限：前台 `scope.userLocation`；后台 `scope.userLocationBackground`（首次弹系统授权+页面引导文案；拒绝则仅前台流）。`app.json` 加 `requiredBackgroundModes: ["location"]`（提审字段）。

## 4. 核心架构

### 4.1 端上：定位总线 locBus（`utils/loc.js` v2）

```
startLocationUpdate(前台) + startLocationUpdateBackground(仅拜访中)
          │
          ▼
  wx.onLocationChange 位置流
          │
          ▼
┌─────────────────────────────┐
│ 定位总线 locBus（模块单例）    │
│ · latest：最新点              │
│ · queue：最近 N 点（t/acc/speed）│
│ · collector：按口径抽点（拜访5s/平时30s）│
│ · uploader：60 秒批量上传云端    │
└─────────────┬───────────────┘
  页面订阅（档位节流：30/45/60 与 8/12/15/20 不变）
```

- **降级铁律**：流不可用（模拟器/权限拒绝/老基础库）→ 唯一路径回落现有 `getOne` 轮询；`calm` 算法口径全保留（1/acc² 加权、滑动窗口、target/maxValid），只换取点来源
- **预期管理（挑毛病 1 定稿）**：轨迹尽力记录、允许空白段（小程序被杀/划掉 = 0 点）；后台看板按"最后上报时间"如实标注，空白不画线不脑补
- **采集节奏**：位置流事件可能很密，collector 按**时间分层口径（§2.1）**抽点——工作时段（拜访中 5s/平时 30s）+ 非工作时段四档（默认 10S/60S），跨段防抖 10 分钟
- **上传**：按档位发包（工作档 60 秒/批；非工作档跟随平时间隔），网络失败本地缓存，下次补传

### 4.2 云端：轨迹存储（复用 `salesman_locations` 空集合）

| 文档类型 | 结构 | 用途 |
|---|---|---|
| 轨迹片段 | `{ salesmanId, day, pts:[{lat,lng,acc,speed,t}...], startedAt }`（每 1 分钟 1 条） | 当天轨迹回放、拜访证据、覆盖分析 |
| 最新位置 | 每人一条 `{ salesmanId, lat, lng, acc, t, visitOngoing }`（覆盖写） | 实时看板 15 秒轮询 |
| 拜访关联 | `visits` 记录加 `trackSegIds`（该拜访时间段的片段 id） | 拜访详情「📍 查看轨迹」直接回放 |

- 免费环境成本估算：5 人 × 8 小时 ≈ 拜访中 5s(按 4h) 2880 点 + 平时 30s(按 4h) 480 点 ≈ 每人 3360 点/天 → 每分钟 1 条片段 ≈ 480 条/人/天，写入量安全
- **保留清理**：settings 新增 `trackKeepDays`（手动输入天数，默认 30）；`autoArchiveExpired` 每日附带清理过期轨迹片段与最新位置

### 4.3 后台：位置监控新页面

- 侧边栏新增「🗺 位置监控」（任务管理下）
- 页面：腾讯地图（复用后台 TMap 能力）显示全业务员当前点（名字+最后更新时间+拜访中蓝点标识）；点人 → 当天轨迹绿线 + 起止时间
- 拜访详情弹窗加「📍 查看轨迹」→ 回放该次拜访路线（关联 trackSegIds）
- 覆盖分析（M3）：按天画扫街覆盖热力/空白区
- 接口：adminapi 新增 `listLatestLocations` / `getDayTrack` / `getVisitTrack`

## 5. 各页面端上升级（同前版，保留）
| 页面 | 升级 |
|---|---|
| map | 实时距离（档位节流）；重排取 `latest()` 免等待 |
| visit | 进页订阅流；提交取最近 5 个有效点加权质心（秒出，2 秒兜底）；拜访中开后台定位；校准收敛吃流点 |
| customer | 报错高精度收敛吃流点（target=8/半径 10m 口径不变），更快稳定 |
| task | 距离排序用 `latest()` |

## 5.5 拜访时长上限（2026-09-08 老板追加，并入 M1 一起做）

### 口径
- 后台设置页新增「拜访时长上限」：**30 分钟 / 1 小时（默认）/ 2 小时**，存 settings `visitDurationLimit`，tasks detail 下发手机端（照 recordingDurationLimit 通道做）
- **拜访中判定**：以「开始拜访」按钮为准（本地全局状态标记），点了开始才算拜访中
- 到点前 **5 分钟提醒**；到点**自动取消拜访**（沿用"取消=删除记录零痕迹"口径）+ 任务流程档案留痕

### 提醒机制（两层，覆盖人在页内/切走两种场景）
1. **页内**：计时胶囊到点前 5 分钟变红 + 顶部提示条「⏰ 拜访将在 5 分钟后自动取消，请尽快提交」+ `wx.vibrateShort` 震动（纯前端本地定时器，零云端成本）
2. **服务号**：云端定时触发检查到"还剩 5 分钟"→ 给该业务员发服务号模板消息「您的拜访「XX店」将在 5 分钟后超时，请及时提交」；每人只提醒一次（标记防重复）

### 自动处理（到点双态，老板定稿：选结果即上报草稿，超时提交不带照片录音）
- **草稿上报**：业务员在拜访页**点选拜访结果/填备注时**，前端立即 `saveDraft` 把草稿（result/text/samples）写到该"拜访中"记录的 draft 字段（轻量无感）；照片/录音仍是提交时才上传——**超时自动提交不带照片录音**（已知取舍，老板认可）
- **到点判定**（云端定时触发执行，不依赖手机在线）：
  - **已选拜访结果**（ongoing 带 draft.result）→ **自动提交**：ongoing 升级为完成记录（结果/备注/样品落库、时长=上限、**跳过定位距离校验**）；档案留痕 `visitAutoSubmit`：`⏱ 超时自动提交 · 结果：已下单（系统自动）`；服务号告知"已自动处理异常提交"
  - **未选结果** → **自动取消**：删除记录零痕迹（沿用取消口径）；档案留痕 `visitAutoCancel`：`⏱ 拜访「XX店」超时自动取消（上限 1 小时）（系统自动）`；服务号告知"已取消"
- **幂等双保险**：手机端若到点时在线，本地先执行同规则；云端触发器随后发现记录已不在/已提交则跳过，绝不重复处理
- 后台 `renderArchive` 加 `visitAutoSubmit`/`visitAutoCancel` 图标（⏱）与文案

### 省资源方案（老板拍板：动态闹钟 + 10 分钟触发器）
- **不盲扫**：`visits start` 时算好两个时刻写进记录——`remindAt = 开始 + 上限 - 5 分钟`、`autoCancelAt = 开始 + 上限`
- 定时触发器**每 10 分钟跑一次**（144 次/天，比每分钟盲扫省 90%），只查"提醒时刻/取消时刻已到"的记录
- 无拜访中记录 → 触发器空转成本极低；免费额度无压力

### 改动清单
| 层 | 改动 |
|---|---|
| 后台 | 设置页「拜访时长上限」下拉（setSetting 沿用）；档案 visitAutoCancel 文案 |
| 手机端 | visit 页倒计时红条+震动+本地到点取消；tasks detail 下发上限 |
| 云端 | visits start 存 remindAt/autoCancelAt；定时触发器（10 分钟）查并执行提醒/取消+logs+服务号 |

## 6. 分期实施

### M1 端上定位总线 + 地图页实时距离 + 拜访时长上限 + 最新位置上传（风险低）
- 改：`utils/loc.js`（总线/降级/collector 抽点/**uploader：最新位置覆盖写**）、`pages/map/map.js`（订阅+档位节流+重排用 latest）、**拜访时长上限全套**（§5.5：后台设置档位、visit 页倒计时提醒+震动、visits start 存 remindAt/autoCancelAt、10 分钟定时触发器、服务号提醒与取消通知、档案 visitAutoCancel 文案）、`pages/mine/mine.js`（位置记录说明文案）
- 验收：①距离随走动实时变（档位节流）；②模拟器降级 getOne 与原行为一致；③采集点按 5s/30s 口径进缓存；④**最新位置每 60 秒覆盖写一次，后台可查**；⑤拜访 5 分钟前页内红条+服务号提醒各一次；⑥到点自动取消+档案留痕；⑦mine 页显示位置记录说明；⑧语法检查全过
- 注：M1 不改 app.json；轨迹片段上传（M2）代码预埋开关；最新位置移动阈值：**工作时段正常写、非工作时段超 10 米才写**（挑毛病 2 定稿）

### M2 云端存储 + 位置监控页 + 拜访证据（核心价值）
- 改：`utils/loc.js`（uploader 60s 批量）、新云函数接口（adminapi：listLatestLocations/getDayTrack/getVisitTrack + visits 关联 trackSegIds + 采集写入）、`admin.html`（位置监控页 + 拜访详情轨迹按钮 + 设置页保留天数）、`app.json`（requiredBackgroundModes）
- 验收：①后台地图看到全业务员实时位置（15s）；②点人看当天轨迹；③拜访提交后详情可回放轨迹+店内停留时长；④拜访中切后台轨迹不断；⑤拒绝权限路径正常；⑥iOS+安卓真机

### M3 防漂移 + 覆盖分析 + 清理
- ✅ 防漂移（2026-09-08 老板拍板开工，v0.9.03）：手机端 sampleTrack + 云端 reportTrack 双层过滤——精度>150m 丢弃；与前一保留点速度>30m/s（108km/h）判漂移剔除；片段过滤后<2 点整体丢弃
- ⬜ 覆盖分析：按天画扫街覆盖热力/空白区（待老板验收 M2 真机后定形态）
- ✅ 清理：cleanExpiredTracks 已随 M2 落地（trackKeepDays 默认 30 天）
- 改：`pages/visit/visit.js`（速度异常点过滤规则）、adminapi（轨迹清理任务 + 覆盖统计）、位置监控页（覆盖热力，可选）
- 验收：漂移点被过滤；过期轨迹按设置天数自动清理；覆盖分析可用

## 7. 风险与坑

1. 微信推送频率不可控 → 一切"实时"以档位节流与采集口径为准
2. 模拟器不支持持续定位 → 流效果必须真机验证；UI/降级可模拟器验
3. 后台权限只弹一次 → 拒绝后走 `wx.openSetting` 引导
4. 耗电：持续定位比轮询省；后台定位仅拜访中，且提交/取消后严格 stop
5. 轨迹写库频率：绝不逐点写，必须 60 秒批量片段（坑 29 精神）
6. `requiredBackgroundModes` 需进提审配置；审核说明用途"业务员巡检轨迹记录"

## 8. 真机验证清单（M2 完成后）

- [ ] 地图页距离随步行实时变化（档位节流）
- [ ] 模拟器降级 getOne 路径正常
- [ ] 提交拜访定位秒出 + 阈值拦截同口径
- [ ] 拜访中切后台 2 分钟 → 轨迹含后台时段 → 后台回放可见
- [ ] 取消拜访 → 后台定位关闭
- [ ] 拒绝后台权限 → 前台功能全正常
- [ ] 后台位置监控页：全业务员位置、当天轨迹、拜访中标识
- [ ] 拜访详情「查看轨迹」回放正确 + 停留时长合理
- [ ] 保留天数设置生效（改 7 天 → 到期清理）
- [ ] **拜访时长上限：5 分钟前页内+服务号提醒各一次；到点自动取消+档案留痕+服务号告知**
- [ ] iOS + 安卓各一遍

## 9. 不改动项（铁律清单）

- 收敛算法口径、双档位数值、定位校验阈值、坐标存储与显示
- 照片/录音/单开/取消等一切与定位无关的口径
- 老业务员的 getOne 降级路径行为

## 10. 挑毛病六问（2026-09-08 老板逐条答复，已定稿）

1. **轨迹预期管理**：采纳——轨迹尽力记录、允许空白段（小程序被杀/划掉=0 点）；后台看板按"最后上报时间"如实标注，空白不画线不脑补
2. **最新位置移动阈值**：定稿——**工作时段正常写；非工作时段位置变化超 10 米才写**（静止不写省写量）
3. **轨迹叠加客户点+距店最近 X 米**：不做
4. **拜访中判定口径**：不额外管（拜访时长上限已覆盖该场景）
5. **mine 页位置记录说明**：采纳，文案 = 「工作时段记录位置轨迹，仅管理员可见」（去掉"为拜访监管"）
6. **M1 顺带最新位置上传**：同意——M1 即上传最新位置（覆盖写），M1 完成后后台即可看到实时位置点
