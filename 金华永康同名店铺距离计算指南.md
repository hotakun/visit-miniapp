# 金华-永康同名店铺距离计算指南

## 任务要求
处理文件：`金华-永康-同名待人工确认.csv`
- 比较E列（经纬度(B)）和J列（经纬度(A)）
- 如果两地距离超过500米，在最后一列（L列）填入"两家"
- 如果距离≤500米或缺少经纬度数据，留空不填

## 文件结构分析

| 列索引 | 列名 | 示例数据 |
|--------|------|----------|
| A | B行号 | 6 |
| B | 商户名(B) | 永康陈氏肉麦饼 |
| C | 电话(B) | 18267943453 |
| D | 地址(B) | 永康市城东路530号 |
| **E** | **经纬度(B)** | **"120.050197,28.899617"** |
| F | 未剔除原因 | 店名相似但地址/位置不符 |
| G | 客户名称(A) | c1180 肉麦饼 |
| H | 手机(A) | 13967467897 |
| I | 地址(A) | 胡公路与胡库街交叉口正西方向77米左右 |
| **J** | **经纬度(A)** | **"120.18265,28.961603"** |
| K | 店名相似度 | 0.9 |
| **L** | **判断结果列** | （需要填写） |

## 方法一：使用Excel公式（推荐）

### 步骤1：分列提取经纬度
1. 在Excel中打开CSV文件
2. 选中E列（经纬度(B)）
3. 点击"数据" → "分列"
4. 选择"分隔符号" → 逗号 → 完成
5. 经纬度会被分成两列：E列（经度）、F列（纬度）
6. 对J列进行同样操作：分成J列（经度）、K列（纬度）

### 步骤2：计算距离公式
在L列（最后一列）输入以下公式（假设分列后经纬度在各自位置）：

```excel
=IF(AND(E2<>"",J2<>""), 
    IF(SQRT(((J2-E2)*111000)^2 + ((K2-F2)*111000)^2) > 500, 
    "两家", 
    ""), 
    "")
```

**公式说明：**
- `(J2-E2)*111000`：经度差转换为米（1度经度约111km，随纬度变化）
- `(K2-F2)*111000`：纬度差转换为米
- `SQRT(x^2 + y^2)`：计算直线距离（勾股定理）
- `>500`：判断是否大于500米
- 结果：如果>500米显示"两家"，否则留空

### 步骤3：填充公式
1. 在L2单元格输入上述公式
2. 双击L2单元格右下角的小方块，或向下拖动填充所有行

## 方法二：手工计算示例

以第1行数据为例：
- E列：`"120.050197,28.899617"`
- J列：`"120.18265,28.961603"`

**计算过程：**
1. 提取经纬度：
   - 左边：经度1 = 120.050197，纬度1 = 28.899617
   - 右边：经度2 = 120.18265，纬度2 = 28.961603

2. 计算经纬度差：
   - 经度差 = 120.18265 - 120.050197 = 0.132453
   - 纬度差 = 28.961603 - 28.899617 = 0.061986

3. 转换为距离（近似计算）：
   - 经度距离 = 0.132453 × 111000 ≈ 14,702米
   - 纬度距离 = 0.061986 × 111000 ≈ 6,880米

4. 计算直线距离（勾股定理）：
   - 距离 = √(14702² + 6880²) = √(216,149,604 + 47,334,400) = √263,484,004 ≈ 16,233米

5. 判断：16,233米 > 500米，L列填入"两家"

## 方法三：在线工具批量计算

如果数据量大，可以使用在线批量计算工具：

1. 准备数据格式：
   ```
   经度1,纬度1,经度2,纬度2
   120.050197,28.899617,120.18265,28.961603
   120.052549,28.894892,120.143105,28.957912
   ```

2. 上传到在线距离计算网站
3. 下载结果后复制到Excel

## 快速判断法

对于永康市范围内的快速估算：
- 1度经度 ≈ 111km
- 1度纬度 ≈ 111km
- 0.001度 ≈ 111米

**快速判断法：**
如果经纬度差绝对值之和 > 0.0045度，则距离很可能超过500米
```excel
=IF(AND(E2<>"",J2<>""), 
    IF(ABS(J2-E2)+ABS(K2-F2) > 0.0045, 
    "两家", 
    ""), 
    "")
```

## 结果验证

处理后应呈现如下效果：

| 行号 | 店铺A | 店铺B | 计算距离(米) | L列结果 |
|------|-------|-------|-------------|---------|
| 6 | 永康陈氏肉麦饼 | c1180 肉麦饼 | 约16,233 | 两家 |
| 15 | 江西特色小炒(金莺路店) | c459 江西特色小炒 | 约10,500 | 两家 |
| 64 | 江西实惠小炒(东塔路店) | c863 江西实惠小炒 | 约1,500 | 两家 |

## 注意事项

1. **坐标格式**：确保经纬度是数值格式，不是文本
2. **单位**：Excel计算的是近似距离，足够500米判断
3. **空值处理**：公式已包含空值检查
4. **精度**：对于500米阈值，Excel公式精度足够

## 结果检查

完成后检查：
1. 所有有完整经纬度数据且距离>500米的行，L列应有"两家"
2. 距离≤500米的行，L列应为空
3. 缺少经纬度的行，L列应为空
4. 总行数应与原始文件一致

## 保存结果

1. 在Excel中处理完成后
2. 另存为CSV格式
3. 建议文件名：`金华-永康-同名待人工确认_已判断距离.csv`

---

**提示**：从已有数据看，大多数同名店铺距离都超过500米，因为它们分布在永康市不同乡镇（东城、古山、芝英等），所以"两家"的标记会比较多。