# 永康市Excel文件按regionName分组处理指南

## 概述
此文档指导您如何将 `金华-永康.xlsx` 文件按 `regionName` 列分割为多个独立的Excel文件。

## 文件准备
1. **源文件位置**: `D:\WFR\visit-miniapp\TMP\金华-永康.xlsx`
2. **输出目录**: `D:\WFR\visit-miniapp\TMP\永康市`

## Python脚本解决方案

### 方案一：使用命令行运行（推荐）
1. 打开 **命令提示符 (CMD)** 或 **PowerShell**
2. 切换到工作目录：
   ```cmd
   cd /d D:\WFR\visit-miniapp
   ```
3. 运行处理脚本：
   ```cmd
   python 永康市处理_最后尝试.py
   ```

### 方案二：如果缺少Python库
1. 安装所需库：
   ```cmd
   pip install pandas openpyxl
   ```
2. 再次运行脚本

## 脚本功能说明
### 执行步骤
1. **读取文件**: 打开 `金华-永康.xlsx`
2. **查找区域列**: 优先查找 `regionName` 列，如果没有则查找相似列
3. **分析数据**: 统计不同区域的数量和记录分布
4. **分组保存**: 将每个区域的记录保存为独立的Excel文件
5. **生成统计**: 创建 `永康市_分组统计.txt` 报告文件

### 文件名处理
- 自动替换非法字符：`/ \ : * ? " < > |`
- 空区域名会保存为 `无区域信息.xlsx`
- 长文件名会被截断（最多100字符）

## 预期输出
- **目录结构**:
  ```
  D:\WFR\visit-miniapp\TMP\永康市\
    ├── 区域1名称.xlsx
    ├── 区域2名称.xlsx
    ├── ...
    └── 永康市_分组统计.txt
  ```
- **统计报告**: 包含各区域记录数、文件名等信息

## 错误处理
### 常见问题及解决方案
1. **文件不存在**: 请检查路径是否正确
2. **缺少库**: 运行 `pip install pandas openpyxl`
3. **内存不足**: Excel文件较大（2.7MB），应该能够正常处理
4. **权限问题**: 确保有文件读写权限

### Python环境检查
```python
# 在Python环境中运行以下代码检查环境
import pandas as pd
import sys
print(f"Python版本: {sys.version}")
print(f"pandas版本: {pd.__version__}")
print(f"系统路径: {sys.path[0]}")
```

## 手动处理（备选方案）
如果Python脚本无法运行，您可以：
1. 使用Excel打开文件
2. 选择 `regionName` 列
3. 按该列筛选或排序
4. 手动复制每个区域的数据到新工作簿
5. 分别保存为不同的Excel文件

## 联系我们
如果遇到任何问题，请提供：
1. Python错误信息
2. Excel文件截图
3. 您的Python环境信息

---

**注意**: 本脚本基于之前成功处理的杭州数据模型，应该能够正确处理永康市数据。脚本会自动检测并处理各种边界情况。