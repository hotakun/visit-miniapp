# 部署上线清单（聚火拜访）

> 依据：《项目交接-当前进度.md》§0 快照（2026-09-09 深夜）+ §3.3 待部署队列 + git 历史（截至 `d81968c`，2026-09-10）。
> 云端环境 `cloud1-d0gwlmbwp31181eb5`；后台固定 `http://localhost:8581`（bat 已写死 PORT）。
> 逐项打勾用 `[x]`。**部署顺序=一节 → 二节 → 三节 → 四节 → 五节**（云函数先、前端后，否则前端调不到新接口）。

## 一、云函数重传（开发者工具 → 逐个右键「上传并部署：云端安装依赖」）

| # | 云函数 | 必传理由 | 依据 |
|---|---|---|---|
| 1 | `init` | COLLECTIONS 补 `registrations` / `customer_batches` / `batch_members`（新集合自愈兜底） | 71dcb03 |
| 2 | `adminapi` | ① **ACTIONS 白名单补 `listRegistrations`/`reviewRegistration`/`setUserBoss`**——不传则注册审核列表、通过/拒绝、启用老板**直接 BAD_ACTION 失效**；② `listCustomerBatches` 返回 `summary`（客户级去重四数字）；③ `setMpOpenid` 放开管理员（新人注册申请推送到老板微信）；④ 累积项：导入超时修复、比对认领本地化（`listMallLibrary`+`applyMallMatch`）、删批次并行、批次 CRUD、坐标审核、`autoArchiveExpired` | d81968c、5c917d4、§3.3 |
| 3 | `login` | 注册→后台审核→免登进入；老板判定 `users.boss===true` + 返回 `canBoss`；下发 `welcomeConfig`；openid 双占用修复（正式员工优先）；老板注册 `users/_` 崩溃修复；实习绑定入口移除 | d81968c、ed66d99、§7.14 |
| 4 | `tasks` | boss 全量查询 + 虚拟 `finish`/`replanDay` + `bossBoard`/`bossWar`（实习排除）；`list` 过滤 `archivedAt`；`detail` 返回 `locCheck`/`coordFixPending`/`taskNo`/`recordingDurationLimit`/`logs` | 66c63da、§3.8 |
| 5 | `visits` | boss 虚拟 `start`/`submit`/`cancel`/`saveDraft` + 定位丢弃；`submit` 收 `photos≤3{fileID,thumbID}`+`audio`；`cancel` 直接删除零痕迹；`reportTrack` 防漂移 | ea936f1、§3.8 |
| 6 | `coordfix` | boss 模拟提交；`note`+`photos≤3`（兼容旧 string） | ea936f1、§3.8 |
| — | `subscribe` / `bindadmin` / `seed` / `ping` | 自首次提交（dc16ee3）未改动，**可不传** | git |

- [ ] init　- [ ] adminapi　- [ ] login　- [ ] tasks　- [ ] visits　- [ ] coordfix

## 二、云端一次性操作

- [ ] **初始归档**（若未做过）：后台 → 客户管理 → 批次列表空态 → 「🏗 执行初始归档」（现有客户归入「2026年9月1日第一批导入」）
- [ ] **定时触发器**（若未建）：控制台手动创建 cron `0 */10 * * * * *`（拜访超时双态提交/取消 + 自动归档）
- [ ] 系统设置核对：定位校验阈值（默认 100 米，0/不勾选=关）、录音上限（180/300/600，iOS 封顶 5 分钟）、过期归档天数（2/3/5，默认 3）、任务编号区域码（JH05）、拜访结束自动审核、服务号通知开关与模板字段、欢迎仪式配置

## 三、后台（老板本机 qingyan）

- [ ] **重启壳程序**——壳已禁 F5/Ctrl+R，改过 `admin.html`/`nt-map.js`/`server.js` **必须重启**才加载新代码；别开两个 server 实例（token 互踢）
- [ ] 确认 `nt-map.js?v=0924`（改 js 必须 bump 版本号防缓存）
- [ ] 文员机分发更新：重打文员包，或 `tools/upload_dist.py` 上传分发版（**老板明确说"上传"才执行，绝不自动**；本次发布需 bump APP_VERSION，云端当前分发 v0.9.04）
- [ ] 后台自检：
  - 任务管理大卡（当前任务/历史任务/任务地图三段页签、地图图例开关、⭐仓库开关默认关）
  - 客户管理（批次卡两栏、工具卡 `summary` 四数字、批次详情固定 20 条翻页 + 中央搜索 + 铅笔改名）
  - 人员管理（业务员/管理员/⏳待审核三 Tab、红点、👑 老板列与启用按钮、服务号绑定列与解绑）
  - 设置页（欢迎仪式配置、版本更新区块、危险区）

## 四、小程序

- [ ] 上传体验版（9 页，新增 `bossWar` 战况监控页）
- [ ] 老板微信绑定 `super_admin`（`bindadmin`，开通码 `JuhuoAdmin2026`）→ 首页「👑 进入老板模式」
- [ ] 后台 → 人员管理 → 管理员行「👑 启用老板」（`users.boss=true`，决定入口是否显示）
- [ ] 待重绑的三人（丰炳全/宋子军/程绍君）重开小程序填注册表单 → 后台「⏳待审核」通过
- [ ] 拜访目的三选的服务号文案映射需**重传后**才生效

## 五、真机验收点

**业务员端**：照片三格绝对一样大 + 试听 + 录音控件 + 坐标拍照 + 历史任务 Tab/归档 + 重置清理（连删云存储文件）+ 地图页（天页签/绿线路线/下一家引导条/导航）+ 导入分片断点续跑 + 批次卡比对认领 + 定位校验拦截 + 拜访中单开与取消零痕迹

**老板端**：首页欢迎仪式（canvas 礼花）+ 任务地图 + 战况地图（在线/拜访中，排除实习）+ 我的页 + 虚拟操作 + 新人注册申请推送到微信

## 六、上线前配置（正式版提审）

- [ ] 小程序名称「聚火拜访」（多端产物 `miniprogram/i18n/base.json` 已改；**微信后台的小程序名称仍需改**）+ 上传头像
- [ ] qingyan 初始密码 `123456` 必须改
- [ ] 服务号两个新模板（「新工单提醒」发布任务 / 「工单确认提醒」审核结果）老板已选未添加
- [ ] 未完成的真机项：iPhone 录音 aac、手机地图页
- [ ] 正式版提审（上一版因二期地图页面被拒）

## 附：当前未决/待清理（非部署项）

- 微信开发者工具新增的多端产物未入库：`project.config.json`（`projectArchitecture: "multiPlatform"`）、`miniprogram/project.miniapp.json`、`miniprogram/i18n/`、`miniprogram/miniapp/android/`、`miniprogram/miniprogram/app.miniapp.json`（老板决定：先留着，名字已改「聚火拜访」）
- `_scratch/`（4 个残留脚本）、`TMP/`（7.1MB 图片）按约定用完即删
- 交接文档头部快照停留在 09-09 深夜，09-10（注册审核/老板注册修复/欢迎仪式/壳刷新按钮）尚未沉淀
- `AGENTS.md` 中「小程序 8 页」已过期（现 9 页，含 `bossWar`）
