# 降频与设置开关 · 改造方案（**待老板确认，尚未动手**）

> 起因：2026-09-11 云开发控制台提示「调用次数」用尽（该配额同时管云数据库 / 云存储 / 云函数 / 大模型），小程序与后台会整体不可用。
> 时间线索：**9 月 9 日定位功能上线后消耗陡增** → 定位类高频上报是主因。
> 目标：月调用量从当前估算 **40~55 万** 降到 **8 万以内**（稳在免费额度内），并加上"看得见、能刹车"的开关。
> 原则：① 先止血（不用改代码的开关）② 再根治（打包上报）③ 每项都能单独开关、默认值安全；④ 老版本小程序不回读新配置也不会出错。

---

## 1. 现状盘点（已逐行核对代码）

### 1.1 后台设置页现有的项（`admin/admin.html` 设置页 + `adminapi.setSetting` 的 ALLOWED 白名单）

| 分组 | 设置项 | key | 取值 / 默认 |
|---|---|---|---|
| 📍 定位与拜访 | 拜访定位校验 | `locationCheck` | 阈值 1~500 米；0=关（默认 100） |
| | 正常页面刷新 | `locRefreshInterval` | 30/45/60 秒（默认 30） |
| | 重要页面刷新 | `locKeyRefreshInterval` | 8/12/15/20 秒（默认 15） |
| | 拜访录音上限 | `recordingDurationLimit` | 180/300/600 秒（默认 300） |
| | 拜访时长上限 | `visitDurationLimit` | 1800/3600/7200 秒（默认 3600） |
| | 定位轨迹 | `workStartHour` / `workEndHour` / `offDutyTier` / `trackKeepDays` | 工作时段 7~20 点；非工作档位 5_30/10_60/20_120/30_180；保留 30 天 |
| 📢 任务与通知 | 任务结束审核 | `autoApproveFinish` | 勾选=全完自动通过 |
| | 服务号长期通知 | `mpConfig` | enabled/appid/appsecret/templateId |
| | 过期任务自动归档 | `expireArchiveDays` | 2/3/5 天（默认 3） |
| ⚙️ 系统 | 数据刷新间隔 | `globalRefreshInterval` | 5/10/15/30/60 秒（默认 15） |
| | 任务编号区域码 | `taskRegionCode` | JH05 |
| | （另有）腾讯地图 Key、欢迎仪式配置 | `mpKey` / `welcomeConfig` | — |

### 1.2 缺口（本方案要补的）

| 缺口 | 现状 | 后果 |
|---|---|---|
| **没有"定位轨迹 / 最新位置"开关** | 设置页"定位轨迹"只有工作时段/非工作档位/保留天数三个控件 | 定位上报关不掉，是当前最大消耗源 |
| **工作时段频率写死** | 页面原文："工作时段=采样全速（拜访中 **5 秒**/平时 **30 秒**）" | 无法降频，只能靠"关掉整个轨迹" |
| **逐点上报** | 手机端每个定位点都单独调一次云函数 | 一条轨迹几十上百次调用 |
| **后台刷新只有"间隔"没有"开关"** | `globalRefreshInterval` 5~60 秒 | 想停只能关窗口；开 2~3 台机器即翻倍 |
| **看不到用量** | 后台没有任何用量展示 | 只能等控制台报警，已经太晚 |
| **业务类开关缺失** | 录音/照片/证据门槛等写死在代码里 | 想临时收紧或放宽都得改代码重传 |

---

## 2. 新增设置项设计（云端 `settings` key 一览）

| key | 类型 / 取值 | 默认 | 作用 | 读取方 |
|---|---|---|---|---|
| `locTrackEnabled` | bool | `true` | **定位轨迹开关**（关=不再记录/上传行踪轨迹） | 手机端 `utils/loc.js`、`visits.reportTrack` |
| `locLatestEnabled` | bool | `true` | **最新位置上报开关**（关=后台看不到"人在哪"） | 手机端 `loc.js` |
| `locWorkTier` | `5_30` / `15_60` / `30_120` | **`30_120`** ⬅ 老板定 | **工作时段上报档位**（拜访中/平时） | 手机端 `loc.js` |
| `locPackUpload` | bool | `true` | 上报方式：打包 / 逐点 | 手机端 `loc.js` |
| `locPackSec` | 30 / 60 / 120 | **60** | 打包间隔（秒）：攒够时间或点数就发一次 | 手机端 `loc.js` |
| `locMoveThreshold` | 0~200 米 | **20** | 移动超过 N 米才上报（0=不启用） | 手机端 `loc.js` |
| `adminAutoRefresh` | bool | `true` | 后台自动刷新开关 | 后台 `admin.html` |
| `adminHiddenPause` | bool | `true` | 后台窗口隐藏/切页时暂停刷新 | 后台 `admin.html` |
| `tickIntervalMin` | 10 / 30 / 60 | **30** | adminapi 定时器（拜访超时双态 + 过期补记）频率 | 云开发控制台 cron（见 3.7） |
| `asrPollMin` | 5 / 15 / 30 | **15** | 转写结果轮询频率 | `transcribe/config.json` cron |
| `reviewWatchEnabled` | bool | `true` | 手机端审核观察员轮询开关 | 手机端 `app.js` |
| `recEnabled` | bool | `true` | 现场录音开关 | 手机端拜访页 |
| `photoLimit` | 3 / 6 / **9** / 15 | **`9`** ⬅ 老板定 | 现场照片数量档位 | 手机端拜访页 + `visits.submit` |
| `evidenceRequired` | bool | `false` | 提交是否强制"至少一张照片" | 手机端 + 云端校验 |
| `platformPhotoShow` | bool | `true` | 新客户页是否显示平台参考图 | 手机端客户页 |
| `coordFixEnabled` | bool | `true` | 坐标报错入口开关 | 手机端客户页 + `coordfix` |
| `salesmanScope` | `task` / `all` | `task` | 业务员可见范围：只看本任务客户 / 全部 | 手机端 `tasks` |
| `usageAlertPct` | 60 / 80 | **60** | 用量红线（百分比） | 后台 + 云端统计 |
| （已有）`phoneVisibility` | visible / masked | visible | 电话可见性（**设置页要露出 UI**） | 手机端 |

> 兼容原则：除已拍板的三项（`locWorkTier=30_120`、`photoLimit=9`）外，其余新 key **默认值=维持现行为**；老版本小程序不读新配置 → 行为不变。

---

## 3. 逐项改动清单

### 3.1 A1 定位开关（轨迹 / 最新位置，**设置页必须写清开关后的情况**）

设置页按下面结构改造（老板要求：开关下面直接说明"开之后怎样、关之后怎样"）：

```
定位上报
  ├ 定位轨迹        [ 开 ●— ]
  │    开启：手机端记录并上传行踪轨迹 → 后台可回放全天路线、看到每段停留；
  │          消耗调用较多（按下面的工作/非工作档位采样）。
  │    关闭：不再记录任何行踪轨迹（后台"轨迹回放""当天轨迹"无数据），
  │          但"最新位置"仍按下一项执行 → 定位类调用大幅下降。
  ├ 最新位置上报    [ 开 ●— ]
  │    开启：后台「位置监控」能看到业务员当前在哪、是否在拜访中；
  │    关闭：后台只保留最后一次已知位置（不再刷新）→ 定位类调用降到最低。
  ├ 工作时段 [7] 点 ~ [20] 点      工作档位 [30S/120S（省调用）]
  ├ 非工作档位 [10S/60S（默认）]
  └ 轨迹保留 [30] 天
```

- **后台**：按上面结构改造（两个开关 + 说明文案 + 原三个控件；"定位轨迹"关闭时把"工作档位"置灰）。
- **云端**：`ALLOWED` 加 `locTrackEnabled` / `locLatestEnabled`；`getSettings` 带出；`visits.reportTrack` 收到关闭标志直接返回、不写库。
- **手机端**：`utils/loc.js` 初始化读配置（沿用现有下发链路 `tasks.detail`，把新配置一并带上，**不额外增加调用**）。
- **降幅**：关轨迹 ≈ **70~85%**；轨迹 + 最新位置都关 ≈ **100%**（但后台位置监控会空）。
- **验收**：关轨迹后 `salesman_locations` 当天不再新增 `type:'track'` 记录；关最新位置后 `latest_*` 文档不再更新。

### 3.2 A2 工作时段频率档位（现在写死 5S/30S，**默认改为 30S/120S**）
- **后台**：`工作档位` 下拉：`5S/30S 全速` / `15S/60S` / **`30S/120S（省调用）`** ← 默认。
- **云端**：`ALLOWED` 加 `locWorkTier`；`tasks.detail` 下发。
- **手机端**：`loc.js` 把写死的 5 秒/30 秒换成按档位取值（非工作时段逻辑不变，仍受 `offDutyTier` 控制）。
- **降幅**：5S/30S → **30S/120S ≈ 85~90%**（最省的一档）。
- **代价（要写在设置页说明里）**：轨迹从"每 5 秒一个点"变成"每 30 秒一个点"，只能看大致动线与停留，看不出秒级行踪；配合打包上报后，**回传仍是 1 分钟内准实时**。
- **验收**：切档后同一时段轨迹点数量按比例下降。

### 3.3 A3 打包上报（降幅最大的一条）
- **现状**：每个定位点 → 一次 `visits.reportTrack` 调用。
- **改法**：端上维护缓冲，满足「攒够 `locPackSec` 秒」或「攒够 10 个点」→ **一次**上报 `{ pts: [...] }`；云端兼容数组（旧端仍传单点）。
- **云端**：`visits.reportTrack` 支持 `pts` 数组（一次写一条 `salesman_locations` 文档，`pts` 内含多个点）→ **写入次数同步下降**。
- **风险**：端上崩溃会丢当前未发出的几个点（可接受，轨迹是辅助监督）；包体积 10 点 ×约 40B ≈ 400B，远小于限制。
- **降幅**：**80~90%**（与 30S/120S 叠加后，定位类调用只剩零头）。
- **验收**：同一天轨迹点数不变，但 `reportTrack` 调用次数下降一个数量级（加临时计数核对）。

### 3.4 A5/A6 后台刷新开关 + 隐藏时暂停
- **后台**：设置页"⚙️系统 → 数据刷新间隔"旁加 `自动刷新 [开/关]`；新增 `窗口隐藏时暂停刷新 [开/关]`（默认开；实现：`document.hidden` 时停 `startAuto` 定时器，回前台补一次拉取）。
- **降幅**：关掉 ≈ 该机器 100%；隐藏暂停 ≈ 30~50%。
- **验收**：勾上暂停后最小化后台 → `server.js` 的 `[api]` 日志不再增长。

### 3.5 C 业务开关（一次加齐，以后不用改代码）
| 开关 | 改动点 | 说明 |
|---|---|---|
| 录音开关 `recEnabled` | 拜访页录音区隐藏 + `visits.submit` 拒收 audio | 关掉后只能拍照+文字 |
| 照片档位 `photoLimit`（**默认 9，档位 3/6/9/15**） | 拜访页上限由写死 15 → 读配置；`visits.submit` 同步校验 | 收紧到 3/6 可再省上传调用 |
| 强制证据 `evidenceRequired` | 提交时校验至少 1 张照片 | 默认关，不影响现有习惯 |
| 参考图开关 `platformPhotoShow` | 新客户详情页"平台参考图"区块显隐 | 与"门头照"那条线配套 |
| 坐标报错开关 `coordFixEnabled` | 客户页「📍报错」入口显隐 + `coordfix` 校验 | 排查期可临时关 |
| 可见范围 `salesmanScope` | `tasks.list/detail` 按配置过滤 | 默认只看本任务客户 |
| 电话可见性 `phoneVisibility` | 把已有后端项**露出 UI** | 后端已有、设置页没露 |
| 审核观察员 `reviewWatchEnabled` | `app.js` 轮询开关 | 手机端省调用 |

### 3.6 B 用量显示 + 红线告警
- **难点**：微信云开发**没有对外 API 能查自己的用量**（只有控制台可见）→ 采用"自建趋势统计 + 人工额度"双轨：
  - **自建统计**：云函数实例内**内存计数**（每次 `adminapi`/`visits`/`tasks` 调用 +1），定时器每 10 分钟**合并写一次** `settings.usageCounter{ day, month, total }`（避免"统计本身"造成大量写调用）；
  - **后台展示**：设置页顶部显示 `本月云调用 ≈ X 万 / 额度 100 万（Y%）`，按 `usageAlertPct` 变色；
  - **告警**：到 60% 变橙、80% 变红 + **服务号给老板发一条**（复用 `sendNotify` 链路）。
- **口径说明**：自建统计**只统计我们云函数的调用**，不含数据库/存储调用 → 数值低于控制台真值，**仅作趋势与预警**（页面上要写清）。
- **降幅**：不降消耗，但这是**防止再次"悄悄用尽"**的关键。

### 3.7 定时器频率（配置侧，无需改逻辑）
- **adminapi 10 分钟轮询**：控制台手动建的 cron → 改成 `0 */30 * * * * *`。
- **转写轮询 `transcribePoll`**：`transcribe/config.json` 改 15 分钟一次，且**无 pending 任务时直接返回**。
- **降幅**：≈ 1.4 万 → 0.5 万 / 月。

---

## 4. 实施批次（每批一次部署）

| 批次 | 内容 | 部署动作 |
|---|---|---|
| **批 1（后台+云端）** | A1 / A2（后台部分）/ A5 / A6 / C 全部开关（设置页 UI + 云端 `ALLOWED` + 下发）+ B1 展示位 | bump `APP_VERSION` → 上传后台到云端 → **重传 `adminapi`** |
| **批 2（手机端）** | A1 / A2（30S/120S）/ A3 / A4 端上逻辑（`utils/loc.js`）+ C 开关端上读取（拜访页/客户页/`app.js`） | 上传小程序体验版（真机验证：轨迹照常、打包不丢点） |
| **批 3（告警）** | B2 自建统计 + 红线告警 + 服务号提醒 | 重传 `adminapi` + 控制台调 cron |
| 现场动作（老板侧） | 控制台改 adminapi cron 30 分钟、确认 `transcribePoll`；买不买资源包按需 | 控制台操作 |

---

## 5. 预期效果（对照）

| 阶段 | 月调用量（估算） | 说明 |
|---|---|---|
| 现在 | 40~55 万 | 定位 5S/30S 逐点 + 后台 15 秒轮询 |
| 只关轨迹（A1） | 15~22 万 | 立刻见效 |
| A1 + A2（**30S/120S**）+ A5/A6 | 8~12 万 | 后台+云端改完即可，不用等手机端 |
| 再做 A3 打包 + A4 阈值 | **4~8 万** ✅ | 目标：稳在免费额度内，轨迹仍可用 |
| 加 B 告警后 | 同上，且**提前可预警** | 防复发 |

---

## 6. 风险与注意事项

1. **已拍板的默认值**：`locWorkTier = 30S/120S`（最省档）、`photoLimit = 9`；其余默认=维持现状。
2. **粒度代价要讲清**：30 秒一个点 = 只能看大致动线与停留，看不出秒级行踪；配合打包上报后**回传仍是 1 分钟内准实时**。
3. **老版本兼容**：未更新小程序的老端不读新配置 → 行为不变（后台关轨迹也不会让它报错）。
4. **统计口径**：后台显示的"本月调用"是自建统计，**低于控制台真值**，只看趋势。
5. **别自己给自己加负担**：B 的统计必须走"内存计数 + 定时合并写"，否则统计本身会吃掉几千次写。
6. **上线顺序**：批 1（不动手机端）就能省掉大半；批 2 是深水区，必须真机验证（轨迹是否照常、打包有没有丢点）。
7. **额度恢复**：控制台买"调用次数资源包"（不可退，建议最小档）或等次月 1 日重置；在此之前让业务员**暂停提交**。

---

*本文件为方案稿，未改动任何后台 / 小程序 / 云函数代码。*
