/* 地图页（2026-09-08 二期核心） */
.page { position: relative; width: 100vw; height: 100vh; overflow: hidden; }
.m-wrap { position: relative; width: 100%; height: 100%; }
.m { width: 100%; height: 100%; }
.center-box { position: absolute; left: 50%; top: 45%; transform: translate(-50%, -50%); color: var(--sub); font-size: 14px; font-weight: 400; text-align: center; white-space: pre-line; }

/* 地图小圆钮（2026-09-09 老板定：横幅条下方靠右横排——📍我的位置/↻刷新，纯图标无文字；路况已移除） */
.m-ctrl { position: absolute; right: 14px; top: 104px; display: flex; flex-direction: row; gap: 8px; z-index: 12; }
.mfab { width: 32px; height: 32px; border-radius: 50%; background: rgba(255,255,255,.96); box-shadow: 0 3px 10px rgba(31,36,48,.16); display: flex; align-items: center; justify-content: center; font-size: 15px; }
.mfab.spin { animation: mspin 0.8s linear; } /* 刷新旋转动效（2026-09-09） */
@keyframes mspin { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }

/* 仓库小开关（2026-09-09 老板定：与右侧小圆钮同一行对齐；很小；开=金色高亮，关=整体变淡更直观） */
.wh-switch { position: absolute; left: 14px; top: 108px; height: 24px; line-height: 24px; background: rgba(255,255,255,.7); border-radius: 999px; padding: 0 10px; font-size: 11px; font-weight: 400; color: #A8B0BA; box-shadow: 0 2px 8px rgba(31,36,48,.10); z-index: 12; opacity: 0.55; }
.wh-switch.on { background: #FFF7E6; color: #B97A0B; font-weight: 700; opacity: 1; }

/* 天页签 */
.daybar { position: absolute; top: 14px; left: 0; right: 0; display: flex; justify-content: center; gap: 8px; z-index: 10; }
.dtag { padding: 7px 16px; border-radius: 999px; background: rgba(255,255,255,.92); color: var(--sub); font-size: 13px; font-weight: 400; box-shadow: 0 2px 10px rgba(31,36,48,.14); }
.dtag.on { background: var(--primary); color: #fff; }
/* 第 6 天起纯数字圆标（老板 2026-09-08 定） */
.dtag.dot { width: 28px; height: 28px; padding: 0; display: flex; align-items: center; justify-content: center; font-size: 12px; border-radius: 50%; }

/* 下一家引导条（2026-09-09 老板定：收矮——紧凑高度，不臃肿） */
.nextbar { position: absolute; left: 14px; right: 14px; top: 62px; display: flex; align-items: center; gap: 10px; background: #fff; border-radius: 12px; padding: 6px 12px; box-shadow: 0 4px 16px rgba(31,36,48,.16); z-index: 10; }
.ntxt { flex: 1; display: flex; align-items: center; gap: 6px; min-width: 0; }
.nlab { font-size: 12px; font-weight: 400; color: var(--primary-deep); flex-shrink: 0; }
.nname { font-size: 13px; font-weight: 800; min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.ndist { font-size: 11.5px; color: var(--sub); flex-shrink: 0; }
.nbtn { display: flex; align-items: center; background: var(--blue-tint); color: var(--blue); border: 0.5rpx solid rgba(47,128,237,.28); font-size: 11px; font-weight: 400; padding: 2px 9px; border-radius: 999px; flex-shrink: 0; line-height: 1.4; }
/* 老板模式：业务员下拉选择器（2026-09-10 老板定：只放业务员名，横向尽量紧凑，给左边店名/距离留空间；
   宽度按"三个字 + 图标 + 箭头"算足，长名直接裁切、不出现省略号；点开下拉列表里仍是完整的「业务员 · 任务名」） */
.nbtn-boss { width: 72px; box-sizing: border-box; justify-content: center; gap: 0; padding: 2px 6px; }
.nbtn-boss .nb-ic { flex-shrink: 0; font-size: 10px; }
.nbtn-boss .nb-arrow { flex-shrink: 0; font-size: 10px; }
.nbtn-boss .nb-name { flex: 1; min-width: 0; text-align: center; overflow: hidden; white-space: nowrap; }

/* 底部客户卡（2026-09-09 老板定：动态抬到 TAB 上方 14px；2026-09-10 换成胶囊 TAB 后，抬高量=64+14+14）
   —— 若回滚成矮栏，把 bottom 改回 calc(40px + env(safe-area-inset-bottom) + 14px) */
.custcard { position: absolute; left: 14px; right: 14px; bottom: calc(92px + env(safe-area-inset-bottom)); background: #fff; border-radius: 16px; padding: 14px 16px; box-shadow: 0 8px 24px rgba(31,36,48,.22); z-index: 10; }
.cc-close { position: absolute; top: 10px; right: 12px; width: 22px; height: 22px; line-height: 21px; text-align: center; background: #F0F2F5; color: var(--sub); border-radius: 50%; font-size: 11px; }
.cc-name { font-size: 16px; font-weight: 800; padding-right: 28px; display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
.cc-st { font-size: 11px; font-weight: 400; padding: 3px 10px; border-radius: 999px; background: #FFF4EE; color: var(--primary-deep); }
.cc-st.v { background: #F0F9F2; color: var(--green); }
.cc-st.o { background: var(--blue-tint); color: var(--blue); }
.cc-sub { margin-top: 5px; font-size: 12.5px; color: var(--sub); font-weight: 400; }
.cc-btns { display: flex; gap: 10px; margin-top: 12px; }
.ccb { flex: 1; text-align: center; padding: 11px 0; border-radius: 12px; font-size: 14px; font-weight: 400; }
.ccb.nav { background: var(--blue); color: #fff; }
.ccb.go { background: var(--primary); color: #fff; }

/* 底部 TAB 栏（2026-09-10 定稿：与首页同款悬浮胶囊，位置口径与首页一致=离底 14px；业务员三栏/老板四栏） */
.tabbar {
  position: fixed; left: 14px; right: 14px; bottom: 14px; height: 64px; border-radius: 22px; display: flex; align-items: center;
  background: linear-gradient(180deg, rgba(255,255,255,.99), rgba(248,250,253,.95));
  box-shadow: 0 0 0 1px rgba(23,30,44,.05), 0 14px 30px -10px rgba(23,30,44,.38), inset 0 1px 0 rgba(255,255,255,.95);
  z-index: 30;
}
.tabbar .tb { flex: 1; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 3px; font-size: 11px; color: #88909A; font-weight: 700; }
.tabbar .tb .pill { display: flex; flex-direction: column; align-items: center; gap: 2px; padding: 7px 15px; border-radius: 16px; }
.tabbar .tb .tic { font-size: 19px; line-height: 1; }
.tabbar .tb.on { color: var(--primary-deep); }
.tabbar .tb.on .pill {
  color: #fff; background: linear-gradient(180deg, #FF8A50, #F5531C);
  box-shadow: 0 10px 20px -7px rgba(245,83,28,.75), inset 0 1px 0 rgba(255,255,255,.5);
}

/* ===== 以下为原「矮 TAB 栏」样式，2026-09-10 起停用（保留以便一行回滚：wxml 换回 .tabbar-slim 结构即可） ===== */
.tabbar-slim { position: absolute; left: 0; right: 0; bottom: 0; height: 40px; background: #fff; border-top: 1rpx solid var(--line); display: flex; z-index: 20; padding-bottom: env(safe-area-inset-bottom); box-sizing: content-box; }
.ts { flex: 1; display: flex; align-items: center; justify-content: center; font-size: 13px; font-weight: 400; color: var(--sub); }
.ts.on { color: var(--primary); font-weight: 700; }
